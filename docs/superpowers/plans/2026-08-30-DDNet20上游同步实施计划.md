---
title: DDNet 20.0 上游同步实施计划
date: 2026-08-30
status: active
upstream: 1d507dad90812cd0873b151aac60ad7bff5127b6
---

# DDNet 20.0 上游同步实施计划

## 目标

将 DDNet `20.0..master` 的适用功能、稳定性修复和平台支持按可验证批次合入 QmClient；iOS 是明确必须完成的目标平台。本文替代把旧 `19.9-rc1..master` 标题比对清单直接作为合并输入的做法。

校对基准为 `ddnet-official/master` 的 `1d507dad9081`（2026-09-01）。该基准位于 `20.0` tag `a5a61806e434` 之后 235 个提交；截至 2026-09-01 再次刷新后未发现新的上游提交；每次开始新批次前必须刷新远端并更新基准。

## 决策与边界

- iOS 必须合入：先完成上游 SDL/OpenGL ES iOS bring-up、构建脚本和模拟器验证；原生 Metal iOS 后端作为后续独立阶段，不阻塞首个 iOS 可运行版本。
- `dyl_dev` 已存在选择性同步和 QmClient 私有改动，不能按版本号、merge-base 或提交标题单独判定已覆盖。
- `git cherry -v dyl_dev ddnet-official/master 20.0` 只用于识别精确 patch 等价；带 `+` 的提交仍必须人工检查依赖、revert 和本地行为差异。
- 不直接搬迁上游 CI/release 配置。iOS CI 只在本地 iOS build/模拟器成功后适配 QmClient 的 workflow。
- 当前工作树有并行 Metal、菜单、统计和翻译改动。任何批次不得覆盖这些未提交修改；涉及 `CMakeLists.txt`、渲染后端或 macOS/iOS 平台文件前，先检查后台任务和目标文件的工作树状态。

## 批次顺序

### P0：上游台账与等价审计

状态：基准台账已生成；逐模块校对进行中。

1. 刷新 `ddnet-official/master`，记录 hash、时间和 `20.0..master` 数量。
2. 使用 `python3 qmclient_scripts/generate_upstream_manifest.py --base 20.0 --output docs/superpowers/plans/2026-08-30-DDNet20上游同步台账.tsv` 为全部候选提交记录 upstream hash、主题、`git cherry` 等价状态、模块、处理状态、依赖、QmClient 冲突文件和验证证据。
3. `-` 只标记为“patch 等价待复核”，`+` 只标记为“候选未覆盖”；不得把标题相同作为已合并证据。
4. 每个代码批次完成后回填台账；完成且长期需要追溯的结果进入本文，过程数据放 `tmp/`。

完成条件（台账阶段）：已生成 235 条基线记录，现已全部完成 disposition。`candidate / merge-commit / cherry` 分类只表示来源；`merged-local`、`adapted-local`、`covered-equivalent` 与 `deferred-*` 分别表示实际本地代码、适配、等价覆盖和延期策略，不表示官方 hash 原样进入本分支。

### P1：安全、崩溃和构建正确性

状态：低耦合安全、崩溃、构建和客户端修复已形成独立本地提交并完成桌面构建/C++ 回归；剩余项目仅在有明确行为缺口时建立新代码批次。

以下 git cherry 候选已人工确认由本地实现等价覆盖，不在本批重复 cherry-pick：

- `1f5095abe`：断言输出时间值错误，已由 `5bb8c21e6` 覆盖。
- `50b4dfa02`：编辑器保存文件名输入未定义行为，已由 `5e022ee92` 覆盖。
- `33fbdc853`：GLSL shader compiler 越界读写，已由 `4c0d1c491` 覆盖。
- `c9a9931f8`：异常 shader log 越界读写，已由 `4c0d1c491` 覆盖。

本批实际在工作树实现：

- `bc2c2f930`：修复 map_convert_07 外置 PNG 图像数据生命周期。
- `91eb6b94`：修复空快照 ID 池的可选值解引用。
- `a38ba7bd`：修复 tuning 溢出和 NaN 转换。
- `0b108953`、`a23094c69`：修复 Xcode/libpng CMake 查找。
- `821d5ae4b`：统一校验带引号的 victim 参数，并拒绝空值。
- `e58155fb8`：修复 editor brush picker 松键后的扩展 tile 数据崩溃。
- `c2cea3f30`：避免包络缩放热键在 dialog、popup 或其他文本输入占用时触发。
- `a39ed1834`：设置/命令导出脚本以当前 `SERVER_MAX_CLIENTS = 128` 生成边界。
- `25dc1ab14`：移除 Valgrind `--track-origins=yes`，降低 Linux CI memcheck 的时长和超时波动。
- `5ca808242`：为断言失败分支补充 `[[unlikely]]` 提示。
- `eb96fe096`：为 vanilla antispoof 预连接包补充 chunk header/data 长度边界检查，避免短包读取陈旧缓冲区；已加源码合同回归测试。

后续按依赖继续处理地图加载、网络边界、快照 ID、server 限流、libpng/Xcode/CMake 与 Vulkan/OpenGL 安全修复。协议、快照、地图格式或预测改动必须单独建子批次。

验证：先跑候选相关 C++ 测试或新增最小回归测试；批次代码完成后在确认无后台构建占用时，串行运行 `game-client`、`run_cxx_tests`、`run_rust_tests` 和 `check_gate.py --mode default`。

### P2：iOS 构建基础设施

状态：已完成依赖阶段；iOS 客户端运行时仍由 P3 负责。

- 审计并补齐 iOS `xcframework` 依赖生成、iOS libs build 脚本、toolchain 和 `BUILDING-ios.md`；资源工程属于 P3。
- 对应上游历史包括 `6bb2d0af1`、`fa5500661` 及其 PR 汇总；当前本地的 `covered(sync)` 记录不替代对实际文件存在性和可执行性的验证。
- 不先修改发布 workflow；先在本地完成 simulator build，随后再接入 QmClient CI。

完成条件：已满足。`tmp/ios-libs` 从空目录生成了设备 arm64、模拟器 arm64/x86_64 三种 slice 和 8 个 `.xcframework`；脚本只依赖文档列出的工具和 Xcode SDK。

### P3：iOS 客户端运行时

状态：P3A/P3B 已完成并提交（`1eafa9ed3`、`e5325e77b`），已完成编译/测试证据；P3C simulator/真机验收待执行。

- 已按本地 owner 适配上游 `936f2b7c1` 所需的客户端入口、文件系统、网络、输入、通知、后台前台生命周期和资源打包；`51a2ea409` 仅作为 merge wrapper 记录，`eeaf76158` 的 CI workflow 仍延期，不会将其误述为直接 cherry-pick。
- 首个版本使用上游 SDL/OpenGL ES 路径；不得假定 macOS 原生 Metal 实现能直接用于 iOS。
- 当前已得到 Apple Silicon simulator 的 Xcode compile/link/bundle 证据；仍须用 simulator 和真机分别验证启动、资源加载、触控输入、联网、后台恢复、截图和退出。

完成条件：iOS simulator smoke 与至少一台真机 smoke 有证据；运行时失败不影响 macOS、Windows、Linux 和 Android 构建。当前不满足，原因是本机未安装 simulator runtime，asset catalog、安装/启动和真机验证尚未完成。

### P4：128 人与 0.6/0.7 兼容

状态：128 人主体已大规模合入；后续上游补丁只做残余差异审计和混合会话运行时验证，不再重复实现 128 人主体。

- `dae667a56` 已完成 128 人主体；`01bbf415e`、`2185493e2`、`54ec87f53`、`38e005621` 已补齐旧 DDNet、映射、server-info 和 timeout 兼容。当前 `CPlayerMapping`、`IServer::Translate`/`ReverseTranslate`、`FakeSnap` 及 128 人 scoreboard 均有对应实现。
- `f817a14fc` 至 `752b09ca4` 的后续 0.6/0.7 mapping 提交仅作为差异审计输入：已有实现覆盖其核心意图，不能因 hash 不同声称逐 patch 等价；只对确认未吸收的行为建立独立补丁。
- `bc927c2a6` 的 128 人 scoreboard 意图已由当前 `MAX_CLIENTS` row-plan 和超过 64 人时的三列布局覆盖；剩余的是实际 128 人 UI smoke，不是 scoreboard 主体重做。
- 必须有混合 0.6/0.7/128 人和 timeout reconnect 运行时验证，不能只凭单元测试标记完成。

### P4A：GameInfoEx 与 team schema

状态：协议与客户端适配已完成并形成独立提交；混合旧/新服务器运行时验证仍待执行。

- `c181dfbd1` 适配 `a9c6d2ef`、`4e7c31935`、`2612f425f` 和 `a8d5e0c7` 的 GameInfoEx 队伍规模协议；`c931bf276` 补齐菜单/计分板对服务器广播队伍上限的消费。
- `GameInfoEx` 增加最小/最大队伍人数和 `m_NumDDRaceTeams`；服务端广播，客户端按字段长度安全读取。
- 旧 v12/过渡对象保留 `SUPPORTS_128_TEAMS` 兼容回退；非法队伍数量归一化到 65 人 schema。
- TeamSuper、锤击检测、旧 0.6 team-state 和菜单/计分板队伍上限均使用动态 schema/服务器广播值。
- 新增 17/65/129 队伍 schema 与非法值回归测试，并将测试登记到 CMake（`a560ea5a4`）。

验证：`generate_protocol`、`game-client`、C++ 全量 `2916/2916`、Rust 全量测试通过；default gate 仅剩既有 `src/test/net_test.cpp:45` clang-format 漂移。

剩余：真实 0.6/0.7/128 混合会话、旧客户端兼容和 timeout reconnect 运行时验证。

### P5：编辑器、地图、客户端 UI 与输入

状态：独立候选可继续审计；不依赖重新实现 128 人主体。

- 先合独立崩溃/边界修复：brush picker、font typer、envelope hotkey、地图声音、触摸坐标和可见性。
- 官方 editor tabs/UI 重构单独审计；QmClient 已有实现，只吸收确认缺失的状态管理和修复，禁止整组覆盖。

### P6：Web、Vulkan、翻译与 CI

状态：待前述客户端稳定批次。

- Emscripten HTTP 修复与 Web 构建链成组处理。
- Vulkan 的安全/正确性修复可随 P1 吸收，HDR/headless 等能力扩展留在独立批次。
- 翻译在源码功能稳定后按 `qmclient-i18n-workflow` 生成；版本号升级和发布 CI 只在覆盖范围达到发布标准后执行。

## 停止条件

- 当前批次与未提交 Metal、菜单、统计或翻译文件冲突。
- 上游补丁触及协议、地图格式、物理、预测、快照或回放语义，且未建立独立子计划。
- iOS build 只能通过隐藏环境变量、工作区外依赖或跳过资源打包成功。
- 未完成适用验证时，不得升级 `GAME_RELEASE_VERSION_INTERNAL` 到 20.0。

## 证据记录

每个批次在本文件追加：upstream hash、实际改动文件、等价审计、构建/测试命令和结果、运行时或真机验证、未覆盖 gap。未运行的检查不得标记为通过。

### 2026-09-01：上游刷新与 graphics/iOS 校验同步

- 上游已刷新到 `1d507dad9081`，相对 `20.0` 为 235 个提交；新增 6 个提交均已登记，不能把 merge wrapper 当作独立代码合并。
- `eb56d7e2b` 已适配到当前 iOS bundle：`IOS_DEPLOYMENT_TARGET` 默认值由 `12.0` 提升为 `15.0`，并在 `Info.plist` 增加 `UIRequiresFullScreen` 和 CoreBluetooth 用途说明。既有 simulator bundle 仍是旧的 `12.0` 产物，需在安装 runtime 后重跑默认 asset-catalog 构建和 App Store 校验。
- `b3bc9f18c` 已适配并形成本地提交 `d699c86a1`：graphics API 改为 `SetColor2`/`SetColor4`，移除旧 `CColorVertex`/`SetColorVertex` 和未使用的当前四边形颜色接口；保留 QmClient `SetColor4Raw`，并迁移 graph、settings、render_map 与 moving_tiles 调用点。未改动并行 Metal backend 文件。
- 验证：后台无 CMake/Xcode/Metal 构建命令占用；`cmake --build cmake-build-release --target game-client -j 14` 通过；`cmake --build cmake-build-release --target run_cxx_tests -j 14` 通过，2914/2914；`git diff --check` 通过。

当前 gap：graphics 仅完成桌面编译和 C++ 回归，尚无各后端视觉 smoke；iOS 仍缺 simulator runtime、AppIcon 构建、安装/启动和真机验证；GameInfoEx 协议批次未合入，128 人主体不重复实现，只保留混合会话验证。

### 2026-09-01：GameInfoEx 队伍规模协议批次

- 上游 `a9c6d2ef`、`2612f425f` 及其 merge wrapper `4e7c3193`、`a8d5e0c7` 已完成本地适配，形成提交 `c181dfbd1`；`c931bf276` 补齐菜单/计分板消费，`a560ea5a4` 登记回归测试；这不是重新实现 128 人主体。
- `GameInfoEx` 现在广播 `m_MinTeamSize`、`m_MaxTeamSize` 和 `m_NumDDRaceTeams`；客户端按收到的字段计算 `TeamSuper()`，并保留旧版 version-12 `SUPPORTS_128_TEAMS` 位及旧 0.6 16 人 team-state 的兼容回退。
- 新增 `GameInfoEx` schema/TeamSuper/hammer super-team 回归测试；协议生成已通过。
- 验证：`cmake --build cmake-build-release --target game-client -j 14` 通过；`cmake --build cmake-build-release --target run_cxx_tests -j 14` 通过，2916/2916；`git diff --cached --check` 通过。
- quick gate 结果为 `10 pass / 1 fail`，唯一失败是并行改动 `src/test/net_test.cpp:45` 的既有格式漂移，未归因于本批。

当前 gap：尚未完成旧 20.0 server、新客户端以及新 server、旧 0.6/0.7 客户端的真实混合运行时验证；该批次也未改变 128 人主体实现。

### 2026-08-30：P1 首批与 P2 依赖验证

- 上游 `bc2c2f930` 已吸收：`map_convert_07` 改为持有 `CImageInfo`，消除外置 PNG 嵌入时的悬空数据指针。
- 上游 `91eb6b94` 已吸收：快照 ID 池耗尽时不再对空 `std::optional` 解引用；新增 `CharacterHandlesEmptySnapshotIdPool`。
- 上游 `a38ba7bd` 按本地 `CTuneParam` 接口适配：超范围值和 NaN 统一映射为 `INT_MIN`；新增 `Tunings.OutOfRangeBecomesIntMin`。
- 上游 `0b108953` 与 `a23094c69` 已吸收；`96d35b3f` 已被上游 `76444d664` 撤回，未合入。
- P2 已加入 iOS 三 slice 依赖构建与 `.xcframework` 聚合，覆盖 curl、freetype、ogg/opus/opusfile、png、SDL、sqlite3；`docs/BUILDING-ios.md` 明确这是依赖阶段。Xcode 26.6 的 iPhoneOS 与 iPhoneSimulator SDK 已在本机发现。
- 实际从空目录运行 `M4="$(brew --prefix m4)/bin/m4" scripts/compile_libs/gen_libs.sh tmp/ios-libs ios`：成功生成 8 个 `.xcframework`。每个 bundle 均包含 `ios-arm64` 和 `ios-arm64_x86_64-simulator` 库，中间 slice 目录已清理。
- 验证中补齐两个脚本兼容性缺口：为 FreeType/Ogg 传入上游已有的 `CMAKE_POLICY_VERSION_MINIMUM=3.5`，使其可在 CMake 4.4 配置；SQLite 下载改为优先使用 macOS 自带 `curl`，保留 `wget` 回退。
- P3 审计结论：`936f2b7c1` 不能直接应用。当前树缺少其 `src/base/net.cpp`、`src/base/net.h` 与 `src/base/os.cpp` 路径，且 `client.cpp`、SDL 后端、CMake 与现有 QmClient/Metal 结构存在实质差异。必须以独立适配批次处理网络恢复、生命周期、SDL/OpenGL ES、资源打包和 Rust iOS target，之后再跑 simulator 与真机 smoke。
- P3A/P3B 已在当前工作树完成：加入 iOS CMake/Xcode app bundle、xcframework/Rust target、`src/ios/` entry、资源复制、Documents storage、横屏、Files URL export、后台配置保存与 iOS EPIPE socket 恢复。网络恢复只有在三个 socket 都成功重建后才恢复主/分身连接；失败保持 offline，避免重连已标记 broken 的旧 socket。
- P3A/P3B 已形成提交 `1eafa9ed3`，并以 `e5325e77b` 补入 iOS 平台入口文件；该批只改 iOS 平台路径及其现有 owner，没有修改并行的 macOS Metal backend。P3C 仍未完成，不能把 bundle build 当作运行时或真机验收。
- `2026-08-31` P1 低耦合批次：包络编辑器在 `DIALOG_NONE`、无 popup、无任意活动行输入时才响应 `S` 缩放；两份设置导出脚本将 `SERVER_MAX_CLIENTS` 解析为 128；Valgrind memcheck 不再传递 `--track-origins=yes`；`dbg_assert` 失败分支标注为 `[[unlikely]]`。

验证：

- `bash -n scripts/compile_libs/_build_common.sh scripts/compile_libs/cmake_lib_compile.sh scripts/compile_libs/gen_libs.sh scripts/compile_libs/make_lib_opusfile.sh scripts/compile_libs/make_lib_sqlite3.sh`：通过。
- `python3 -m py_compile qmclient_scripts/generate_upstream_manifest.py`：通过。
- `cmake --build cmake-build-release --target game-client -j 14`：通过。
- `cmake --build cmake-build-release --target map_convert_07 -j 14`：通过；仅有现有依赖 SDK 部署版本链接警告。
- `cmake --build cmake-build-release --target run_cxx_tests -j 14`：2884/2884 通过。
- `cmake --build cmake-build-release --target run_rust_tests -j 14`：通过；包含 15 个 `qm_update` 测试和 24 个 doctest。
- `python3 qmclient_scripts/gate/check_gate.py --mode default`：通过。
- `cmake -G Xcode -S . -B tmp/cmake-xcode-png-prefix -DCMAKE_BUILD_TYPE=Release -DPREFER_BUNDLED_LIBS=OFF -DCLIENT=OFF -DSERVER=OFF -DTOOLS=OFF`：通过；`libpng` 头文件和库均来自 `/opt/homebrew/opt/libpng`。
- `M4="$(brew --prefix m4)/bin/m4" scripts/compile_libs/gen_libs.sh tmp/ios-libs ios`：通过；8 个 iOS `.xcframework` 的设备和模拟器架构已用 `xcrun lipo -info` 核验。
- `cmake --build cmake-build-release --target run_cxx_tests -j 14`：通过，2887/2887；包含 iOS runtime 契约。
- `IOS_USE_ASSET_CATALOG=OFF scripts/ios/cmake_ios.sh sim QmClient org.qmclient.client Release tmp/cmake-ios-sim-fix`：Apple Silicon simulator 的 Xcode compile/link/bundle 验证；只用于无 simulator runtime 时的诊断，不代表 AppIcon、安装或启动通过。
- `python3 qmclient_scripts/gate/check_gate.py --mode quick`：通过；不替代 iOS runtime/真机验收。

Gap：P3C 尚未完成：本机未安装 iOS simulator runtime，默认 asset catalog、simulator 安装/启动、GLES、触控、联网、后台恢复、截图、Files export、退出、真机和 CI 均未运行；`map_convert_07` 尚无含外置 PNG 地图的端到端转换样本。

### 2026-08-31：P1 低耦合候选与台账校对

- `75f1ccc48`、`212d08cfe`、`48b3aea80` 和 `dd35e93d3` 已在历史提交 `152d2029e` 按本地 KCP/FakeNet 接收路径适配；不应再标记为 `unreviewed`。`git cherry` 的 `+` 仅表示不是逐 patch 等价。
- 本批新增的 `c2cea3f30` 已形成父仓库提交 `eea5c224e`；`a39ed1834`、`25dc1ab14` 与 `5ca808242` 仍按各自本地适配提交记录维护。
- 设置导出脚本的完整 HTML 生成仍会被现有 `qm_global_card_order` lambda 注册回调解析失败阻断；该故障早于本批，`parse_settings` 的 `SERVER_MAX_CLIENTS` 分支另行验证。
- 尚未完成编辑器 GUI smoke，也未在 Linux Valgrind 环境运行 integration test；这两项不得由 C++ 构建或 gate 替代。

### 2026-08-31：网络游标与快照架构审计

- `6c0126f99` 已按当前网络实现适配：`CPacketChunkUnpacker` 保存下一个 chunk header 偏移，避免每次从包头重复扫描；保留 QmClient 现有 vital sequence 统计、重发去重和 malformed header 边界检查。
- `9526f5f6d` 已按当前服务端投票链适配：`CPlayer` 保存最后发送的投票节点；新连接、删除投票和清空投票时清空游标，追加投票仍可沿链继续发送。
- `8fb321d0a` 不直接合入：QmClient 已使用 Rust `CSnapshotBuilder`，上游 C++ `NewItemRaw` 不存在；Rust `type_id_from_i32` 已在容量检查前解析 UUID 类型，语义由另一实现覆盖。
- `98f0064af`、`7c366145d`、`f8cd61af` 经源码审计确认当前 OpenGL/Vulkan 后端已有等价实现，不重复 cherry-pick。

验证：新增 `Network.UnpackChunks` 回归测试；`git diff --check` 和 Python 脚本编译通过。网络/投票完整 C++ 测试、客户端构建和默认 gate 待本轮串行验证。

### 2026-08-31：0.7 ServerInfo 适配与后续协议批次定级

- `8c73e5d60` / `f8eb4ceab`：按当前 QmClient `CacheServerInfoSixup` 结构完成本地适配。0.7 server-info 在 128 人服务器上限制为 `LEGACY_MAX_CLIENTS`，重新计算可表示的玩家/客户端数量，限制 max-player/max-client，并保证序列化客户端列表与报告数量一致；保留 QmClient 现有 server-info 重打包路径。
- `f817a14fc`、`aefb9b0f1`、`60d9886cf`、`1a1e165e7`、`31a6120f4`、`752b09ca4`：当前 128 人 mapping、timeout、FakeSnap 与旧客户端路径已覆盖核心行为；后续只审计这些提交与本地实现的残余差异，并补混合 0.6/0.7/128 会话验证，不再建立“128 人主体待实现”的原子批次。
- `2612f425f` / `4e7c31935`：标记为 **deferred-protocol-batch**。`GameInfoEx` 的 team capability/schema 改动需与生成协议代码、旧客户端兼容和 team-score 语义一起验证，不能混入普通稳定性修复。
- `bc927c2a6` / `a3f21cb33`：当前 QmClient row-plan 已遍历 `MAX_CLIENTS`，非 team scoreboard 在超过 64 人时使用三列布局；标记为源码等价覆盖，保留 128 人实际 UI smoke gap，不直接套上游 UI patch。

本轮验证：后台无并行构建进程；`cmake --build cmake-build-release --target game-client -j 14` 通过；`cmake --build cmake-build-release --target run_cxx_tests -j 14` 通过（2889/2889）；`python3 qmclient_scripts/gate/check_gate.py --mode quick` 通过（11/11）。仍缺 0.6/0.7/128 混合版本真实运行时验证、GameInfoEx 旧客户端兼容验证和 iOS P3C simulator/真机 smoke。

### 2026-08-31：低耦合安全与平台辅助修复

- `063018148`：地图声音统一使用 `snd_enable && snd_game && !cl_snd_mute_map_sound` 判定；关闭声音时立即停止已播放的地图声源，避免配置关闭后仍残留播放。
- `9931d2dc9`：本地地址判断覆盖 IPv4 私网、IPv6 ULA、链路本地、loopback 与 WebSocket 地址类型；localhost fallback 改为直接填充地址结构。
- `dc7549396`：集成测试将 `conn_timeout` 随 timeout multiplier 缩放，保留现有 Valgrind 20x 逻辑。
- 上述改动与 `eb96fe096` 预连接包边界修复共同完成本轮验证；仍未运行 Linux Valgrind integration test。

### 2026-08-31：低风险候选复核与 SQLite 适配

- `b68c684a2` 已按当前 `CServerBrowserPingCache` 实现适配，加载前复位 SQLite prepared statement；其 merge wrapper `10e5321d9` 标记为 `covered-by-b68c684a2`。
- `cee0914d6` / `b943a8711` 已确认由本地 `5373d7486` 及 `6c0126f99` 的 unpacker reset/offline guard 覆盖。
- `5cd375498` / `7879324b8` 已确认由本地 `e44c70b62` 覆盖，`kick` 使用 victim 解析并支持 `all`。
- `59784659d`、`d429ac720`、`297a2a486` 已确认由本地短 demo 跳转修复覆盖。
- `250a390549` 不直接合入；其触摸/viewport 修复依赖当前 Metal 与 iOS 输入路径，列为 `deferred-platform-adaptation`，待 P3C 运行时验证后单独适配。

### 2026-08-31：低耦合覆盖校对与验证收口

- 刷新 `ddnet-official/master`：仍为 `944ac7b28ef1`（2026-08-30 16:24:47 UTC），`20.0..master` 仍为 225 个提交；本轮未发现新的上游提交。
- 台账新增确认的本地等价覆盖包括 CMake 安全/clang 选项、demo 退出保护、spectate 菜单可读性、`sv_vote_majority` 帮助文本、macOS 鼠标焦点、projectile team mask、以及对应 merge wrapper；不重复 cherry-pick。
- 当前工作树已有 `4318df0fc` resend 请求限速、`8b5d3b7a8` 服务端未验证地址回复预算和 `c4ba80c69` 单客户端 map/pre-input 预算的本地适配；代码保留 KCP/FakeNet 差异，已完成 game-client、全量 C++ 测试和 quick gate 验证，运行时压测仍待专项批次。
- 验证：无后台构建进程；`git diff --check` 通过；`cmake --build cmake-build-release --target game-client -j 14` 通过；`cmake --build cmake-build-release --target run_cxx_tests -j 14` 通过（2889/2889）；`python3 qmclient_scripts/gate/check_gate.py --mode quick` 通过（11/11）。

### 后续合并优先级

1. **P3C iOS 运行时验收（最高）**：安装可用 simulator runtime，完成默认 AppIcon、安装/启动、GLES、触控、联网、后台恢复、Files export、退出和至少一台真机 smoke；`250a390549` 的触摸/viewport 修复在此之后适配。
2. **P1 网络与服务端资源预算**：补充 resend 限速、server-info/vanilla antispoof 回复预算和 map/pre-input 预算的回归测试；确认 KCP、FakeNet、0.6 路径不改变现有行为后再收口。
3. **128 人残余兼容与运行时验证**：以现有 mapping/FakeSnap/server-info/scoreboard 为基线，逐项审计上游 follow-up；优先验证混合 0.6/0.7/128 会话和 timeout reconnect，只为实证缺口建立补丁。
4. **GameInfoEx team schema**：生成协议代码、旧客户端兼容、team-score 语义和混合客户端运行时验证完成后再合入。
5. **P5 编辑器/地图/UI 修复**：font typer、UI clipping、credits 对齐等独立修复；地图 loading bar 已完成本地适配，仅待地图运行时 smoke；不覆盖 QmClient 自有 editor tabs/UI 架构。
6. **P6 Web/Vulkan/翻译/CI**：Emscripten HTTP 与 Web 构建链成组处理；Vulkan 能力扩展、翻译刷新和 CI workflow 放到前述运行时批次稳定后。

当前不建议：直接把 `20.0..master` 225 个提交整体 merge、提升版本号到 20.0、或在未完成 iOS P3C/协议混合验证前宣称“完整同步 20.0”。

验证：

- `cmake --build cmake-build-release --target game-client -j 14`：通过；仅有现有 macOS SDK 部署版本链接警告。
- `cmake --build cmake-build-release --target run_cxx_tests -j 14`：通过，2889/2889。
- `python3 qmclient_scripts/gate/check_gate.py --mode quick`：通过，11/11。

Gap：尚未运行 Linux Valgrind integration test、iOS simulator/device smoke，以及 0.6/0.7/128 混合协议运行时验证；GameInfoEx schema 仍属于独立后续候选，128 人 scoreboard 主体已存在。

### 2026-09-01：MOTD 与重要提醒触摸关闭

- 上游 `c5ad692163` / `b67775e6bf` 已按当前 QmClient 输入所有权适配，形成提交 `311ab3555`。
- `CComponent::OnTouchState` 改为允许组件消费并移除手指；`CGameClient` 每帧复制 touch-state，触摸控件消费后清空，后续组件不会重复处理。
- MOTD 支持在提示框外触摸关闭；重要提醒在最短显示时长后支持触摸关闭，并保留键盘 Escape/Tab 行为。

验证：`game-client` 构建通过；C++ 全量测试 `2919/2919` 通过；Rust 全量测试通过；默认 gate 通过 12 项，唯一失败仍为既有 `src/test/net_test.cpp:45` 格式漂移。

Gap：尚无 iOS simulator/device 真实触控 smoke；`250a390549` 的 5:4 viewport 坐标修复仍待 P3C 后单独适配。

平台结论：Apple 官方将 `CAMetalLayer`/自定义 Metal view 作为 UIView/NSView 的独立承载路径；SDL 官方同时支持 iOS 与 Metal/OpenGL，但当前 QmClient iOS 适配走 SDL/OpenGL ES。macOS 原生 Metal 后端不能直接复制到 iOS，仍需单独适配 layer、drawable/present、shader/metallib、能力协商、资源恢复和 simulator/真机验证。

### 2026-08-31：存档完整性与平台专属候选审计

- `c1a83f17e` 已按当前 QmClient 存档所有权完成本地适配：`CSaveTeam::GetString()` 在序列化结果超出固定缓冲区时返回 `nullptr`，dry-save、SQL save 和 TeeHistorian 路径均拒绝写入截断存档；保留现有中文错误提示。其 merge wrapper `a01b524762` 与大队伍 rank 修复一并由 `43e40188d`、`76ed91f0e` 覆盖。
- `5d449febc` 已由 `76ed91f0e` 完成本地适配：队伍 rank 消息严格控制在 256 字节 chat 上限内，超出姓名以真实 SQL 队员总数摘要；这不涉及既有 128 人 scoreboard 或协议主体。`game-client` 构建、2909/2909 C++ 测试与 quick gate（11/11）均通过。
- `8e57ffcb4`、`98ca8c95f`、`a073f12ab`、`b53c14c05`、`b611680ed`、`021dfd5c0` 均为 Emscripten HTTP 专属修复。当前 QmClient 没有 `http_emscripten.cpp/.h` 或对应构建路径，标记为 `deferred-platform`，待 Web/Emscripten 批次成组适配，不能套用桌面 curl 实现。
- `d3810ba518` 将 IPv4/IPv6 TOS 改为 DSCP EF，属于 QoS 策略变化；QmClient 当前保留 LOWDELAY 行为，标记为 `deferred-policy`，需单独决策后再改。

本轮验证：确认无后台构建任务后，串行运行 `cmake --build cmake-build-release --target game-client -j 14`（通过，仅现有 macOS SDK 部署版本链接警告）；`cmake --build cmake-build-release --target run_cxx_tests -j 14`（2889/2889 通过）；`git diff --check`（通过）。

新增 gap：存档超长边界尚无专门单元测试；网络预算仍缺压测；iOS P3C、0.6/0.7/128 混合运行时、GameInfoEx 旧客户端兼容、Linux Valgrind integration 仍未完成。

### 2026-08-31：reload 与低耦合编辑器/构建修复

- `a9ea04925` 已由本地提交 `311ab3555` 覆盖：dummy 连接收到 `SV_READYTOENTER` 时调用 `EnterGame`，避免 reload 后分身连接停在握手阶段；该修复与 iOS touch-state 适配同批落地。
- `13ec5da02`、`7fa25fc16` 的 font typer 行首/边界修复已由当前 `m_LineStart`、右边界光标和 `PlaceTile` 路径覆盖；`22e9a486d` 作为 merge wrapper 标记为 `covered-by-e58155fb8`。
- `bc7b3225a` 的 UI clip 交集宽高计算已存在于当前 `CUi::ClipEnable`；`86955479e` 及其 merge wrapper 已由当前 CMake 的统一 `LIBS` 链接路径覆盖。
- `6dc7aae63` 已确认由 QmClient 历史 `d62e5de55` 覆盖；`5cfb6f5e3` 已由两份 settings export 脚本的 128 人边界适配覆盖；`9a599f250` 已由 `5ca808242` 的 `dbg_assert` unlikely 标注覆盖。

本轮新增代码验证：新增 CMake in-source guard 后，`game-client` 构建通过；C++ 全量测试 `2889/2889` 通过。台账后续校对已将 225 条记录全部补齐 disposition；剩余延期项仍按平台、协议和策略边界保留。

### 2026-08-31：历史同步覆盖回填

- 已执行 `git fetch --no-tags ddnet-official master`：上游仍为 `944ac7b28ef1`（2026-08-30 16:24:47 UTC），`20.0..master` 仍为 225 个提交；本次没有新增上游基线。
- 已复核 P3C 前置条件：`xcrun simctl list runtimes` 未列出任何已安装 runtime，故无法执行 simulator 安装、启动、GLES、触控或网络 smoke；该 gap 继续阻塞 iOS 运行时验收，不以 compile/link/bundle 替代。
- `677e4f7f3` 的服务端 team-mask 惰性构造已由可达的本地提交 `54ec87f53` 覆盖；当前实现仅在确有声音事件时计算 mask，保持 128 人服务端语义。
- `05ab16f91`、`945456bd1`、`19e7d08a9`、`7cf4372da` 和 quad visibility 的 `762732489` / `6fa9e4c7a`，分别已有可达的上游等价提交或本地 follow-up；相应 merge wrapper 同步回填台账。
- `6f47c5b00` / `3ffc300b4` 是已覆盖安全修复的 merge wrapper；`abfe01656` 的 own-nameplate-only 渲染已由当前 QmClient 的 `RenderNames` gate 覆盖；`a65e97729` 的 in-source CMake guard 已有本地适配。

### 2026-08-31：macOS Server.app 可用性适配

- `d2b784139` 已按当前 DMG 包装路径适配：Server.app 的 Resources/data 现在包含 `autoexec_server.cfg`，使“Use default config”不再依赖包外文件；Server launcher 使用黑色背景、白色等宽输出，避免深色系统外观下控制台不可读。
- 实现保持上游意图且不改服务端默认配置、协议或运行时控制流；只影响 macOS DMG 的 Server.app 资源和 launcher 的本地呈现。

验证：确认没有后台构建占用后，`cmake --build cmake-build-release --target game-server-launcher -j 14`、`cmake --build cmake-build-release --target game-client -j 14` 均通过；`cmake-build-release/testrunner` 为 `2891/2891`；`cmake --build cmake-build-release --target run_rust_tests -j 14` 通过；`python3 qmclient_scripts/gate/check_gate.py --mode quick` 为 `11/11`。仍存在既有 macOS 11.0 对 Homebrew 26.0 依赖的链接警告，非本批引入。
- 本次仅做源码和可达历史审计，不额外触发构建；前述构建与测试证据仍适用于当前工作树中已存在的实现。

### 2026-08-31：激光门预测同步

- `ac566d06f` / `9d8a20bd0` 已按当前预测世界适配：door 从相邻 `ENTITY_LASER_*` 地图实体恢复配置长度，door 碰撞在删除过期 snapshot 实体后按地图顺序重建，避免旧 door 清空仍有效的重叠碰撞。
- 激光门改由 `m_PredictTiles` 控制，保留普通武器 laser 的 `m_PredictWeapons` 语义；当 door 无法从地图恢复长度时，拒绝零长度或超过地图宽高上界的快照长度，防止每个 snapshot 执行无界碰撞遍历。
- 已形成独立本地提交 `3f6d2b1e4`，只包含 `src/game/client/prediction/entities/door.*` 与 `src/game/client/prediction/gameworld.*` 四个文件。
- 验证结果：后台构建结束后串行执行 `cmake --build cmake-build-release --target game-client -j 14`，通过（仅有现有 macOS SDK 部署版本链接警告）；`cmake --build cmake-build-release --target run_cxx_tests -j 14`，通过，`2901/2901`；本批四个预测文件的 `fix_style.py -n` 与 `git diff --check` 通过。仓库 quick gate 为 `10 pass / 1 fail`，唯一失败是既有无关格式漂移 `src/game/client/components/qmclient/qmclient.h:5`。
- 运行时 gap：尚无包含激光门地图的客户端人工 smoke 或专门回归测试；该批次只完成编译和全量 C++ 回归。

### 2026-08-31：皮肤度量与 econ 认证边界

- `03407db9e6`：按当前 QmClient 的 `PrepareSkinData` 复用路径适配 `Data.m_Metrics.Reset()`，覆盖下载皮肤先加载缓存 PNG、再加载 HTTP PNG 的两次准备流程；新增 extrema reset 和源码顺序回归测试。
- `045cce7561`：适配 econ 认证失败边界；`ec_bantime != 0` 时先加入地址封禁，随后无条件断开当前连接，`ec_bantime == 0` 时仍只断开不封禁。新增源码合同测试；真实 TCP/重连集成验证仍待专门测试夹具。
- `4b72deffe6`、`b427813d2`：仅为上述两个修复的上游 merge wrapper，由本地适配覆盖，不单独 cherry-pick。

### 2026-09-01：地图声音与网络资源预算批次

- `063018148` 已形成独立本地提交 `a3d77b5a1`：地图声音统一使用 `snd_enable && snd_game && !cl_snd_mute_map_sound`，关闭任一声音开关时停止已经创建的地图声源；不改变地图声音资源格式或播放时序。
- `6c0126f99`、`4318df0fc`、`8b5d3b7a8`、`eb96fe096`、`c4ba80c69` 已形成独立本地提交 `070f529e2`：chunk 解包使用游标、resend 请求合并并限速、无连接 server-info/vanilla antispoof 回复限额、预连接 chunk 边界检查，以及单客户端地图分片/pre-input 预算。保留 QmClient 的 KCP/FakeNet 接收路径和 128 人兼容分支。
- `9526f5f6d` 的投票选项游标已由本地提交 `da2e95f59` 覆盖；本轮全量测试重新核验。

验证：

- `cmake --build cmake-build-release --target run_cxx_tests -j 14`：通过，2913/2913。
- `python3 qmclient_scripts/gate/check_gate.py --mode quick`：通过，11/11（本轮代码提交前已完成；提交后未重复运行，未将其误述为新提交专属证据）。
- `git diff --cached --check`：通过；地图声音和网络提交均未混入并行文档、Metal、iOS 或翻译改动。

Gap：网络预算尚无 Linux Valgrind integration 和高并发压测；地图声音没有音频设备级人工 smoke；iOS P3C、0.6/0.7/128 混合会话和 GameInfoEx schema 仍未完成。

### 2026-09-01：网络解包异常路径回归

- 在 `070f529e2` 的网络预算批次之后，补充 `Network.UnpackChunksRejectsTruncatedHeadersAndPayloads`，覆盖普通头、vital 头和 payload 越界三类 malformed packet。
- 本项只证明 `CPacketChunkUnpacker` 的边界拒绝和游标终止行为，不替代高并发压测、旧协议混合会话或运行时网络验收。
- 验证：`cmake --build cmake-build-release --target run_cxx_tests -j 14` 通过（2914/2914）；`git diff --check -- src/test/network_test.cpp` 通过。

### 2026-09-01：地图加载进度条实际代码合并

- 上游 `e8c2df1d5` 已按 QmClient 当前分线程渲染加载合同完成本地适配，不是仅更新台账。
- `CMapRenderer::Load` 按当前 `ERenderType` 实际参与的有效逻辑层统计 `TotalWork`，每层初始化完成后报告 `CompletedWork/TotalWork`；背景、前景、无效层和空地图保持分母一致，并覆盖实体层自行实现 `Init()` 的情况。
- `CMenus::RenderLoadingDirect` 接收可选的直接进度值，同时保留旧 `RenderLoading` 计数接口供其他加载路径使用。

验证：`cmake --build cmake-build-release --target game-client -j 14` 通过；`cmake --build cmake-build-release --target run_cxx_tests -j 14` 通过（2919/2919）；`git diff --check` 与 `qmclient_scripts/fix_style.py -n` 通过。

剩余 gap：尚未执行包含多组背景/前景/实体层的真实地图加载视觉 smoke；本批不涉及 128 人主体重做。

### 2026-09-01：包络缩放热键实际代码合并

- 上游 `c2cea3f30` 已形成本地提交 `eea5c224e`，不是文档占位。
- `CEnvelopeEditor::Render` 只有在无 dialog、无 popup、无活动 `CLineInput` 且满足原有编辑条件时才响应 `S` 缩放；新增 `EnvelopeScaleHotkeyGuardsModalInput` 源码合同测试，防止模态输入再次误触发编辑操作。
- 验证：`cmake --build cmake-build-release --target game-client -j 14` 通过；`cmake --build cmake-build-release --target run_cxx_tests -j 14` 通过，`2920/2920`；提交前 `git diff --cached --check` 通过。
- 尚未执行编辑器 GUI smoke；该缺口不影响源码和全量 C++ 回归证据。

### 2026-09-01：本地地址判定实际代码合并

- 上游 `9931d2dc9` 已适配并形成本地提交 `561740714`；只包含本地地址识别、localhost fallback 和边界测试，不混入并行 iOS socket 改动。
- `net_addr_is_local` 现在按地址类型识别 IPv4/IPv6/WebSocket 的私有、ULA、link-local 和 loopback 地址；fallback 直接写入 loopback 地址，避免字符串解析断言路径。
- 验证：`cmake --build cmake-build-release --target game-client -j 14` 通过；`cmake --build cmake-build-release --target run_cxx_tests -j 14` 通过，`2920/2920`；提交前 `git diff --cached --check` 通过。
- quick gate 仍受并行改动 `src/test/net_test.cpp:45` 的既有 clang-format 漂移影响；本批未修改该文件。

### 2026-09-01：设置导出边界实际代码合并

- 上游 `a39ed1834` 已形成提交 `7aae013cb`，同步修改官方脚本和 QmClient 派生脚本，将 `SERVER_MAX_CLIENTS` 解析边界统一为 `128`。
- 验证：两份脚本 `py_compile` 通过；直接 `parse_settings` 边界测试均返回 `128`；提交前暂存差异检查通过。
- 完整 HTML 导出仍受既有 `qm_global_card_order` lambda 注册回调解析限制，未将该路径标记为通过。

### 2026-09-01：HTTP 请求生命周期实际合并

- 上游 `ceb86ada94ad07e47082646204ec31b88d634b88` 已形成独立本地提交 `e8c873823`，修复 HTTP 请求在析构阶段取消可能访问已关闭 HTTP 组件的问题。
- `CClient::Run()` 现在先调用 `m_ServerBrowser.Shutdown()`，再关闭 HTTP 和 JobPool；server-browser HTTP owner 的析构函数只断言请求已清空，显式 `Shutdown()` 负责 Abort 并释放指针。
- master chooser 在 HEAD/GET 请求启动前用同一把锁检查 `STATE_ABORTED`、登记请求并提交 HTTP，避免退出与第二个请求启动之间的竞态。
- 皮肤目录扫描、皮肤列表计划、官方皮肤索引和容器加载任务在 `CSkins::OnShutdown()` 中统一 Abort 并清空，容器析构不再依赖 HTTP/job 组件。
- 新增 `src/test/http_shutdown_lifecycle_test.cpp`，覆盖客户端关闭顺序、空指针保护、析构断言和请求清理合同。

验证：确认无后台构建进程后，`cmake --build cmake-build-release --target game-client -j 14` 通过；`cmake --build cmake-build-release --target run_cxx_tests -j 14` 通过（2924/2924）；`git diff --cached --check` 通过。`python3 qmclient_scripts/gate/check_gate.py --mode quick` 为 10 pass / 1 fail，唯一失败是并行改动 `src/test/net_test.cpp:45` 的既有 clang-format 漂移。

剩余 gap：当前新增测试为源码合同测试，尚无可控 fake HTTP/job 或真实退出 smoke 来覆盖请求并发时序；该缺口不阻塞本地适配提交，但不能将其表述为真实 HTTP 运行时验证。

### 2026-09-01：RCON 与 snapshot 安全修复实际合并

- 上游 `821d5ae4b` 已按 QmClient 现有单 victim API 适配，形成提交 `ece1c1b4d`。quoted/unquoted 参数现在走同一套校验，空引号 victim 返回 `PARSEARGS_MISSING_VALUE`，不会把无效字符串写入 RCON victim；未引入上游后续的多 victim API 重构。
- 上游 `91eb6b94a` 已形成提交 `6c659543b`。字符实体释放翻译 ID、weapon snapshot 和 hook snapshot 均在 optional ID 有值时才执行，避免 snapshot ID 池耗尽导致 `.value()` 崩溃；该修复不重新实现 128 人主体。

验证：确认无后台构建/测试进程后，串行执行 `cmake --build cmake-build-release --target game-client -j 14`（通过，仅现有 macOS SDK 部署版本链接警告）和 `cmake --build cmake-build-release --target run_cxx_tests -j 14`（通过，2924/2924）。`python3 qmclient_scripts/gate/check_gate.py --mode quick` 为 10 pass / 1 fail，唯一失败是并行改动 `src/test/net_test.cpp:45` 的既有 clang-format 漂移；本批文件未触及该行。

当前仍未完成：iOS P3C simulator/真机 smoke、0.6/0.7/128 混合会话和 timeout reconnect、网络预算高并发压测、Econ TCP 重连集成测试、HTTP fake job/退出并发测试、Linux Valgrind integration，以及 Emscripten/Vulkan/DSCP 策略批次。`fb5e04c01` 已覆盖 econ ban 与 skin metrics，不能重复列为待合并候选。

### 2026-09-01：地图转换与断言分支实际合并

- 上游 `bc2c2f930` 已形成提交 `889486023`。`map_convert_07` 现在由 `CImageInfo` 持有外置 PNG 数据直到 `CDataFileWriter::AddData` 完成，消除局部图像对象析构后的悬空指针；不涉及 128 人、协议或 Metal/iOS 路径。
- 上游 `5ca808242` 已形成提交 `4cfa6f029`。`dbg_assert` 和 `dbg_assert_failed` 的失败路径标记为 `[[unlikely]]`，仅提供 C++20 分支提示，不改变断言行为。

验证：无后台构建任务时串行执行 `cmake --build cmake-build-release --target map_convert_07 -j 14`（通过，只有现有 macOS SDK/重复库链接警告）；此前同一工作树的 `game-client` 与 C++ 全量测试已通过（2924/2924），覆盖 `dbg_assert` 宏和 snapshot/console 回归。外置 PNG 地图端到端样本、ASAN 和 Linux Valgrind 仍未执行。

### 2026-09-01：集成测试超时与 Valgrind 参数实际合并

- 上游 `dc7549396` 与 `25dc1ab14` 已形成测试基础设施提交 `a0b2c952b`。客户端和服务端的 `conn_timeout` 按本地 integration timeout multiplier 缩放并限制为 1000；Valgrind memcheck 不再默认启用昂贵的 `--track-origins=yes`。
- 本地 Valgrind multiplier 仍为 `20`，没有机械套用上游提交中的 `25`；该差异属于现有 QmClient 测试策略。

验证：`python3 -m py_compile scripts/integration_test.py` 与 `git diff --check -- scripts/integration_test.py` 通过。当前宿主未运行 Linux Valgrind integration，因此只确认脚本语法与静态差异，不能声称集成测试运行通过。

### 2026-09-01：数据库、编辑器与 tuning 修复实际合并

- 上游 `b68c684a2` 已形成提交 `53b8dade6`。server-browser ping cache 在重复加载前重置 SQLite prepared statement，避免停留在 `SQLITE_DONE` 后再次 step 失败。
- 上游 `e58155fb8` 已形成提交 `a86b5e2cd`。编辑器 brush picker 使用稳定的 `m_ShowPicker` 状态判断是否复制 tele/speedup/switch/tune 扩展数据，避免依赖瞬时空格键状态导致崩溃。
- 上游 `a38ba7bd4` 已形成提交 `58e74ecde`。crazy tuning 与 NaN 值超出 int 范围时统一映射为 `INT_MIN`；snapshot ID 池耗尽回归用例也在该批测试提交中收录。

验证：此前同一工作树的 `game-client` 和 C++ 全量测试已通过（2924/2924）；`map_convert_07` 目标构建通过；本轮各提交均通过 `git diff --cached --check`。quick gate 仍仅因并行改动 `src/test/net_test.cpp:45` 的 clang-format 漂移失败。SQLite reload 专项测试、editor GUI smoke、外置 PNG ASAN 与 Linux Valgrind 仍待补。

### 2026-09-01：台账提交证据收口

- 已补齐所有 `adapted-local`/`merged-local` 台账条目的本地提交字段；台账不再把上游 hash 误当作本地提交，也不再把未提交的并行工作树改动统计为已合并。
- `0b108953f`、`a23094c69` 和 `eb56d7e2b` 的当前行为由本地 `1eafa9ed3` 的平台/CMake 适配覆盖，标记为 `covered-equivalent`；没有重复 cherry-pick。
- `8c73e5d60` 的 0.7 server-info 行为由现有 128 人/网络批次 `070f529e2` 覆盖；`39c675977` 的 font typer 行为由 `c519155c9`、`152d2029e` 的本地编辑器适配覆盖。
- `c1a83f17e`、`2612f425f`、`c5ad692163`、`936f2b7c1`、`ceb86ada9`、`045cce756`、`03407db9e6` 和 `b3bc9f18c` 分别回填为 `43e40188d`、`c181dfbd1`、`311ab3555`、`1eafa9ed3/e5325e77b`、`e8c873823`、`fb5e04c01` 和 `d699c86a1`。
- 重新检查 `CChooseMaster` 的 HEAD/GET 启动路径：请求登记、Abort 和 `Http()->Run()` 均在同一把锁保护下，Shutdown 竞态已有状态检查与主动 Abort；当前没有足够证据再新增重复修复。剩余缺口是可控 fake HTTP/job 或真实退出 smoke，而不是继续改文档。

本收口只修改台账和计划文档；工作树中的 Metal、菜单、统计、翻译、Emscripten 等并行改动未被暂存。
