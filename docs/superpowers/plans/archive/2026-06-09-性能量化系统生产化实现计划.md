# 性能量化系统生产化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 将 `qmclient_scripts/perf` 和 C++ `perf/*` telemetry 收口成可信、可对比、可进入 debug bundle 的生产级性能诊断系统第一轮。

**Architecture:** 保留现有 key=value + JSON Lines 兼容解析器和自包含 HTML 报表，不重建系统。新增无依赖的 operation signature 核心层，以及 report quality / summary JSON 层，使报表明确样本可信度、缺失数据和对比可比性；C++ 侧只补合同、阈值门控 helper 和 server browser list frame 摘要 telemetry。

**Tech Stack:** TypeScript `tsx`/`node:test` style assertions, self-contained HTML report, C++ inline perf logging helpers, GoogleTest, CMake Windows gate.

---

## Scope

本计划实现 `docs/superpowers/specs/2026-06-08-页面性能优化框架设计.md` 的 Phase 1。它不清理 FBO、不重构 Settings / Assets / Tee 页面、不做 Demo Browser 异步化，也不引入全局 scheduler。完成后，后续性能优化项必须能用这套系统说明操作路径、可信度、长帧归因和回归防护。

## Source Context

**Authoritative docs:**

- `docs/superpowers/specs/2026-06-08-页面性能优化框架设计.md`
- `docs/ai-workflow/advanced/perf-system-workflow.md`
- `docs/ai-workflow/advanced/performance-workflow.md`
- `docs/ai-workflow/advanced/observability-debugging.md`
- `docs/ai-workflow/advanced/regression-prevention.md`

**Current implementation anchors:**

- `qmclient_scripts/perf/lib/parse.ts`: parses old key=value and JSON Lines.
- `qmclient_scripts/perf/lib/stats.ts`: percentiles, spike detection, attribution, comparison.
- `qmclient_scripts/perf/lib/report.ts`: self-contained HTML report.
- `qmclient_scripts/perf/analyze.ts`: CLI entry, latest log selection, report output.
- `qmclient_scripts/perf/test.ts`: TS regression coverage.
- `src/game/client/components/qmclient/perf_logging.h`: shared C++ logging helpers.
- `src/game/client/components/menus_demo.cpp`: existing threshold-gated demo browser `list_frame`.
- `src/game/client/components/menus_browser.cpp`: server browser list rendering target for summary telemetry.
- `src/test/qmclient_monitoring_test.cpp`: C++ telemetry contract tests.

## File Structure

- Create `qmclient_scripts/perf/lib/quality_core.ts`
  - Owns operation signature and comparison compatibility. It imports only `parse.ts` types, so `stats.ts` can depend on it without a runtime cycle.
- Create `qmclient_scripts/perf/lib/quality.ts`
  - Owns missing-data warnings, sampling warning, and summary JSON shape. It may import `stats.ts`; `stats.ts` must not import this file.
- Modify `qmclient_scripts/perf/lib/parse.ts`
  - Add parse diagnostics without changing existing `parseLog()` callers.
- Modify `qmclient_scripts/perf/lib/stats.ts`
  - Reuse `quality_core.ts` operation metadata in snapshots and comparisons.
- Modify `qmclient_scripts/perf/lib/report.ts`
  - Add report quality panel, N/A output, and comparable / advisory comparison badge.
- Modify `qmclient_scripts/perf/analyze.ts`
  - Write both a fixed `perf_summary.json` for debug bundle pickup and a per-log `${logName}_summary.json` for report archives.
- Modify `qmclient_scripts/perf/test.ts`
  - Add boundary, quality, operation, and summary tests.
- Modify `qmclient_scripts/perf/README.md`
  - Document production usage, summary JSON, and comparison limits.
- Modify `src/game/client/components/qmclient/perf_logging.h`
  - Add a cheap `QmPerfShouldLogDuration()` helper for hot-path threshold gates.
- Modify `src/game/client/components/menus_browser.cpp`
  - Add threshold-gated `event=list_frame page=server_browser ...` summary telemetry.
- Modify `src/test/qmclient_monitoring_test.cpp`
  - Add contract assertions for the helper and server browser telemetry.
- `docs/superpowers/explore/preview.html` is read-only context for this plan. Do not modify it in Phase 1 unless a separate docs task explicitly asks for preview synchronization.

---

### Task 1: Add Report Quality Model Without Cyclic Imports

**Files:**
- Create: `qmclient_scripts/perf/lib/quality_core.ts`
- Create: `qmclient_scripts/perf/lib/quality.ts`
- Modify: `qmclient_scripts/perf/test.ts`

- [x] **Step 1: Write failing TS tests for quality classification**

Add this import block to `qmclient_scripts/perf/test.ts`:

```ts
import {
  compareOperationSignatures,
  operationSignature,
} from './lib/quality_core.ts';
import {
  reportQuality,
  summarizeForBundle,
} from './lib/quality.ts';
```

Add this test function near the existing sampling and comparison tests:

```ts
function testReportQualityExplainsMissingAndBiasedData() {
  const entries = parseLog([
    '2026-06-04 12:00:00 I perf/menu: stage=settings_page_content duration_ms=10.000 frame=10 page=settings:tee',
    '2026-06-04 12:00:01 I perf/menu: stage=settings_page_content duration_ms=11.000 frame=11 page=settings:tee',
    '2026-06-04 12:00:02 I perf/menu: stage=settings_page_content duration_ms=12.000 frame=12 page=settings:tee',
    '2026-06-04 12:00:03 I perf/menu: stage=settings_page_content duration_ms=13.000 frame=13 page=settings:tee',
    '2026-06-04 12:00:04 I perf/menu: stage=settings_page_content duration_ms=14.000 frame=14 page=settings:tee',
    '2026-06-04 12:00:05 I perf/menu: stage=settings_page_content duration_ms=15.000 frame=15 page=settings:tee',
    '2026-06-04 12:00:06 I perf/menu: stage=settings_page_content duration_ms=16.000 frame=16 page=settings:tee',
    '2026-06-04 12:00:07 I perf/menu: stage=settings_page_content duration_ms=17.000 frame=17 page=settings:tee',
    '2026-06-04 12:00:08 I perf/menu: stage=settings_page_content duration_ms=18.000 frame=18 page=settings:tee',
    '2026-06-04 12:00:09 I perf/menu: stage=settings_page_content duration_ms=19.000 frame=19 page=settings:tee',
  ].join('\n'));

  const quality = reportQuality(entries, { invalidLines: 2, totalLines: 12 });

  assert.equal(quality.sampleCount, 10);
  assert.equal(quality.invalidLines, 2);
  assert.equal(quality.biased, true);
  assert.match(quality.warnings.join('\n'), /sampling threshold/i);
  assert.match(quality.warnings.join('\n'), /invalid lines/i);
  assert.deepEqual(quality.operation.pages, ['settings:tee']);
}
```

Add this test function after it:

```ts
function testOperationCompatibilityIsExplicit() {
  const settingsEntries = parseLog([
    '2026-06-04 12:00:00 I perf/menu: stage=settings_page_content duration_ms=6.000 frame=10 page=settings:tee',
  ].join('\n'));
  const demoEntries = parseLog([
    '2026-06-04 12:00:00 I perf/menu: stage=demo_browser duration_ms=9.000 frame=10 page=demo_browser',
  ].join('\n'));

  const result = compareOperationSignatures(operationSignature(settingsEntries), operationSignature(demoEntries));

  assert.equal(result.comparable, false);
  assert.match(result.reason, /page set differs/i);
}
```

Add this test function after it:

```ts
function testBundleSummaryIsStableJsonShape() {
  const entries = parseLog([
    '2026-06-04 12:00:00 I perf/menu: stage=settings_page_content duration_ms=6.000 frame=10 page=settings:tee',
    '2026-06-04 12:00:01 I perf/section: event=section page=settings:tee section=identity dur_ms=4.000 visible=1 dirty=unknown text_new=0 text_reused=0 frame=11',
  ].join('\n'));

  const summary = summarizeForBundle(entries, 'qm_perf_current.log', { invalidLines: 0, totalLines: 2 });

  assert.equal(summary.sourceFile, 'qm_perf_current.log');
  assert.equal(summary.quality.sampleCount, 1);
  assert.deepEqual(summary.quality.operation.pages, ['settings:tee']);
  assert.equal(summary.attribution.top.length, 1);
  assert.equal(summary.attribution.top[0].kind, 'UI Rebuild');
  assert.equal(typeof summary.generatedAt, 'string');
}
```

Register the three tests at the bottom:

```ts
testReportQualityExplainsMissingAndBiasedData();
testOperationCompatibilityIsExplicit();
testBundleSummaryIsStableJsonShape();
```

- [x] **Step 2: Run TS tests and confirm failure**

Run:

```powershell
Push-Location qmclient_scripts/perf
npm test
Pop-Location
```

Expected: FAIL with module-not-found or missing export errors for `./lib/quality_core.ts` or `./lib/quality.ts`.

- [x] **Step 3: Implement `quality_core.ts`**

Create `qmclient_scripts/perf/lib/quality_core.ts` with this structure:

```ts
import type { PerfEntry } from './parse.ts';

export interface OperationSignature {
  pages: string[];
  stages: string[];
  systems: string[];
  events: string[];
}

export interface OperationComparison {
  comparable: boolean;
  reason: string;
}

function sortedUnique(values: string[]): string[] {
  return [...new Set(values.filter(v => v.length > 0))].sort();
}

function eventName(e: PerfEntry): string {
  const event = e.fields.event;
  return event === undefined || event === null ? '' : String(event);
}

export function operationSignature(entries: PerfEntry[]): OperationSignature {
  return {
    pages: sortedUnique(entries.map(e => String(e.fields.page ?? e.fields.page_name ?? 'unknown'))),
    stages: sortedUnique(entries.map(e => e.stage)),
    systems: sortedUnique(entries.map(e => e.system)),
    events: sortedUnique(entries.map(eventName)),
  };
}

function sameSet(a: string[], b: string[]): boolean {
  return a.length === b.length && a.every((value, index) => value === b[index]);
}

export function compareOperationSignatures(previous: OperationSignature, current: OperationSignature): OperationComparison {
  if (!sameSet(previous.pages, current.pages)) {
    return { comparable: false, reason: 'page set differs; comparison is advisory only' };
  }
  if (!sameSet(previous.systems, current.systems)) {
    return { comparable: false, reason: 'telemetry system set differs; comparison is advisory only' };
  }
  return { comparable: true, reason: 'same page and telemetry system sets' };
}
```

- [x] **Step 4: Implement `quality.ts`**

Create `qmclient_scripts/perf/lib/quality.ts` with this structure:

```ts
import { basename } from 'node:path';

import type { PerfEntry } from './parse.ts';
import { operationSignature, type OperationSignature } from './quality_core.ts';
import {
  BUDGET,
  calcPercentiles,
  detectSpikes,
  inferSamplingThreshold,
  isSamplingBiased,
  pagePerformanceAttribution,
  selectFrameTimeEntries,
  type AttributionEntry,
  type Percentiles,
  type Verdict,
  computeVerdict,
  entryDurationMs,
} from './stats.ts';

export interface ParseDiagnostics {
  totalLines: number;
  invalidLines: number;
}

export interface ReportQuality {
  sampleCount: number;
  totalEntries: number;
  totalLines: number;
  invalidLines: number;
  samplingThresholdMs: number;
  biased: boolean;
  operation: OperationSignature;
  warnings: string[];
}

export interface PerfBundleSummary {
  generatedAt: string;
  sourceFile: string;
  percentiles: Percentiles;
  verdict: Verdict;
  spikeCount: number;
  quality: ReportQuality;
  attribution: {
    top: AttributionEntry[];
  };
}

export function reportQuality(entries: PerfEntry[], diagnostics: ParseDiagnostics): ReportQuality {
  const frameEntries = selectFrameTimeEntries(entries);
  const durations = frameEntries.map(e => entryDurationMs(e) ?? e.durationMs);
  const samplingThresholdMs = inferSamplingThreshold(durations);
  const biased = isSamplingBiased(durations, BUDGET.samplingDefault);
  const warnings: string[] = [];

  if (frameEntries.length === 0) {
    warnings.push('no frame-time samples; percentile and verdict data are unavailable');
  }
  if (diagnostics.invalidLines > 0) {
    warnings.push(`${diagnostics.invalidLines} invalid lines were ignored during parsing`);
  }
  if (biased) {
    warnings.push(`sampling threshold appears above default 4ms; p5=${samplingThresholdMs.toFixed(1)}ms`);
  }

  return {
    sampleCount: frameEntries.length,
    totalEntries: entries.length,
    totalLines: diagnostics.totalLines,
    invalidLines: diagnostics.invalidLines,
    samplingThresholdMs,
    biased,
    operation: operationSignature(entries),
    warnings,
  };
}

export function summarizeForBundle(entries: PerfEntry[], sourceFile: string, diagnostics: ParseDiagnostics): PerfBundleSummary {
  const frameEntries = selectFrameTimeEntries(entries);
  const durations = frameEntries.map(e => entryDurationMs(e) ?? e.durationMs);
  const percentiles = calcPercentiles(durations);
  const spikes = detectSpikes(frameEntries, BUDGET.h60);
  return {
    generatedAt: new Date().toISOString(),
    sourceFile: basename(sourceFile),
    percentiles,
    verdict: computeVerdict(percentiles, spikes.length),
    spikeCount: spikes.length,
    quality: reportQuality(entries, diagnostics),
    attribution: {
      top: pagePerformanceAttribution(entries).slice(0, 10),
    },
  };
}
```

- [x] **Step 5: Run TS tests and confirm pass**

Run:

```powershell
Push-Location qmclient_scripts/perf
npm test
Pop-Location
```

Expected: PASS with `qmclient perf tests passed`.

---

### Task 2: Preserve Parse Diagnostics Without Breaking Existing Callers

**Files:**
- Modify: `qmclient_scripts/perf/lib/parse.ts`
- Modify: `qmclient_scripts/perf/test.ts`

- [x] **Step 1: Write failing parser diagnostics test**

Update the parse import in `qmclient_scripts/perf/test.ts`:

```ts
import { parseLine, parseLog, parseLogWithDiagnostics } from './lib/parse.ts';
```

Add this test near existing parser tests:

```ts
function testParseLogWithDiagnosticsCountsInvalidLines() {
  const result = parseLogWithDiagnostics([
    '2026-06-04 12:00:00 I perf/menu: stage=settings_page_content duration_ms=6.000 frame=10 page=settings:tee',
    'not a perf line',
    '',
    '{"timestamp":"2026-06-04T12:00:02","system":"perf/device","event":"sample","frame":77}',
    '{broken json',
  ].join('\n'));

  assert.equal(result.entries.length, 2);
  assert.equal(result.diagnostics.totalLines, 4);
  assert.equal(result.diagnostics.invalidLines, 2);
}
```

Register it:

```ts
testParseLogWithDiagnosticsCountsInvalidLines();
```

- [x] **Step 2: Run TS tests and confirm failure**

Run:

```powershell
Push-Location qmclient_scripts/perf
npm test
Pop-Location
```

Expected: FAIL because `parseLogWithDiagnostics` is not exported.

- [x] **Step 3: Implement diagnostic parse API**

Add this interface and function to `qmclient_scripts/perf/lib/parse.ts`:

```ts
export interface ParseDiagnostics {
  totalLines: number;
  invalidLines: number;
}

export interface ParseResult {
  entries: PerfEntry[];
  diagnostics: ParseDiagnostics;
}

export function parseLogWithDiagnostics(content: string): ParseResult {
  const entries: PerfEntry[] = [];
  let totalLines = 0;
  let invalidLines = 0;

  for (const rawLine of content.split('\n')) {
    const line = rawLine.trim();
    if (line.length === 0) {
      continue;
    }
    totalLines++;
    const entry = parseLine(line);
    if (entry === null) {
      invalidLines++;
      continue;
    }
    entries.push(entry);
  }

  return {
    entries,
    diagnostics: { totalLines, invalidLines },
  };
}
```

Rewrite existing `parseLog()` to call the diagnostic API:

```ts
export function parseLog(content: string): PerfEntry[] {
  return parseLogWithDiagnostics(content).entries;
}
```

- [x] **Step 4: Run TS tests and typecheck**

Run:

```powershell
Push-Location qmclient_scripts/perf
npm test
npx tsc --noEmit
Pop-Location
```

Expected: both commands PASS.

---

### Task 3: Make Report Quality Visible and N/A Explicit

**Files:**
- Modify: `qmclient_scripts/perf/lib/report.ts`
- Modify: `qmclient_scripts/perf/test.ts`

- [x] **Step 1: Write failing report tests**

Add this test after `testReportIncludesSectionTop10()`:

```ts
function testReportShowsQualityAndUnavailableData() {
  const entries = parseLog([
    '2026-06-04 12:00:00 I perf/interaction: event=tee_enter frame=10 page=settings:tee visible_rows=8',
  ].join('\n'));

  const html = generateReport(entries, 'qm_perf_empty_frame_data.log', null, {
    totalLines: 2,
    invalidLines: 1,
  });

  assert.match(html, /样本可信度/);
  assert.match(html, /N\/A/);
  assert.match(html, /no frame-time samples/i);
  assert.match(html, /invalid lines/i);
}
```

Register it:

```ts
testReportShowsQualityAndUnavailableData();
```

- [x] **Step 2: Run TS tests and confirm failure**

Run:

```powershell
Push-Location qmclient_scripts/perf
npm test
Pop-Location
```

Expected: FAIL because `generateReport` does not accept diagnostics or render quality.

- [x] **Step 3: Extend `generateReport` signature**

Change the signature in `qmclient_scripts/perf/lib/report.ts`:

```ts
export function generateReport(
  entries: PerfEntry[],
  sourceFile: string,
  comparison?: ComparisonResult | null,
  diagnostics = { totalLines: entries.length, invalidLines: 0 },
): string {
```

Add imports:

```ts
import { reportQuality } from './quality.ts';
```

After `const narrative = ...`, add:

```ts
  const quality = reportQuality(entries, diagnostics);
  const metricValue = (value: number, digits = 1) => p.count === 0 ? 'N/A' : value.toFixed(digits);
```

- [x] **Step 4: Render quality section and N/A values**

In the title meta grid, keep total entries and menu frames, then add:

```html
    <span class="label">Quality</span><span class="value">${quality.warnings.length === 0 ? 'OK' : `${quality.warnings.length} warning(s)`}</span>
```

Add this section immediately after the abstract:

```ts
<section class="section">
  <div class="section-head">
    <span class="section-num">quality</span>
    <h2>样本可信度</h2>
  </div>
  <table class="data-table">
    <tbody>
      <tr><td class="mono">Frame samples</td><td>${quality.sampleCount}</td></tr>
      <tr><td class="mono">Parsed entries</td><td>${quality.totalEntries}</td></tr>
      <tr><td class="mono">Invalid lines</td><td>${quality.invalidLines}</td></tr>
      <tr><td class="mono">Sampling p5 estimate</td><td>${quality.samplingThresholdMs.toFixed(1)}ms</td></tr>
      <tr><td class="mono">Pages</td><td>${escapeHtml(quality.operation.pages.join(', ') || 'N/A')}</td></tr>
      <tr><td class="mono">Systems</td><td>${escapeHtml(quality.operation.systems.join(', ') || 'N/A')}</td></tr>
    </tbody>
  </table>
  ${quality.warnings.length === 0 ? '<p class="body-text">当前样本未发现明显采样或解析风险。</p>' : `<p class="body-text">${escapeHtml(quality.warnings.join('；'))}</p>`}
</section>
```

Change KPI values from `p.p50.toFixed(1)` style to `metricValue(p.p50)`. Keep CSS classes using existing thresholds, but use a neutral class when `p.count === 0`:

```ts
const metricClass = (value: number, ok: number, warn: number) => p.count === 0 ? 'warn' : kpiClass(value, ok, warn);
```

- [x] **Step 5: Run TS tests and typecheck**

Run:

```powershell
Push-Location qmclient_scripts/perf
npm test
npx tsc --noEmit
Pop-Location
```

Expected: PASS.

---

### Task 4: Make Comparison Advisory Unless Operation Signatures Match

**Files:**
- Modify: `qmclient_scripts/perf/lib/stats.ts`
- Modify: `qmclient_scripts/perf/lib/report.ts`
- Modify: `qmclient_scripts/perf/test.ts`

- [x] **Step 1: Write failing comparison compatibility test**

Add this test near `testComparisonReportWarnsAboutAutomaticBaseline()`:

```ts
function testComparisonReportMarksDifferentOperationsAsAdvisory() {
  const previousEntries = parseLog([
    '2026-06-04 12:00:00 I perf/menu: stage=demo_browser duration_ms=8.000 frame=10 page=demo_browser',
  ].join('\n'));
  const currentEntries = parseLog([
    '2026-06-04 12:00:00 I perf/menu: stage=settings_page_content duration_ms=6.000 frame=10 page=settings:tee',
  ].join('\n'));
  const comparison = compareSessions(
    snapshot(previousEntries, 'qm_perf_previous.log'),
    snapshot(currentEntries, 'qm_perf_current.log'),
  );

  const html = generateReport(currentEntries, 'qm_perf_current.log', comparison);

  assert.equal(comparison.operation.comparable, false);
  assert.match(html, /advisory only/i);
  assert.match(html, /page set differs/i);
}
```

Register it:

```ts
testComparisonReportMarksDifferentOperationsAsAdvisory();
```

- [x] **Step 2: Run TS tests and confirm failure**

Run:

```powershell
Push-Location qmclient_scripts/perf
npm test
Pop-Location
```

Expected: FAIL because `comparison.operation` does not exist.

- [x] **Step 3: Extend snapshots and comparison result**

In `qmclient_scripts/perf/lib/stats.ts`, import operation helpers from the no-cycle core module:

```ts
import { compareOperationSignatures, operationSignature, type OperationComparison, type OperationSignature } from './quality_core.ts';
```

Add to `SessionSnapshot`:

```ts
  /** 操作路径签名，用于判断两个日志是否可严格对比 */
  operation: OperationSignature;
```

Add to `snapshot()` return object:

```ts
    operation: operationSignature(entries),
```

Add to `ComparisonResult`:

```ts
  /** 对比是否来自同一类操作路径 */
  operation: OperationComparison;
```

In `compareSessions()`, compute and return it:

```ts
  const operation = compareOperationSignatures(previous.operation, current.operation);
```

Return:

```ts
  return { previous, current, metrics, compliance, verdictChanged, operation, narrative };
```

- [x] **Step 4: Render comparison compatibility**

In `qmclient_scripts/perf/lib/report.ts`, inside the comparison section, add:

```html
  <p class="body-text">
    对比可信度：${comparison.operation.comparable ? 'same operation signature' : `advisory only - ${escapeHtml(comparison.operation.reason)}`}
  </p>
```

Keep the existing warning that auto-selected previous logs are not strict regression baselines.

- [x] **Step 5: Run TS tests and typecheck**

Run:

```powershell
Push-Location qmclient_scripts/perf
npm test
npx tsc --noEmit
Pop-Location
```

Expected: PASS.

---

### Task 5: Emit Summary JSON for Debug Bundle Integration

**Files:**
- Modify: `qmclient_scripts/perf/analyze.ts`
- Modify: `qmclient_scripts/perf/test.ts`
- Modify: `qmclient_scripts/perf/README.md`

- [x] **Step 1: Add a summary serialization shape test**

Add this test in `qmclient_scripts/perf/test.ts`:

```ts
function testSummaryJsonCanBeSerializedForDebugBundle() {
  const entries = parseLog([
    '2026-06-04 12:00:00 I perf/menu: stage=settings_page_content duration_ms=6.000 frame=10 page=settings:tee',
  ].join('\n'));

  const summary = summarizeForBundle(entries, 'C:/tmp/qm_perf_current.log', { invalidLines: 0, totalLines: 1 });
  const parsed = JSON.parse(JSON.stringify(summary));

  assert.equal(parsed.sourceFile, 'qm_perf_current.log');
  assert.equal(parsed.quality.operation.pages[0], 'settings:tee');
  assert.equal(parsed.verdict, 'PASS');
}
```

Register it:

```ts
testSummaryJsonCanBeSerializedForDebugBundle();
```

- [x] **Step 2: Run TS tests**

Run:

```powershell
Push-Location qmclient_scripts/perf
npm test
Pop-Location
```

Expected: PASS after Task 1; this test locks summary JSON stability before CLI changes. This is a regression-shape test, not the red step for CLI output.

- [x] **Step 3: Write failing CLI source contract for both summary files**

Add this source-text test in `qmclient_scripts/perf/test.ts`:

```ts
function testAnalyzeWritesBundleAndArchiveSummaryFiles() {
  const source = readFileSync(new URL('./analyze.ts', import.meta.url), 'utf-8');

  assert.match(source, /parseLogWithDiagnostics/);
  assert.match(source, /perf_summary\\.json/);
  assert.match(source, /\$\{logName\}_summary\.json/);
  assert.match(source, /summarizeForBundle/);
}
```

Register it:

```ts
testAnalyzeWritesBundleAndArchiveSummaryFiles();
```

Run:

```powershell
Push-Location qmclient_scripts/perf
npm test
Pop-Location
```

Expected: FAIL because `analyze.ts` does not write either summary file yet.

- [x] **Step 4: Update CLI to use diagnostics and write summary**

In `qmclient_scripts/perf/analyze.ts`, change imports inside `main()`:

```ts
  const { parseLogWithDiagnostics } = await import('./lib/parse.ts');
  const { generateReport } = await import('./lib/report.ts');
  const { snapshot, compareSessions } = await import('./lib/stats.ts');
  const { summarizeForBundle } = await import('./lib/quality.ts');
```

Replace:

```ts
  const entries = parseLog(content);
  console.log(`有效条目: ${entries.length}`);
```

with:

```ts
  const parsed = parseLogWithDiagnostics(content);
  const entries = parsed.entries;
  console.log(`有效条目: ${entries.length}`);
  if (parsed.diagnostics.invalidLines > 0) {
    console.log(`忽略无效行: ${parsed.diagnostics.invalidLines}`);
  }
```

For previous logs, use:

```ts
      const prevParsed = parseLogWithDiagnostics(prevContent);
      const prevEntries = prevParsed.entries;
```

Change report generation:

```ts
  const reportHtml = generateReport(entries, logPath, comparison, parsed.diagnostics);
  const summaryJson = summarizeForBundle(entries, logPath, parsed.diagnostics);
```

After writing HTML, add both summary files:

```ts
  const summaryText = JSON.stringify(summaryJson, null, 2);
  const archiveSummaryPath = join(reportDir, `${logName}_summary.json`);
  const bundleSummaryPath = join(reportDir, 'perf_summary.json');
  writeFileSync(archiveSummaryPath, summaryText, 'utf-8');
  writeFileSync(bundleSummaryPath, summaryText, 'utf-8');
  console.log(`摘要已生成: ${archiveSummaryPath}`);
  console.log(`Debug bundle 摘要已生成: ${bundleSummaryPath}`);
```

- [x] **Step 5: Update README with production usage**

In `qmclient_scripts/perf/README.md`, add a section:

```md
## 生产级使用约定

- 默认采集阈值使用 `qm_perf_debug_threshold_ms 4`；4.17ms 只表示 240Hz 帧预算线。
- HTML 报表用于人工分析，`*_summary.json` 用于按日志归档，固定名 `perf_summary.json` 用于 debug bundle 自动拾取。
- 自动选择的上一份日志只作为趋势提示；只有 page/system operation signature 一致时，才可以作为严格对比依据。
- 空日志、缺字段、畸形行和采样偏差必须看作质量警告，不能解读为性能优秀。
```

- [x] **Step 6: Run TS tests and a sample CLI**

Run:

```powershell
Push-Location qmclient_scripts/perf
npm test
npx tsc --noEmit
npx tsx analyze.ts test/sample.log
Pop-Location
```

Expected:

```text
qmclient perf tests passed
报表已生成: ...
摘要已生成: ...
```

Check that `Perf_Report/sample_report.html`, `Perf_Report/sample_summary.json`, and `Perf_Report/perf_summary.json` are generated under the perf log directory used by the script. If `test/sample.log` is not a valid path in the current directory, from repo root run `npx tsx qmclient_scripts/perf/analyze.ts qmclient_scripts/perf/test/sample.log`.

---

### Task 6: Add C++ Threshold Helper and Contract Tests

**Files:**
- Modify: `src/game/client/components/qmclient/perf_logging.h`
- Modify: `src/test/qmclient_monitoring_test.cpp`

- [x] **Step 1: Write failing C++ helper test**

Add this test near `PerfConfigDefaultsUseLowThresholdWithoutJsonToggle` in `src/test/qmclient_monitoring_test.cpp`:

```cpp
TEST(QmMonitoringHelpers, PerfDurationGateUsesConfiguredThreshold)
{
	const int OldThreshold = g_Config.m_QmPerfDebugThresholdMs;
	g_Config.m_QmPerfDebugThresholdMs = 4;

	EXPECT_FALSE(QmPerfShouldLogDuration(3.999));
	EXPECT_TRUE(QmPerfShouldLogDuration(4.0));
	EXPECT_TRUE(QmPerfShouldLogDuration(0.0, true));

	g_Config.m_QmPerfDebugThresholdMs = OldThreshold;
}
```

Add this source contract inside `MenuPerfEventsExposePageAttributionFields()` or a new test:

```cpp
EXPECT_NE(Source.find("if(!QmPerfShouldLogDuration(DurationMs, Force))"), std::string::npos);
```

- [x] **Step 2: Run C++ tests and confirm failure**

Run:

```powershell
qmclient_scripts/cmake-windows.cmd --build cmake-build-release --target run_cxx_tests -j 14
```

Expected: FAIL because `QmPerfShouldLogDuration` does not exist.

- [x] **Step 3: Add helper**

Add this function to `src/game/client/components/qmclient/perf_logging.h` after `QmPerfThresholdMs()`:

```cpp
inline bool QmPerfShouldLogDuration(double DurationMs, bool Force = false)
{
	return Force || DurationMs >= QmPerfThresholdMs();
}
```

Update `QmPerfLogStage()`:

```cpp
	if(!QmPerfShouldLogDuration(DurationMs, Force))
		return;
```

- [x] **Step 4: Run C++ tests**

Run:

```powershell
qmclient_scripts/cmake-windows.cmd --build cmake-build-release --target run_cxx_tests -j 14
```

Expected: PASS for `QmMonitoringHelpers.PerfDurationGateUsesConfiguredThreshold`; existing tests should continue to pass. The `ListFrameDurationMs` source contract is intentionally left to Task 7, where that callsite is introduced.

---

### Task 7: Add Server Browser `list_frame` Summary Telemetry

**Files:**
- Modify: `src/game/client/components/menus_browser.cpp`
- Modify: `src/test/qmclient_monitoring_test.cpp`
- Modify: `qmclient_scripts/perf/test.ts`

- [x] **Step 1: Write failing TS attribution test for server browser**

Extend `testReportAttributesPagePerformanceEvents()` or add a new test:

```ts
function testServerBrowserListFrameAttributionUsesRowCounts() {
  const entries = parseLog([
    '2026-06-04 12:00:00 I perf/interaction: event=list_frame page=server_browser items_total=1200 rows_visible=18 rows_rendered=18 rows_iterated=1200 rows_skipped=1182 dur_ms=5.250 frame=11 source=server_browser',
  ].join('\n'));

  const attribution = pagePerformanceAttribution(entries);

  assert.equal(attribution.length, 1);
  assert.equal(attribution[0].kind, 'List Interaction');
  assert.match(attribution[0].summary, /rows_visible=18/);
  assert.match(attribution[0].details, /rows_rendered=18/);
  assert.match(attribution[0].details, /rows_iterated=1200/);
  assert.match(attribution[0].details, /rows_skipped=1182/);
}
```

Register it:

```ts
testServerBrowserListFrameAttributionUsesRowCounts();
```

- [x] **Step 2: Write failing C++ source contract**

In `src/test/qmclient_monitoring_test.cpp`, add a server browser block:

```cpp
{
	std::ifstream File(TestSourcePath("src/game/client/components/menus_browser.cpp"));
	ASSERT_TRUE(File.good());
	std::stringstream Buffer;
	Buffer << File.rdbuf();
	const std::string Source = Buffer.str();

	EXPECT_NE(Source.find("event=list_frame page=server_browser items_total=%d rows_visible=%d rows_rendered=%d rows_iterated=%d rows_skipped=%d dur_ms=%.3f source=server_browser"), std::string::npos);
	EXPECT_NE(Source.find("QmPerfShouldLogDuration(ListFrameDurationMs, false)"), std::string::npos);
}
```

- [x] **Step 3: Run tests and confirm C++ failure**

Run:

```powershell
Push-Location qmclient_scripts/perf
npm test
Pop-Location
qmclient_scripts/cmake-windows.cmd --build cmake-build-release --target run_cxx_tests -j 14
```

Expected: TS test should pass if attribution already handles `list_frame`; C++ test should fail because server browser telemetry is not present.

- [x] **Step 4: Add threshold-gated server browser list summary**

In `src/game/client/components/menus_browser.cpp`, in `CMenus::RenderServerbrowserServerList`, add a `CPerfTimer ListFrameTimer;` before the listbox row loop starts. Track:

```cpp
int RowsVisible = 0;
int RowsRendered = 0;
int RowsIterated = 0;
```

Increment `RowsIterated` for each sorted server considered by the list rendering loop. Increment `RowsVisible` / `RowsRendered` only for rows that actually enter visible rendering. After the listbox is complete, add:

```cpp
const double ListFrameDurationMs = ListFrameTimer.ElapsedMs();
if(QmPerfEnabled() && QmPerfShouldLogDuration(ListFrameDurationMs, false))
{
	char aPayload[256];
	const int ItemsTotal = ServerBrowser()->NumSortedServers();
	const int RowsSkipped = maximum(0, ItemsTotal - RowsRendered);
	str_format(aPayload, sizeof(aPayload),
		"event=list_frame page=server_browser items_total=%d rows_visible=%d rows_rendered=%d rows_iterated=%d rows_skipped=%d dur_ms=%.3f source=server_browser",
		ItemsTotal, RowsVisible, RowsRendered, RowsIterated, RowsSkipped, ListFrameDurationMs);
	QmPerfLogPayload("perf/interaction", aPayload, Client(), "server_browser");
}
```

Use the same visible-row semantics as `CListBox` exposes in the surrounding code. `rows_rendered` represents visible drawing cost; `rows_iterated` represents synchronous loop work; `rows_skipped` is `items_total - rows_rendered`.

- [x] **Step 5: Run C++ tests**

Run:

```powershell
qmclient_scripts/cmake-windows.cmd --build cmake-build-release --target run_cxx_tests -j 14
```

Expected: PASS.

---

### Task 8: Add Fixed Scenario Documentation for Perf Runs

**Files:**
- Create: `docs/superpowers/explore/2026-06-09-性能量化固定场景.md`
- Modify: `qmclient_scripts/perf/README.md`

- [x] **Step 1: Create fixed scenario document**

Create `docs/superpowers/explore/2026-06-09-性能量化固定场景.md`:

Use this content:

> # 性能量化固定场景
>
> 日期：2026-06-09
> 状态：active
>
> ## 目的
>
> 固定场景用于让性能优化、代码重构和回归检查使用同一套操作路径。不同操作路径的日志只能做趋势参考，不能作为严格回归结论。
>
> ## 通用采集配置
>
> qm_perf_debug 1
> qm_perf_logfile 1
> qm_perf_debug_threshold_ms 4
>
> ## 场景
>
> | ID | 操作路径 | 主要归因 | 失败信号 |
> | --- | --- | --- | --- |
> | PERF-SETTINGS-TEE-SWITCH | 打开 Settings，切到 Tee 页，等待首屏稳定 | page_switch、section、work_drain | p99 > 16ms 或连续长帧 |
> | PERF-DEMO-FIRST-ENTER | 首次进入 Demo Browser，打开含大量 demo 的目录 | page_switch、list_frame | 单帧 > 33ms 或 list_frame 无 rows_skipped |
> | PERF-SERVER-SCROLL | 进入 Server Browser，滚动一屏 | list_frame | rows_iterated 明显大于 rows_rendered，或 rows_rendered 明显大于 rows_visible |
> | PERF-ASSETS-DRAIN | 进入 Assets / Workshop，等待缩略图和资源完成 | work_drain、device | 交互帧无 stop reason 的集中 publish |
> | PERF-TEE-SCROLL | Tee 页快速滚动一屏 | list_frame、work_drain | 已显示 preview 回退到 loading 或 drain 无预算原因 |
>
> ## 报告要求
>
> - 保存 HTML report、`*_summary.json` 和固定名 `perf_summary.json`。
> - 写明客户端版本、renderer、窗口模式、刷新率、UI scale。
> - 对比官方 DDNet 或上一轮 QmClient 时，必须说明 operation signature 是否一致。

- [x] **Step 2: Link it from README**

Add to `qmclient_scripts/perf/README.md`:

```md
## 固定场景

固定场景见 `docs/superpowers/explore/2026-06-09-性能量化固定场景.md`。性能优化 PR 中的前后对比应优先选择这些场景，避免把不同操作路径的日志当成严格回归数据。
```

- [x] **Step 3: Run docs check**

Run:

```powershell
python qmclient_scripts/gate/check_docs.py
```

Expected: PASS.

---

### Task 9: Final Verification and Review Checkpoint

**Files:**
- No new source files beyond earlier tasks.

- [x] **Step 1: Run TS verification**

Run:

```powershell
Push-Location qmclient_scripts/perf
npm test
npx tsc --noEmit
Pop-Location
```

Expected:

```text
qmclient perf tests passed
```

`npx tsc --noEmit` exits with code 0.

- [x] **Step 2: Run C++ verification**

Run:

```powershell
qmclient_scripts/cmake-windows.cmd --build cmake-build-release --target run_cxx_tests -j 14
```

Expected: target `run_cxx_tests` exits with code 0.

- [x] **Step 3: Run docs verification**

Run:

```powershell
python qmclient_scripts/gate/check_docs.py --sync-only --prefer agents
python qmclient_scripts/gate/check_docs.py
```

Expected: both commands pass and AGENTS / CLAUDE mirror remains consistent.

- [x] **Step 4: Run quick gate**

Run:

```powershell
python qmclient_scripts/gate/check_gate.py --mode quick
```

Expected: quick gate exits with code 0. If it fails because unrelated dirty worktree changes are present, capture the exact failing check and keep this task open until the owner decides whether to isolate or include those changes.

- [x] **Step 5: Request code review**

Because this plan changes C++ telemetry and report semantics, dispatch one read-only code-review subagent after implementation is complete. Review focus:

```text
Review the performance quantification productionization changes. Check TS report correctness, parse boundary behavior, C++ telemetry overhead, list_frame counting semantics, and whether generated summaries can mislead users. Do not ask questions; produce findings first with file/line references.
```

Do not mark the review complete until the subagent returns. If findings are valid, fix them and rerun the relevant verification.

## Completion Criteria

The implementation is complete only when all are true:

1. HTML report shows sample quality, invalid-line count, sampling bias, operation signature, and N/A for unavailable frame metrics.
2. `*_summary.json` and fixed `perf_summary.json` are generated and contain stable fields for debug bundle consumption.
3. Session comparison states whether operation signatures match; mismatched comparisons are marked advisory.
4. Demo Browser and Server Browser list_frame telemetry are threshold-gated. Demo Browser keeps total / visible / processed / skipped fields; Server Browser exposes total / visible / rendered / iterated / skipped fields so visible drawing and synchronous loop work are not conflated.
5. TS tests, TS typecheck, C++ tests, docs check, and quick gate have run with results recorded.
6. Read-only code review has returned and all accepted findings are fixed.

## 执行记录

日期：2026-06-09

Command: `python qmclient_scripts/gate/check_docs.py --sync-only --prefer agents`
Result: pass. AGENTS / CLAUDE 镜像一致，未发现断链。
Scope: 验证计划 / 规格文档修复后治理文档同步状态。
Gaps: 不验证代码行为。

Command: `python qmclient_scripts/gate/check_docs.py`
Result: pass. AGENTS / CLAUDE 镜像一致，未发现断链。
Scope: 验证 docs/superpowers 计划、规格、固定场景文档和 README 链接入口不会破坏治理文档检查。
Gaps: 不验证 C++ / TS 行为。

Command: `Push-Location qmclient_scripts/perf; npm test; npx tsc --noEmit; Pop-Location`
Result: pass. `npm test` 输出 `qmclient perf tests passed`，`npx tsc --noEmit` 退出码 0。
Scope: 验证 parser diagnostics、report quality、operation signature、summary JSON、Server Browser row attribution 和 report N/A 行为。
Gaps: 不验证 C++ 编译和真实客户端采集。

Command: `Push-Location qmclient_scripts/perf; npx tsx analyze.ts test/sample.log; Pop-Location`
Result: pass. 生成 `sample_report.html`、`sample_summary.json` 和固定名 `perf_summary.json`。
Scope: 验证 CLI 能解析 sample log 并输出 HTML / archive summary / debug bundle summary。
Gaps: sample log 不是客户端真实采集日志。

Command: `qmclient_scripts/cmake-windows.cmd --build cmake-build-release --target game-client -j 14`
Result: pass. `menus_browser.cpp` 编译并成功链接 `DDNet.exe`。
Scope: 验证 Server Browser list_frame telemetry 和 perf helper include 在客户端目标中可编译。
Gaps: 不启动客户端做人工交互采集。

Command: `qmclient_scripts/cmake-windows.cmd --build cmake-build-release --target run_cxx_tests -j 14`
Result: pass. 1180 tests run, 1178 passed, 2 skipped.
Scope: 验证 `QmPerfShouldLogDuration()` helper、源码契约和既有 C++ 单测。
Gaps: 仍不覆盖真实 Server Browser 滚动交互日志内容。

Command: `python qmclient_scripts/gate/check_gate.py --mode quick --explain-scope --report-json-path tmp/check-gate-main-fix-report.json`
Result: pass. BaseRef 自动解析为 `origin/master`，BaseRef 可用；10 checks passed, 0 failed, 0 warnings.
Scope: 验证源码卫生、文档、header guard、ruff、shellcheck 等 quick gate。
Gaps: quick gate 不做真实构建测试。

Command: read-only code review subagent
Result: returned findings. Accepted and fixed: empty / no-frame logs no longer report a misleading PASS, report KPI / spikes / verdict now use the same frame-time sample set, operation signature comparison now checks pages / systems / events / stages, and Server Browser row counters are only maintained while perf logging is enabled.
Scope: 验证生产化性能量化实现的 TS 报表语义、summary JSON、operation comparison 和 C++ hot-path telemetry 开销。
Gaps: Server Browser `sort_changed` / `filter_active` / `refresh_triggered` 类字段仍未在本轮补齐；当前 Phase 1 只接入 `items_total`、`rows_visible`、`rows_rendered`、`rows_iterated`、`rows_skipped` 和耗时，后续应作为独立采集合同扩展。

Command: `Push-Location qmclient_scripts/perf; npm test; npx tsc --noEmit; Pop-Location`
Result: pass after review fixes. `npm test` 输出 `qmclient perf tests passed`，`npx tsc --noEmit` 退出码 0。
Scope: 复验 review 修复后的空样本 warning verdict、统一 frame-time KPI、operation stage / event comparison、summary `verdictAvailable` 字段。
Gaps: 不验证 C++ 编译。

Command: `qmclient_scripts/cmake-windows.cmd --build cmake-build-release --target run_cxx_tests -j 14`
Result: pass after review fixes. 1180 tests run, 1178 passed, 2 skipped.
Scope: 复验 `PerfListFrameEnabled` 源码契约、duration gate helper 和既有 C++ 单测。
Gaps: `run_cxx_tests` 不编译完整 `game-client` 客户端目标。
