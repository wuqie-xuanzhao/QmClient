# 设置页 FBO 用户可见问题清理 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `qmclient_scripts/perf` 量化系统已收口的前提下，清理 Settings 预热链路里剩余的 FBO 用户可见问题，只保留 live fallback，不新增任何 FBO 能力或 telemetry。

**Architecture:** 以 `src/game/client/components/menus.cpp` 和 `src/game/client/components/tclient/menus_tclient.cpp` 的 warmup / cache 入口为边界，沿着 `PrewarmSettingsRuntimeCaches -> PrewarmSettingsPageRuntimeCache -> DrawSettingsPageRuntimeCache` 做最小绕开和收口。所有行为变化都由 `src/test/settings_warmup_test.cpp` 的源码契约测试锁住；`docs/superpowers/specs/2026-06-08-页面性能优化框架设计.md` 仍然是本轮 FBO cleanup 的上位约束。

**Tech Stack:** C++, GoogleTest, CMake Windows gate, existing settings warmup tests.

---

## Scope

本计划只做 Settings / TClient / QmClient 预热链路里的 FBO cleanup，不重建性能量化系统，不改 `qmclient_scripts/perf`，也不把源头降温、Demo Browser 异步化或 Server Browser 列表优化塞进来。

## Source Context

**Authoritative docs:**

- `docs/superpowers/specs/2026-06-08-页面性能优化框架设计.md`
- `docs/superpowers/plans/2026-06-09-性能量化系统生产化实现计划.md`

**Current implementation anchors:**

- `src/game/client/components/menus.cpp`
- `src/game/client/components/tclient/menus_tclient.cpp`
- `src/test/settings_warmup_test.cpp`
- `src/game/client/components/qmclient/perf_logging.h`

## File Structure

- Modify `src/game/client/components/menus.cpp`
  - Keep Settings warmup / runtime cache behavior aligned with live fallback, and remove any remaining user-visible FBO gating from the warmup path.
- Modify `src/game/client/components/tclient/menus_tclient.cpp`
  - Keep TClient / QmClient sibling warmup non-blocking when the page/tab is not FBO-supported.
- Modify `src/test/settings_warmup_test.cpp`
  - Add source-contract coverage for the remaining FBO boundary behavior and guard the fallback path.

---

### Task 1: Lock the Remaining FBO Boundary Contract

**Files:**
- Modify: `src/test/settings_warmup_test.cpp`

- [ ] **Step 1: Write failing source-contract tests for the warmup boundary**

Add the following tests near the existing settings warmup checks:

```cpp
TEST(SettingsWarmup, RuntimeWarmupCanonicalizesBeforeFboSupportCheck)
{
	std::ifstream File("src/game/client/components/menus.cpp");
	ASSERT_TRUE(File.good());
	std::stringstream Buffer;
	Buffer << File.rdbuf();
	const std::string Source = Buffer.str();

	const size_t CanonPos = Source.find("Page = SettingsCanonicalPage(Page);");
	const size_t SupportPos = Source.find("if(!SettingsPageCanUsePageFbo(Page, SETTINGS_ASSETS, -1, Tab))");
	ASSERT_NE(CanonPos, std::string::npos);
	ASSERT_NE(SupportPos, std::string::npos);
	EXPECT_LT(CanonPos, SupportPos);
}

TEST(SettingsWarmup, UnsupportedSiblingTabsStayNonBlocking)
{
	EXPECT_FALSE(SettingsPageCanUsePageFbo(CMenus::SETTINGS_TCLIENT, CMenus::SETTINGS_ASSETS, -1, 0));
	EXPECT_TRUE(SettingsPageCanUsePageFbo(CMenus::SETTINGS_TCLIENT, CMenus::SETTINGS_ASSETS, -1, 1));
	EXPECT_FALSE(SettingsPageCanUsePageFbo(CMenus::SETTINGS_QMCLIENT, CMenus::SETTINGS_ASSETS, -1, 0));
}

TEST(SettingsWarmup, RuntimeWarmupSkipsUnsupportedSiblingFboPrewarm)
{
	std::ifstream File("src/game/client/components/tclient/menus_tclient.cpp");
	ASSERT_TRUE(File.good());
	std::stringstream Buffer;
	Buffer << File.rdbuf();
	const std::string Source = Buffer.str();

	EXPECT_NE(Source.find("if(!SettingsPageCanUsePageFbo(SETTINGS_TCLIENT, SETTINGS_ASSETS, -1, Tab))"), std::string::npos);
	EXPECT_NE(Source.find("if(!SettingsPageCanUsePageFbo(SETTINGS_QMCLIENT, SETTINGS_ASSETS, -1, Tab))"), std::string::npos);
	EXPECT_NE(Source.find("m_aSettingsTClientSiblingPrewarmed[Tab] = true;"), std::string::npos);
	EXPECT_NE(Source.find("m_aSettingsQmClientSiblingPrewarmed[Tab] = true;"), std::string::npos);
}
```

- [ ] **Step 2: Run the test target and confirm the current tree still lacks at least one cleanup guard**

Run:

```powershell
qmclient_scripts/cmake-windows.cmd --build cmake-build-release --target run_cxx_tests -j 14
```

Expected: at least one of the new contracts fails before the cleanup patch is applied.

- [ ] **Step 3: Add the minimum cleanup patch if the tests expose a remaining FBO gate**

If the tests show a remaining warmup stall, adjust the relevant callsite so the page/tab is canonicalized before the FBO capability check and unsupported sibling tabs are marked prewarmed instead of stalling the cursor. The intended shape is:

```cpp
Page = SettingsCanonicalPage(Page);
if(Page == SETTINGS_TCLIENT)
	Tab = CanonicalizeTClientCacheTab(Tab);
else if(Page == SETTINGS_ASSETS)
	Tab = CurrentSettingsAssetsTab();

if(!SettingsPageCanUsePageFbo(Page, SETTINGS_ASSETS, -1, Tab))
{
	LogSettingsWarmupPerf(Page, Tab, "miss", "n/a", ESettingsWarmupMissReason::PAGE_FBO_UNSUPPORTED, PerfDebugElapsedMs(PerfStartTime));
	return false;
}
```

and for sibling prewarm:

```cpp
if(!SettingsPageCanUsePageFbo(SETTINGS_QMCLIENT, SETTINGS_ASSETS, -1, Tab))
{
	if(Tab >= 0 && Tab < NUMBER_OF_QMCLIENT_SETTINGS_TABS)
		m_aSettingsQmClientSiblingPrewarmed[Tab] = true;
	continue;
}
```

- [ ] **Step 4: Run the C++ tests again**

Run:

```powershell
qmclient_scripts/cmake-windows.cmd --build cmake-build-release --target run_cxx_tests -j 14
```

Expected: the new warmup boundary tests pass.

---

### Task 2: Keep Live Fallback Aligned With the Cleanup

**Files:**
- Modify: `src/game/client/components/menus.cpp`
- Modify: `src/test/settings_warmup_test.cpp`

- [ ] **Step 1: Write a failing contract that protects the live fallback path**

Add this source-contract test:

```cpp
TEST(SettingsWarmup, DrawRuntimeCacheStillFallsBackWhenFboIsUnavailable)
{
	std::ifstream File("src/game/client/components/menus.cpp");
	ASSERT_TRUE(File.good());
	std::stringstream Buffer;
	Buffer << File.rdbuf();
	const std::string Source = Buffer.str();

	const size_t DrawPos = Source.find("bool CMenus::DrawSettingsPageRuntimeCache");
	ASSERT_NE(DrawPos, std::string::npos);
	EXPECT_NE(Source.find("Graphics()->DrawRenderTarget(pCache->m_RenderTarget, ContentView.x, ContentView.y, ContentView.w, ContentView.h);"), std::string::npos);
	EXPECT_NE(Source.find("SettingsPageRuntimeCacheShouldShortCircuit(pCache->m_State, Page, Tab, Width, Height, RuntimeKey);"), std::string::npos);
}
```

- [ ] **Step 2: Run the tests and confirm the contract catches regressions**

Run:

```powershell
qmclient_scripts/cmake-windows.cmd --build cmake-build-release --target run_cxx_tests -j 14
```

Expected: the live fallback contract passes only when the draw path still keeps the non-FBO fallback available.

- [ ] **Step 3: Keep the draw path minimal**

If any cleanup work touches `DrawSettingsPageRuntimeCache`, keep the behavior to:

```cpp
if(!SettingsPageCacheCanUseRecordedResources(CacheMatches, pCache->m_RenderTarget.IsValid(), pCache->m_State.m_ResourcesReadyAtRecord, DependentSubcachesReadyAtRecord))
{
	const ESettingsWarmupMissReason Reason = SettingsPageRecordedCacheMissReason(CacheMatches, pCache->m_RenderTarget.IsValid(), pCache->m_State.m_ResourcesReadyAtRecord, DependentSubcachesReadyAtRecord);
	LogSettingsWarmupPerf(Page, Tab, "miss", "n/a", Reason, PerfDebugElapsedMs(PerfStartTime));
	return false;
}

Graphics()->DrawRenderTarget(pCache->m_RenderTarget, ContentView.x, ContentView.y, ContentView.w, ContentView.h);
SettingsPageRuntimeCacheShouldShortCircuit(pCache->m_State, Page, Tab, Width, Height, RuntimeKey);
```

No new FBO path, no new telemetry shape, no FBO A/B or shadow metrics.

---

### Task 3: Verify and Gate

**Files:**
- No new source files beyond earlier tasks.

- [ ] **Step 1: Run the C++ gate**

Run:

```powershell
qmclient_scripts/cmake-windows.cmd --build cmake-build-release --target run_cxx_tests -j 14
```

Expected: `run_cxx_tests` exits with code 0.

- [ ] **Step 2: Run the quick gate**

Run:

```powershell
python qmclient_scripts/gate/check_gate.py --mode quick
```

Expected: quick gate exits with code 0.

- [ ] **Step 3: Record any gaps explicitly**

If any remaining FBO path still needs a separate cleanup, stop there and record it as a follow-up bugfix / cleanup item; do not expand this plan into source cooling or new FBO telemetry.

## Completion Criteria

The implementation is complete only when all are true:

1. Settings / TClient / QmClient warmup no longer stalls on unsupported FBO paths.
2. Live fallback still exists in `DrawSettingsPageRuntimeCache` for unavailable or stale cache states.
3. `settings_warmup_test.cpp` locks the boundary behavior with source-contract coverage.
4. `run_cxx_tests` and `check_gate --mode quick` pass.
5. No new FBO capability, FBO telemetry, or FBO A/B / shadow tracking is introduced.

