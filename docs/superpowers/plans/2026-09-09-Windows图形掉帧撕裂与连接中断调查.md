---
title: Windows 图形掉帧撕裂与连接中断调查
date: 2026-09-09
updated: 2026-09-11
status: active
related_spec: /docs/superpowers/specs/2026-09-09-QmClient-GPU-driven-2D渲染架构规格.md
---

# 1. 现象与范围

2026-09-09 18:28 至 18:57，用户在更新 NVIDIA 驱动和 Windows 11 Insider Preview 后观察到：

- Vulkan 关闭垂直同步时撕裂严重，并出现明显掉帧和卡顿；
- Vulkan 开启垂直同步后撕裂减轻，但卡顿仍存在，输入延迟明显增加；
- OpenGL 相比 Vulkan 有改善，但仍然掉帧；
- 偶尔画面完全停滞，随后游戏连接中断。

本调查同时覆盖 Vulkan、OpenGL、Windows 合成/驱动、外部图形注入层、主循环等待和网络泵停顿。当前证据不足以把问题单独归因于 Vulkan、Vulkan 1.4、网络或 QmClient UI。

用户补充：同一台机器上的官方 DDNet、BC 客户端和 QmClient 都出现该问题，而其他机器没有反馈同样现象。该交叉客户端证据显著降低了“QmClient 单独的 renderer bug”作为首要假设的优先级，也意味着 MAILBOX/IMMEDIATE 调整不可能是完整修复。

# 2. 当前机器事实

本轮只读检查得到：

```text
GPU: NVIDIA GeForce RTX 4060 Ti
NVIDIA driver: 620.02
Windows: Windows 11 Pro Insider Preview
OS build: 29648.1000
Vulkan loader instance: 1.4.350
GPU Vulkan API: 1.4.351
GPU conformance: 1.4.3.3
```

`vulkaninfo --summary` 同时发现以下 implicit layers：

```text
VK_LAYER_GAMEPP                 API 1.3.296
VK_LAYER_OBS_HOOK               API 1.3.216
VK_LAYER_VALVE_steam_overlay    API 1.3.207
VK_LAYER_VALVE_steam_fossilize  API 1.4.303
VK_LAYER_reshade                API 1.3.268
VK_LAYER_NV_optimus             API 1.4.351
VK_LAYER_NV_present             API 1.4.351
```

Loader 还报告 BestClient 的 ReShade manifest 存在，但 `ReShade64.dll` 加载失败。当前正在运行 NVIDIA Overlay。以上事实不能单独证明根因，但会改变 Vulkan instance/device/present 调用链，必须先做无注入层对照。

2026-09-08 13:35 的本机 QmClient 崩溃报告显示：异常为 `0xC0000005`，故障地址位于 NVIDIA OpenGL 驱动 `nvoglv64.dll`。同一进程加载了 `graphics-hook64.dll`、`GPP_VKLayer64.dll` 和 `nvspcap64.dll`。该报告来自工作区外的正式客户端，只作为历史证据读取，不对该客户端执行操作。

# 3. Vulkan 1.1、1.3、1.4 在当前代码中的真实差异

当前 `backend_vulkan.cpp` 支持按 `1.4 -> 1.3 -> 1.1` 创建 instance/device，并记录 effective API version。但 `VkDeviceCreateInfo.pEnabledFeatures` 和 `pNext` 当前为空，后端没有因为选择 1.3 或 1.4 自动启用 dynamic rendering、synchronization2、timeline semaphore、descriptor indexing 或 Vulkan 1.4 的其他现代能力。

因此当前三个档位的主要实际差异是：

- instance/device 声明的 API version；
- 设备和 loader 是否接受该版本；
- 驱动和 implicit layer 进入的兼容路径；
- 核心命令/扩展的可见性边界。

它们目前不是三个不同的性能 renderer。若 1.1、1.3、1.4 在同一场景表现明显不同，优先怀疑驱动、loader、implicit layer 或版本协商路径，而不是认为 1.4 已自动使用更现代的 GPU 功能。

# 4. 已确认的代码风险

## 4.1 关闭 VSync 时优先 IMMEDIATE

Windows/Linux Vulkan 在 `gfx_vsync 0` 时按以下顺序选择 present mode：

```text
IMMEDIATE -> MAILBOX
```

`IMMEDIATE` 允许显示器在扫描过程中接收新图像，撕裂是该模式的预期风险。当前严重撕裂不能仅视为驱动回归。首轮修复候选是把无 VSync 的默认顺序改为 `MAILBOX -> IMMEDIATE`，但必须先确认帧延迟、刷新率和窗口模式下的实际结果。

该调整只属于显示策略优化，不是本次跨客户端卡顿的主修复。因为官方客户端、BC 和 QmClient 也复现，必须先完成机器级证据采集；不能用 present mode 改动掩盖共同环境问题。

## 4.2 VSync 的延迟与卡顿

开启 VSync 后优先 `FIFO`。当前架构的 graphics thread 只有一个待处理 command buffer 槽；`CGraphicsBackend_Threaded::RunBuffer()` 在提交下一批前调用 `WaitForIdle()`。如果 render thread 在 acquire、fence、present 或驱动内部阻塞，主线程会在后续 `graphics_swap` 被反压。

这可以解释 VSync 下输入延迟增大，也说明 `graphics_swap` 高耗时不等于纯 GPU 着色耗时。

## 4.3 网络泵与渲染等待位于同一主循环

`CClient::Update()` 每个主循环调用一次 `PumpNetwork()`，随后才进入 render/swap。若主线程长时间阻塞在 graphics swap、驱动调用或同步等待，网络收包和 `CNetClient::Update()` 也不会推进。足够长的停顿可能先表现为画面冻结，随后触发 snapshot gap、too weak connection 或 timeout。

因此“连接中断”可能是长时间进程/图形停顿的结果，而不一定是网络根因。现有 10 秒 hang watchdog 能记录主循环完全停止，但 1 至 9 秒的严重停顿仍可能影响连接且不会生成 hang report。

## 4.4 Vulkan profiling 缺口

现有 verbose Vulkan profile 已记录 fence wait、queue submit、queue/device wait 和 GPU timestamp，但没有单独记录：

- `vkAcquireNextImageKHR` 墙钟时间；
- `vkQueuePresentKHR` 墙钟时间；
- graphics thread command-buffer backpressure 等待；
- present mode、swapchain image count 和 Windows 窗口/合成状态的每会话 metadata；
- 同一时间窗口的 network pump、snapshot age 和 hang heartbeat gap。

没有这些字段时，无法区分 acquire/present、GPU fence、render thread backlog、CPU render、外部注入层和网络停顿。

# 5. 当前假设排序

1. **外部注入层与新驱动/Insider 系统组合问题。** Vulkan Loader 已发现多个 1.3 implicit layer；OpenGL 历史崩溃也加载了 OBS、GamePP、NVIDIA Overlay，并落在 NVIDIA 驱动。
2. **Vulkan IMMEDIATE present 导致撕裂。** 这是代码可直接证明的行为，但不能解释 OpenGL 掉帧和连接中断。
3. **驱动或 present/acquire 长停顿通过 graphics thread 反压主线程。** 可同时造成 Vulkan/OpenGL 掉帧、画面冻结和网络泵停顿。
4. **QmClient Vulkan 后端既有状态/生命周期缺陷。** render pass、command buffer 状态缓存和 render target 问题仍需修复，可能放大驱动差异。
5. **Windows 11 Insider build 29648 或 NVIDIA 620.02 回归。** 需要与稳定 OS/旧驱动或官方 DDNet 对照，当前无法仅靠源码确认。
6. **纯网络问题。** 目前优先级较低，因为掉帧同时影响两个图形 API，且连接中断前出现画面完全停滞；仍须用 packet/snapshot 时间线排除。

# 6. A/B 复现矩阵

每组使用同一地图、窗口模式、分辨率、刷新率、相机路径和客户端配置，至少运行 3 分钟；出现完全停滞时保留日志和 hang/crash dump。

| 组 | Backend | API | Present/VSync | 注入层 |
| --- | --- | --- | --- | --- |
| A | Vulkan | 1.4 | VSync off | 全部关闭 |
| B | Vulkan | 1.3 | VSync off | 全部关闭 |
| C | Vulkan | 1.1 | VSync off | 全部关闭 |
| D | Vulkan | 1.4 | VSync on/FIFO | 全部关闭 |
| E | OpenGL | 当前版本 | VSync off | 全部关闭 |
| F | OpenGL | 当前版本 | VSync on | 全部关闭 |
| G | Vulkan/OpenGL | 与无层基线相同 | 相同 | 逐个恢复 NVIDIA Overlay、OBS、GamePP、Steam、ReShade |

首轮不同时改变 API、VSync、限帧、窗口模式和注入层，避免无法归因。ReShade manifest 指向缺失 DLL，应先移除或通过其 `DISABLE_VK_LAYER_reshade_1=1` 对照；GamePP 和 OBS 使用各自 manifest 声明的 disable environment。不要删除 registry 项作为第一步。

# 7. 需要补充的诊断实现

实现前先增加低开销观测，不直接猜测性能修复：

1. Vulkan 记录 acquire、image-fence wait、command record、queue submit、queue present 的独立墙钟时间。
2. `CGraphicsBackend_Threaded::RunBuffer/WaitForIdle` 记录主线程等待 render thread 消费上一批命令的时间。
3. 每个图形会话记录 effective Vulkan API、driver、OS build、present mode、swap image count、窗口模式、刷新率和已发现工具层。
4. 卡顿窗口关联 `graphics_swap`、`client_update/PumpNetwork`、snapshot age、recv packet delta 和 hang heartbeat gap。
5. 将 hang watchdog 增加轻量级 1 秒以上 stall breadcrumb；10 秒才写 dump 的策略可保留。

## 7.1 日志分层要求

当前 `qm_perf_debug 1` 通过 `QmPerfEnabled()` 同时打开主循环、render、input、UI、菜单、资源、文本和图形相关阶段；`qm_perf_logfile 1` 也会让这些阶段进入同一个文件。该设计适合长期性能趋势，不适合驱动回归定位，容易产生 GB 级日志并掩盖真正的等待点。

后续应增加独立的图形诊断级别，不改变现有长期性能日志语义：

当前配置按作用域分为三类：

| 类别 | 配置项 | 作用 | 体积/用途 |
| --- | --- | --- | --- |
| 通用日志 | `logfile`、`debug`、`ec_output_level` | 控制基础日志目标与全局详细度 | 长期运行；不用于帧级采样 |
| 性能/业务 | `qm_perf_debug`、`qm_perf_logfile`、`qm_perf_debug_threshold_ms`、`qm_perf_stutter_diagnostics`、`qm_ui_runtime_v2_debug` | 主循环、UI、资源、文本和卡顿业务指标 | 适合组件归因；开启后可能高体积 |
| 图形/平台诊断 | `qm_graphics_trace` | backend、swapchain、GPU/CPU 等待和平台捕获；macOS signpost 也归入此级别 | 短时复现；独立 `perf/graphics/*` 通道 |

`qm_graphics_trace` 已落地为 0-3 级独立开关，不再由 `qm_perf_debug` 隐式开启 Vulkan backend 采样。旧的 `qm_macos_graphics_diagnostics` 保留为兼容别名，会被提升为至少一级图形 trace，不再维护第二套语义。`qm_perf_logfile 1` 仍负责把 `perf/*` 路由到专用文件；图形 trace 开启时也会自动创建该文件。

当前已实现的采样范围：Vulkan 的设备元数据、acquire/fence/submit/present/GPU timestamp、OpenGL 的上下文元数据与慢 `SDL_GL_SwapWindow`、渲染线程 `WaitForIdle` 反压，以及主循环 `PumpNetwork` 的执行耗时和两次调用间隔。网络采样只在 `qm_graphics_trace>=2` 且出现 8 ms 执行耗时或 100 ms 间隔时写入 `perf/graphics/network`；线程反压写入 `perf/graphics/thread`。

```text
qm_graphics_trace=0 关闭图形细分
qm_graphics_trace=1 每 120 帧图形摘要：API、present mode、swapchain、GPU/CPU 总时间
qm_graphics_trace=2 摘要 + acquire/fence/submit/present 分项；8 ms 以上写单帧异常
qm_graphics_trace=3 在级别 2 基础上允许详细 backend/validation 辅助信息，仅用于短时复现
```

图形诊断应写入独立文件或独立 `system`，而不是强制打开所有 `perf/*` 阶段。默认阈值建议为 8 ms；异常帧才记录完整上下文。单次采集目标是数十 MB，而不是数 GB。

在该分层完成前，最小采集方式是只开启 `qm_perf_debug 1` 的短窗口，复现后立即关闭；同时避免长时间运行设置页资源预览、文本生成和后台资源扫描路径。

## 7.2 QmClient 本机采集包

官方 DDNet 和 BC 的共同复现只作为外部归因证据；本项目不实现它们的日志适配或采集协议。QmClient 自身需要定义稳定的 machine/session metadata，用于把本机图形、主循环和网络时间线对齐：

```text
timestamp_utc
os_build / display_driver
gpu_name / pci_id / driver_version
monitor_id / refresh_hz / hdr / gsync_freesync
window_mode / drawable_size / dpi
backend / api_version / present_mode / swapchain_images
overlay_layers / capture_hooks
frame_cpu_ms / gpu_ms / acquire_ms / fence_wait_ms / present_ms
queue_submit_ms / render_thread_wait_ms
network_pump_gap_ms / snapshot_age_ms / recv_packets_delta
```

采集分三层，避免把高体积业务日志带入机器诊断：

1. **Machine layer**：OS build、驱动、显示器、DWM/overlay/hook、GPU 电源和系统事件；由一次机器诊断包提供。
2. **Present layer**：每帧或异常帧的 acquire、submit、present、fence、GPU timestamp、backpressure；由各客户端图形后端提供。
3. **Game/network layer**：主循环、`PumpNetwork`、snapshot age、连接状态和 hang breadcrumb；由 QmClient 提供。

QmClient 只需输出短时窗口和统一 CSV/JSONL 字段，随后按 `timestamp/session/frame` 对齐本机事件。这样不需要用户为每一个 Vulkan 版本和 present mode 重复跑长时间 A/B。

# 8. 修复决策门

- 无注入层后 Vulkan 撕裂仍存在：验证 `MAILBOX -> IMMEDIATE` 默认顺序和可配置 present mode。
- 无注入层后 Vulkan 卡顿消失：不先修改 renderer，增加工具层诊断和用户可见提示，逐层定位冲突者。
- Vulkan 1.4 卡顿而 1.3/1.1 正常：检查 instance API 与 implicit layer/driver 兼容路径；不能把结果描述成 1.4 GPU 功能回归，因为当前未启用这些功能。
- 官方、BC、QmClient 在同一时间窗口都有相同卡顿：将机器/驱动/DWM/overlay 作为首要调查面；QmClient 只修复能由自身日志和调用链证明的等待或生命周期问题。
- 只有 QmClient 的 `present_ms` 或 `render_thread_wait_ms` 异常：再进入 QmClient backend 修复；不能因其他客户端“感觉较轻”就排除系统层，只能以对齐后的时间线判断。
- Vulkan/OpenGL 都在 graphics-thread wait 处长停顿：处理 render thread backpressure、driver stall 隔离和主循环网络推进问题。
- GPU 时间正常但 frame/loop 时间异常：转查 CPU 组件、系统调度、hook 和日志/后台任务。
- 画面冻结时 network recv 仍正常：连接中断不是网络收包饥饿，需要继续检查客户端状态机和 socket 错误。

# 9. 与 GPU-driven 2D 架构的关系

本问题是 GPU-driven 2D 架构 P0 的阻断项。persistent upload、indirect draw、render graph 和 compute culling 都会增加同步与资源生命周期复杂度。在 present、render-thread backpressure、外部 layer 和网络泵关联未建立可靠诊断前，不应把新架构默认开启，也不能用大规模重写掩盖当前回归。

GPU-driven 实施可以继续进行 API-neutral 合同和测试设计，但首个运行时代码批次必须先完成本调查的诊断字段和无注入层 A/B 基线。

# 10. AMD 与 macOS 的专项边界

## 10.1 AMD

当前 QmClient 曾因分身小窗动态 viewport 崩溃，对 AMD 采用过厂商级强制单线程规避。现有证据只能证明当时某条小窗/viewport 路径存在 AMD 风险，不能证明 AMD Vulkan 多线程普遍不可用，也不能证明所有 AMD 驱动都需要单线程。

AMD 设备的日志必须额外记录：

- vendor/device/revision、驱动版本和 Vulkan effective API；
- 实际 render thread 数、secondary/primary command buffer 模式；
- viewport/scissor 更新、render pass 切换和 command buffer restart；
- queue/fence/present 等待，以及是否触发 AMD workaround；
- 小窗、强制宽高比、背景捕获、readback 和跨批次绘制的场景标记。

验证顺序应为“保留 workaround 的稳定基线 -> 修复应用侧 pass/状态问题 -> 同一设备逐步恢复 2/3 线程”，不能直接删除 AMD 限制。没有 AMD 真机和驱动版本证据时，不得把 NVIDIA 或 macOS 的结论外推到 AMD。

## 10.2 macOS

macOS 当前主路径是原生 Metal；Vulkan/MoltenVK 已不再作为默认路径。Metal 后端已经具备比 Vulkan 更完整的诊断计数，包括 frame slot wait、drawable acquire 次数/耗时、drawable unavailable、GPU completion wait、GPU execution timing、command processing、upload/readback、display sync、maximum drawable count 和 `presentsWithTransaction`。

macOS 的 machine/present 采集应额外记录：

- macOS 版本、Apple GPU family、Metal feature set、外接显示器和 ProMotion/刷新率；
- `CAMetalLayer.displaySyncEnabled`、`allowsNextDrawableTimeout`、`maximumDrawableCount`、`presentsWithTransaction`；
- `nextDrawable()` 等待、frame slot semaphore、command buffer status 和 GPU completion；
- 窗口切换、睡眠唤醒、HiDPI resize、后台/前台和外接屏变化。

Metal 不能简单套用 Vulkan 的 present mode 结论：它没有 `MAILBOX/IMMEDIATE` 同名选择，卡顿更应从 drawable 池耗尽、frame slot 等待、command buffer 完成时间和系统显示同步判断。已有 `perf/macos_metal` 每 120 帧摘要可以作为基础，但还需要统一字段名，以便和 Vulkan/OpenGL/官方客户端日志对齐。

macOS 验证必须使用原生 Metal 真机和 GPU capture；Windows Vulkan、MoltenVK 或 OpenGL 的结果不能替代 Apple 设备证据。

# 11. 2026-09-11 22:05 复现证据（服务器列表滚动）

## 11.1 数据来源

- 日志：`%APPDATA%/DDNet/dumps/QmClient_Perf/qm_perf_2026-09-11_22-05-51.log`（22:05:51–22:06:43，约 52 秒，14 MB）。
- 场景：`page=server_browser`、`operation=server_browser_scroll`、`context=offline`，`items_total=1367`、`items_visible=24`。
- 该次运行未开启 `qm_graphics_trace`，因此没有 acquire/present/swapchain 与 `perf/graphics/*` 记录；`perf/stutter` 也没有记录（未开 `qm_perf_stutter_diagnostics`）。

## 11.2 fps_summary 序列：1% low 逐段恶化

| 时间 | 样本帧 | 时长 s | fps_avg | fps_1pct_low | p99 ms | max ms | menu_ms_max |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 22:05:54 | 144 | 0.75 | 193.0 | 34.3 | 18.0 | 40.4 | 9.1 |
| 22:06:24 | 726 | 2.07 | 351.3 | 107.0 | 8.4 | 10.7 | 2.2 |
| 22:06:26 | 664 | 1.91 | 347.5 | 91.0 | 10.1 | 11.7 | 1.7 |
| 22:06:28 | 890 | 2.27 | 391.7 | 76.6 | 12.1 | 14.3 | 1.0 |
| 22:06:35 | 1930 | 5.76 | 335.4 | **44.5** | 18.3 | 28.5 | 1.3 |

`fps_1pct_source=real_sampled`、`cap_limited=0`。

各窗口对应不同的操作阶段，不能按时间先后推断“累积恶化”。按秒统计 `loop_total >= 16 ms` 的长帧分布更说明问题：

| 时间段 | 长帧/秒 |
| --- | --- |
| 22:05:53–22:06:10 | 1–3（启动与早期零星） |
| 22:06:11–22:06:31 | **0（连续 20 秒无长帧）** |
| 22:06:32–22:06:43 | **4–6（连续 12 秒密集）** |

即 1% low 44.5 由 22:06:32–22:06:43 这一**连续高负载区间**单独决定，前后都有正常帧段，属于特定场景诱发，不是随时间累积或漂移。

## 11.3 按帧归因（`loop_total >= 16 ms` 共 80 帧）

- `client_update` 主导 76 帧，末期连续 19–28 ms（22:06:35–22:06:43 几乎每帧超标）。
- `input_update` 主导的极端帧来自**单次输入事件在 `SDL_PollEvent` 内的极端延迟**：262 ms 携带 `event_type=1024`（SDL_MOUSEMOTION）、217 ms 携带 `event_type=769`（SDL_KEYUP），同帧 `events` 仅 1–10；另外还有 3–10 ms 级的鼠标移动/窗口事件。即不是事件数量、分发或 UI 处理开销，而是单个输入事件在 SDL/系统/注入层被延迟数百毫秒。启动期另有 179 ms（`result=0`，无事件）。
- `frame_render` 最大 16.7 ms（仅 1 次），`graphics_swap` 无超阈值记录；`client_update` 的 `state=0`、`editor=0`。
- UI/菜单侧很轻：`menu-ui ui_ms` 0.2 ms、`text_container_create_ms` 0.09 ms、`menu_ms_max` 1.0–2.2 ms、`msdf_draws` 62、`glyph_new` 0。
- 设备采样：GPU 47–66%，进程 CPU 7–15%，进程内存 3.64 → 4.03 GB。`perf/device` 由独立采样线程（100 ms 轮询）产生，不占用主线程。

## 11.4 结论与假设更新

1. 本场景 1% low 由 **CPU 侧 `CClient::Update()`** 主导，不是 GPU 着色、present 或 swap；这与“官方/BC/QmClient 同机复现”的系统层假设不冲突，只是把 QmClient 自身可优化面收窄到主循环更新路径。
2. 最差帧由**单个输入事件的极端延迟**（鼠标移动 262 ms、键抬起 217 ms，单帧事件数仅 1–10）造成，该路径不经过 QmClient 渲染代码，也不是事件风暴或客户端分发开销；与假设 1（注入层/驱动/Windows 版本）一致，需无注入层 A/B 验证，并保留该埋点作为证据。
3. 长帧集中在 22:06:32–22:06:43 的连续区间（12 秒、4–6 帧/秒），该区间外连续 20 秒无长帧；因此这是**特定高负载场景**（低频高难场景）的帧成本问题，而不是滚动时长累积或内存增长导致。需要在该区间内定位：`serverbrowser_update`、`gameclient_onupdate` 与组件级 `on_update` 谁是主要成本。
4. 原假设排序中“纯网络问题”进一步降级：`PumpNetwork` 无超阈值记录（该次未开 network 采样，不足以完全排除），但长帧与 input/render 无关联。

## 11.5 本轮诊断补齐

`CClient::Update()` 的子阶段（`pump_network`、`gameclient_onupdate`、`editor_onupdate`、`serverbrowser_update`、`discord_steam_update`）此前只在 `qm_graphics_trace>=2` 时记录到 `perf/graphics/update`。现已改为：在 `qm_perf_debug` / `qm_perf_logfile` / `qm_perf_stutter_diagnostics` 任一开启时同样记录到 `perf/main_thread`，无需开启 graphics trace 即可分解 `client_update`。graphics trace 的原有通道与字段保持不变。

组件级 `on_update` / `on_render` 采样（`perf/stutter component_sample`，含 `total/avg/p95/max`）已具备，入口是 `qm_perf_stutter_diagnostics 1`。

## 11.6 下次测试配置（修正）

```text
qm_perf_stutter_diagnostics 1   # 保留：组件级 on_update/on_render 归因
qm_perf_debug 0                 # 降低日志量（子阶段埋点仍随 stutter 诊断启用）
qm_perf_logfile 1               # 输出到 dumps/QmClient_Perf
qm_graphics_trace 2             # 排除 present/acquire/swapchain
```

滚动 30 秒以上，重点看：

- `perf/main_thread|serverbrowser_update`：>3 ms 说明服务器列表数据/排序是主因；
- `perf/main_thread|gameclient_onupdate`：高则继续看 `perf/stutter component_sample callback=on_update` 的 `module` 字段；
- `perf/stutter component_sample`：直接给出组件名与 `p95/max`；
- `perf/main_thread|pump_network`：确认网络收包是否参与；
- `perf/graphics/*`：若 `slow_swap` / `wait_for_idle` 仍为空，则 present/backpressure 假设可继续降级。

# 12. 2026-09-11 23:02 复现与 stutter 诊断修复

## 12.1 数据来源与配置

- 日志：`qm_perf_2026-09-11_23-02-14.log`（11.5 MB，23:02:14–23:02:35）：server_browser 滚动 → 连接 → 游戏内 → 回菜单。
- 配置：`gfx_backend "OpenGL"`、`gfx_refresh_rate 400`、`gfx_vsync` 未设置（默认 0），运行期 `idle_render_throttle vsync=0`，确认游戏内 16–17 ms 不是垂直同步限制。
- `qm_perf_stutter_diagnostics 1` 已开启，但日志中 **`perf/stutter` 为 0 条**。

## 12.2 结论

1. 59 个 `loop_total >= 16 ms` 长帧中 55 个由 `client_update` 主导，且 `client_update` 与 `gameclient_onupdate` 几乎相等（17.540/17.513、16.695/16.663、24.570/24.570），说明成本在 `CGameClient::OnUpdate()` 内部；`pump_network`、`serverbrowser_update`、`discord_steam_update` 均未进入前列。
2. 游戏内（`state=3`）长帧分两类：
   - `gameclient_onupdate` 12–17 ms 的持续帧（需组件级数据定位）；
   - 渲染侧偶发 29–43 ms，其中 23:02:32 与 ESC 菜单打开重合：`ingame_text_runtime_drain` 42.652 ms、`menu_ms_max` 29.2 ms。
3. 文本系统存在单帧尖峰：`text_frame_stats` 记录 `glyph_rasterize_ms=28.583`（`glyph_new=160`、`glyph_uploads=312`、`container_creates=30/41`）。Zoom 改变相机视野与可见内容，首次遇到新字号/新字符时字形光栅化同步发生在渲染帧内，与“游戏内 Zoom +/- 卡顿”的现象吻合，需要用组件级数据确认归属。
4. **stutter 诊断未产出的根因**：`CGameClient::ProcessQmStutterFrame()` 只有声明与定义、没有任何调用点，`RecordComponentUpdate` / `RecordComponentRender` 采集的组件级样本从未被消费。已修复：在 `OnRender()` 末尾调用 `ProcessQmStutterFrame()`，并在 `OnShutdown()` 用 `EQmStutterFlushReason::SHUTDOWN` 强制结算窗口。
5. 输入侧除单事件延迟外还存在一帧内事件积压：本次出现 `events=358` / `duration_ms=238.120`（`event_type=1024` 鼠标移动）与 25 ms（`events=255`）。

## 12.3 下一步验证

用本次修复后的构建复现：进服后游戏内 30 秒以上，反复 Zoom +/-，再打开 ESC 菜单，最后正常退出。期望在日志中看到：

- `perf/stutter` 的 `stutter_event`（`classification=sustained_low/frame_drop`，含 `frame_ms_p95/p99/max`）；
- `component_sample callback=on_update module=<组件名>`：直接指认 12–17 ms 的持有者；
- `component_sample callback=on_render`：与文本/菜单长帧对齐；
- 若仍无输出，检查 `PerfFrame()` / `RenderFrameTime()` 在该调用路径上的取值是否为有效正数。

# 13. 首次 stutter 报告结果与 ESC 文本池修复（2026-09-11 23:19）

## 13.1 报告结果

日志 `qm_perf_2026-09-11_23-18-57.log`，7 个窗口、168 个组件采样：

| 组件·回调 | 单帧 max | 说明 |
| --- | --- | --- |
| `menus`·on_render | 46.118 ms（p95 33.117，累计 220.477） | 窗口 2，online 且 `active=1`（ESC 打开） |
| `skins7`·on_update | 32.166 ms | 进服早期；异步加载、有回退，用户确认不构成可感卡顿 |
| `menu_background`·on_render | 13.495 ms | 进服早期 |
| `qmclient`·on_update | 11.681 ms | 进服早期 |
| `skins`·on_update | 10.216 ms | 进服早期 |
| 其余 163 项 | < 1 ms | — |

`menus` 重帧内部构成：`ingame_esc_tab_content` 24.904 ms、`ingame_esc_button_column` 11.780 ms、`menu_menubar` 5.056 ms；菜单保持打开时每帧 `ingame_text_runtime_drain` 5.0–32.99 ms。

## 13.2 根因修正

预算日志（`event=settings_adaptive_budget source=ingame_server_info_snapshot_text`）显示 `text_container_tokens=1`、`glyph_rasterize_tokens=1`，同时 `text_create_ewma_ms=22.354`、`glyph_raster_ewma_ms=29.659`。即：

- **不是**每帧排队过多（令牌已经是 1）；
- **而是单次文本容器构建触发的同步字形光栅化**本身就要 22–33 ms（`text_frame_stats` 亦记录 `glyph_rasterize_ms=28.583`、`glyph_new=160`）。

因此“给 drain 增加每帧数量硬上限”无效，必须在“降低单次构建成本”和“把构建移出游戏内帧”之间选择。

## 13.3 本轮修复

- `CMenus::PrewarmIngameEscTextPoolForLoading`（`menus.h`）复用设置页的 plan/预建管线，以 `ingame_esc_open` 操作收集并预建 ESC 菜单文本池。
- `CGameClient::PrewarmSettingsRuntimeCachesDuringLoading`（`gameclient.cpp`）在设置页预热循环之后新增 ESC 预热循环（最多 48 步、每步 8 预算、带连续无进展退出），把构建成本放在加载画面内。
- `game-client` 构建通过。

## 13.4 待验证与后续方案

进服后打开 ESC，检查 `perf/menu|ingame_esc_tab_content` 与 `ingame_text_runtime_drain` 是否显著下降、`settings_adaptive_budget` 的 `backlog` 是否归零。

若仍高，说明 ESC 页面存在未纳入 plan 的动态文本，需要升级为止回方案：按块构建（用 `TextRender()->CreateOrAppendTextContainer` 分帧 append，参考 `DrainIngameMotdParagraphCache` 的 `m_BuildByteOffset` 模式），把单次 22–33 ms 拆成多帧 ≤2 ms。

# 14. 复测结果与第二次修复（2026-09-11 23:34）

## 14.1 复测没有改善

`qm_perf_2026-09-11_23-34-44.log`（已使用含 §13.3 修复的 `DDNet.exe`）中：

- `ingame_text_runtime_drain` 仍有 46.791 / 35.580 / 19.143 / 17.315 ms；
- 打开 ESC 时 `settings_text_plan_collection` 仍为 `units_done=1/21`、`remaining=18`；
- 加载阶段（23:34:44–49）**没有任何 `settings_adaptive_budget` 记录**，说明加载阶段的文本预建循环没有运行。

## 14.2 根因

`CGameClient::PrewarmSettingsRuntimeCachesDuringLoading` 的首行是：

```cpp
if(g_Config.m_QmSettingsPrewarm == 0)
    return;
```

而 `qm_settings_prewarm` 的默认值是 **0**（`config_variables_qmclient.h:473`）。因此不仅 §13.3 新增的 ESC 预热，**原本的设置页文本加载预热在默认配置下也从未执行过**，所有文本构建仍留到运行时（打开 ESC / 切页）现场完成。

## 14.3 第二次修复

在 `gameclient.cpp` 中把设置页预热保留在 `qm_settings_prewarm` 开关内，把 ESC 文本池预热移到开关之外：ESC 首帧卡顿与设置页预热无关，且预热步数很小（约 21 个单元 / 每步 8 预算 ≈ 3 步）。`game-client` 构建通过。

下次复测判定：加载阶段应出现 `settings_adaptive_budget source=stable_text_ingame_esc` 记录；打开 ESC 时 `remaining` 应接近 0，`ingame_text_runtime_drain` 不应再出现 10 ms 以上。

> 注：本节方案（加载阶段预热 ESC 文本容器）在 2026-09-11 复核后被回滚——ESC 的文本 plan 与容器生命周期按设计只允许在菜单打开后收集/构建，加载阶段没有可用的菜单上下文。替代方案见 §15。

# 15. ESC 文本尖峰的最终方案：字形预热（2026-09-11）

## 15.1 约束与关键事实

- 约束：ESC 的文本 plan（`MENU_TEXT_PLAN_UNIT_INGAME_ESC` → `BuildIngameMenuTextPlan`）与文本容器生命周期只能在菜单打开后运行；加载阶段既收集不到也不应收集。
- 关键事实一：单帧 5–33 ms 的成本几乎全在**字形光栅化**（`text_frame_stats`：`glyph_rasterize_ms=28.583`、`glyph_new=160`、`glyph_upload_ms≈0.16`），布局与写顶点占比很小。
- 关键事实二：文本容器构建**不可跨帧续建**（`CUi::DoLabelStreamed` 需要重建时执行 `DeleteTextContainer` 后一次性重建），但**单个字形光栅化是幂等且可中断的**。
- 关键事实三：字形缓存 `CGlyphMap::m_Glyphs`（键为 Face/Chr/FontSize）是引擎级全局资源，与菜单生命周期无关。

## 15.2 方案

在 ESC 打开后、构建某个 plan item 的文本容器之前，先分帧预热该 item 用到的字形：

1. 用 `SMenuTextPlanItem::m_Text` 与 `m_FontSize` 生成去重的 `(字符, 字号)` 队列；字号换算必须与 `CTextRender::AppendTextContainerImpl` 保持一致（`ActualSize = round_truncate(FontSize × FakeToScreen.y)`），否则预热键与运行时不匹配。
2. 每帧最多预热 24 个字形（约 4 ms）；队列未清空时本帧不下发容器构建。
3. 队列清空后构建容器，此时字形全部命中缓存，容器构建只剩布局与写顶点。

实现：`ITextRender::QmPrewarmGlyph(int Chr, int FontSize)`（`textrender.h` / `text.cpp`，默认空实现）；`CMenus` 的 `m_vMenuTextGlyphPrewarmQueue` / `m_MenuTextGlyphPrewarmCursor` / `m_MenuTextGlyphPrewarmPlanCursor`（`menus.h`）；预热阶段插入 `PrebuildSettingsTextPoolForLoading` 的构建循环之前（`menus.cpp`）；plan 重建或 generation 变化时同步失效队列。加载阶段的容器预热（§13.3/§14.3）已回滚。

## 15.3 代价与验证

- 代价：每个 item 的文本显示推迟若干帧（160 字形 ÷ 24 ≈ 7 帧，约 0.2 s），换来单帧不再出现 20–30 ms 尖峰。
- 验证判据：`perf/menu|ingame_text_runtime_drain` 不再出现 10 ms 以上；`perf/text` 的 `text_frame_stats` 中 `glyph_rasterize_ms` 从单帧 28 ms 变为多帧小值（`glyph_new` 每帧 ≤24）。
- 该机制对任何首次文本（设置页、服务器浏览器、demo 页）通用。

# 16. ESC 文本尖峰的收敛方案：缺失字形记录 + 空闲帧预热 + 跨会话持久化（2026-09-12）

## 16.1 上一轮修复为何不够

- 上轮把字形预热挂在 `PrebuildSettingsTextPoolForLoading`（预建管线），但 ESC 打开的首帧渲染走的是 `RenderIngameMenu` 的实时路径。实测首帧 `ingame_esc_tab_content=25.0ms`、`ingame_esc_button_column=11.6ms`，同帧 `glyph_rasterize_ms=30.3`、`glyph_new=108`。
- 预建管线推进极慢（`settings_text_plan_collection units_done=1/21, budget=1`，每帧仅 1 个单元），远晚于首帧渲染，预热来不及生效。
- 用户明确否决"首帧只渲染骨架、容器分帧 drain"（不能允许空白）——容器必须同步构建、首帧即完整显示。

## 16.2 方案：把光栅化成本移出“打开那一帧”

约束条件下唯一出路是：打开时容器照旧同步构建，但所需字形已经在缓存里。字形缓存是引擎级全局资源，与菜单生命周期无关，可在菜单关闭的空闲帧预热。

1. `CGlyphMap::GetGlyph` 在缓存未命中（即将光栅化）时记录 `(Chr, FontSize)`，去重、上限 4096（`m_vQmRecentGlyphMisses`）。
2. 菜单关闭且非 demo 时（`CMenus::OnRender` 早退分支）每帧预热 8 个（`QmPrewarmRecentGlyphs(8)`，约 2ms），**游标前进、集合保留**（否则退出时保存为空集合）。
3. 跨会话持久化：退出时写入 `qmclient/glyph_prewarm.txt`（每行 `Chr Size`），`CMenus::OnInit` 时载入作为待预热集合。

预期效果：
- 本次会话第一次打开 ESC 仍有一次尖峰（此前从未渲染过这些字形，无从记录）；
- 之后每次打开 ESC 首帧应 ≤ 数 ms；
- 重启客户端后第一次打开也平滑（持久化列表在进服后的空闲帧预热完成，约 4096/8 = 512 帧）。

## 16.3 验证判据

- `perf/menu`：第二次打开 ESC 起 `menus_render_total` / `ingame_text_runtime_drain` 不应再出现 10ms 以上；
- `perf/text`：`text_runtime_budget` 的 `glyph_rasterize_ms` 从"打开时 30ms"变为"空闲帧 ~2ms 小值"，`glyph_new` 每帧约为 8；
- 配置目录出现 `qmclient/glyph_prewarm.txt`（正常退出后）。

### 16.4 复测结果（2026-09-12 02:06 会话，已验证）

- 打开 ESC（02:06:54）：`perf/text` 的 `text_runtime_budget` 为 `glyph_new=0`、`glyph_rasterize_ms=0.000`、`text_container_new=171`、`text_container_create_ms=0.057`——容器照旧同步构建（171 个，首帧完整显示），但不再有字形光栅化成本。
- 预热按设计工作：空闲帧 `text_frame_stats` 为 `glyph_new=8`（每帧 8 个，约 5–6 ms），完成后降为 `glyph_new=0`、`glyph_rasterize_ms=0.000`。
- 退出：`shutdown_step` 12 步全齐（`window_hidden` → … → `done`，约 2 秒），无 hang/fatal report。
- `qmclient/glyph_prewarm.txt` 从 18.5 KB 增长到 22.6 KB（持续累积）。
- 仍存在的一次性成本（均非字形光栅化，待查）：
  1. 进服瞬间 `text_frame_stats` 仍有 `glyph_new=178 / rasterize 19.3 ms`，`ingame_text_runtime_drain` 21–25 ms——本次会话新记录的字形会在下次会话被预热吸收；
  2. 打开 ESC 时 `ingame_esc_button_column=17.4 ms`（同刻 `glyph_rasterize_ms=0.000`），来源待查（可能是图标或布局等非文本成本）。

### 16.4.1 最终验证（2026-09-12 02:33 会话，plan 提前收集后）

- 打开 ESC：`ingame_esc_button_column` 17.4 → **6.3 ms**；`ingame_server_info_prepare` / `prebuild` / `ingame_text_runtime_drain` 全部低于记录阈值（此前 20–25 ms）；打开时 `frame_ms_avg=2.880`、`glyph_raster_ewma_ms=0.000`。
- plan 收集提前生效：`stable_text_ingame_esc` 的 `backlog` 从 549 → 171，且打开时帧均 2.9 ms，收集不再造成可感知成本。
- 退出：`shutdown_step` 12 步全齐，约 2 秒干净退出。
- 结论：ESC 打开尖峰（字形光栅化 + plan 同步收集）与退出挂起两个问题均闭环。剩余一次性成本（全新安装首次打开的少量新字形、进服瞬间的 UI 初始化字形）会随会话间预热列表累积进一步缩小，未再构成可感知卡顿。

## 16.5 仍可继续的优化（未做）

- 静态预热：从语言表提取字符 × UI 常用字号，在进服后立即预热，让"本次会话第一次打开"也接近无尖峰。
- 分辨率/UI 缩放变化会改变 `ActualSize` 键，旧记录不命中（无害，仅需重新累积）。

# 18. CPU/GPU 分层优化路线与“时机优化”的关系（2026-09-12）

## 18.1 实测数据对四预算域的标注

- 域 1（CPU 字形光栅化）：曾为唯一瓶颈（打开 ESC 30–44 ms / 108–210 字形）；字形缺失记录 + 空闲帧预热 + 跨会话持久化（§16）已将打开场景降为 0。剩余：全新安装首次打开、进服瞬间 UI 初始化字形（随会话间预热列表累积缩小）。
- 域 2（CPU 文本布局）：plan 同步收集曾是 20 ms 来源（单帧 1697 容器创建 / 12.7 ms + 布局），plan 提前到空闲帧后打开时已无感。
- 域 3（GPU 上传）：实测 `glyph_upload_ms≈0.16`、`text_container_upload_ms≈0.01`，当前规模不是瓶颈；预热摊开后每帧上传 ≤8 个字形。
- 域 4（GPU 帧时间）：与文本无关，由 §11 的帧归因体系观测。

## 18.2 与“时机优化”（§13–§17）的关系

§13–§17 的修复全部是**时机层**：把不可跨帧续建的成本（字形光栅化、plan 收集）挪到空闲帧，并用跨会话持久化消除“首次”。它不降低单次成本，也不需要线程/图集结构改动。
架构层方案（四预算域路线）解决的是“时机覆盖不到”的部分：

1. **FreeType 专用工作线程 + 主线程只发布完整字形**：可把空闲帧预热的 ~2 ms 与进服瞬间的初始化字形也移出主线程，是剩余方案中收益最高的；需要独立设计 FT_Face 生命周期与字形发布协议，不能直接把现有 FT_Face 调用塞进后台线程。
2. **字形上传合并批次**：实测上传占比 <1%，当前优先级低。
3. **CPU/GPU p95 动态预算**：现有 `BeginSettingsUiFrameScheduler` 已具雏形，可作为后续统一入口。
4. **多页图集**：仅在图集扩容（4096→8192 全量重传）时受益；4096 图集约容纳 1.6 万字形，正常使用难以触达，属极端情况。

优先级建议：先维持现状收集更多会话数据；若“全新安装首次打开/进服瞬间”仍有可感知卡顿，再排期 FreeType 线程化；上传合并与多页图集暂缓。不建议手动绑核或提高线程优先级（输入/音频/驱动线程饥饿风险）。

## 18.3 测试基线（2026-09-12）

- 本轮触碰的契约全部通过：qmclient_monitoring_text_runtime_contract、qmclient_monitoring_text_plan_contract、settings_warmup_loading_contract、qm_graphics_shutdown_contract。
- 全量 run_cxx_tests 剩余 2 个失败：`QmMonitoringLayoutContract.HudLayoutPlacesPanelLeftOfGraphColumn` / `HudLayoutUsesLargerPanelOn4kScreens`——为本轮开始前已存在的 monitoring 未提交改动所致，与本轮无关。
- 另修复：`qmclient/rank_demo_manifest.h` 中 json_value 指针误绑引用的编译错误（仅 testrunner 编译单元暴露）。
- 合同要点备忘：`CMenus::OnRender()` 内不得出现 `PrebuildSettingsMenuTextPool(` / `PrebuildIngameEscTextPoolBeforeOpen(`（预建与可见渲染分离）；空闲帧 plan 预建应放在 `CMenus::OnUpdate`。

# 17. 退出卡住：诊断与修复（2026-09-12）

- 现象：退出后窗口突然回到"之前的游戏画面"并冻结数秒才关闭。
- 证据：`dumps/QmClient_Crash` 的 hang report（`Client state: offline (0)`、`No heartbeat duration: 10.0 seconds`）；最新 perf 日志在退出瞬间（00:09:17）后完全静默约 10 秒——即退出清理挂起，最终由 `StartForcedExitWatchdog`（10 秒兜底）强杀。窗口停留在最后一次成功 present 的帧，是因为退出时 `RenderShutdownMessage()` 的 `Swap` 依赖渲染线程命令队列，GPU/驱动卡住时未执行。
- 修复：
  1. 新增 `IGraphics::HideWindow()` 链路（`IEngineGraphics` → `CGraphics_Threaded` → `IGraphicsBackend` → `SDL_HideWindow`），在 `CClient::Shutdown()` 中紧随看门狗启用后调用——不经渲染线程，即使后续清理挂起，用户看到的也是干净的退出；
  2. 清理各阶段插入 `dbg_msg("perf/client", "event=shutdown_step step=...")` 标记，下次复测可直接定位挂起步骤（重点怀疑 `render_shutdown_message` 与 `text_render`）。
