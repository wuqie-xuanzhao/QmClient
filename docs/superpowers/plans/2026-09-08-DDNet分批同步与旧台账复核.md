---
title: DDNet 分批同步与旧台账复核
date: 2026-09-08
status: active
local_base: 6d37ad1c112a3dd9b178be5966cb3b25fc786bb9
upstream_head: 59968143f78ce6bff2123e0e540e8763a8ff6f8e
---

# DDNet 分批同步与旧台账复核

用户授权在现有 dyl_dev 上继续同步适用上游修复，总 token 上限为 2,000,000。旧的同步计划不能当作事实来源：必须核对官方 diff、当前源码与本地实现证据。旧计划的测试通过记录仅证明当时状态。

## 范围与完成条件

- 审核新增 89 条提交记录（49 个普通提交和 40 个 merge wrapper），每条保留适配、覆盖、不适用或待批准原因，不能把 `git cherry +` 当成缺失结论。
- 重新审核旧台账的 234 条记录，并查明漏记的 `bde42affe17af97d84fa21152c51e4d6a3951153`；短 hash 先解析，merge wrapper 与实际变更分开。
- 旧的 covered、adapted、merged 标签均视为待复核的历史陈述，不能只抽查新提交或延期项后宣称历史整体正确。
- 适用修复以最小补丁落入当前项目，保留本地差异。协议、snapshot 布局、预测、输入时序和新增功能另形成具体方案并确认。
- 按批完成必要行为测试、只读审查和串行构建/gate；证据写入本计划与对应台账。缺运行时证据的项目不标记运行时通过。
- 主仓库提交与推送不作为本任务的默认操作。最终更新版本使用仓库脚本；不以版本号表示与官方完全等价。

## 审核次序

先处理确定的输入边界及稳定性问题，再处理 JSON、demo、生命周期、音频和依赖；兼容性和平台专项单列。每批同时核对相关旧记录，随后复核剩余旧台账。总目标不会缩减为第一批完成。

## 第一批：RCON 多行剪贴板

上游：`54fcf119a12fbd160cf1fc4af74e1cbfd756982b`。

确认当前 `CLineInput::ProcessInput` 仅限制首行，后续完整行直接送入回调。16 字节缓冲区的行为测试收到 4097 字节第二行，UTF-8 长行也未截断；普通多行、CRLF 和末尾换行正常。

最小修复与上游相同：统一清理控制字符，首行继续替换当前选择区，后续完整行先写入输入缓冲区，再以 `GetString()` 调用回调。保留最后一行不提交、空行跳过和 UTF-8 截断规则。

行为测试直接编译当前 `lineinput.cpp` 与字符串实现，通过内存 `IInput` 驱动实际 `ProcessInput`，不复制粘贴算法。未使用的图形、计时及表达式函数在测试中误调用会终止，不能替代 UI 验收。

当前证据：

- 旧实现临时回归程序：长第二行和 UTF-8 截断失败，三个正常场景通过。
- 最小修复后同一程序：五个场景全部通过。
- `python -m unittest qmclient_scripts.tests.test_lineinput_clipboard -v`：通过；九个行为场景，包括字符中间截断、四字节 UTF-8、选区替换、无回调路径。追加选区测试首次因测试未同步光标而触发断言，已按真实选择操作先移动光标；此后通过。
- `cmd /c .\qmclient_scripts\cmake-windows.cmd --build cmake-build-release --target game-client -j 14`：通过。
- 只读代理审查：实现正确；补充了其指出的 UTF-8 部分字符截断测试缺口。
- `python qmclient_scripts/gate/check_gate.py --mode default --base-ref 6d37ad1c112a3dd9b178be5966cb3b25fc786bb9`：C++ 全量 2871/2871、Rust 单测及 doc-tests 全部通过；汇总 11 PASS、1 FAIL、1 不适用，唯一失败是 docs/QmClient_docs 未初始化。
- 已执行 `git submodule update --init -- docs/QmClient_docs`，检出原 gitlink `bab7f31bb52044de1a350f2bd12fee7ff639a8b9`。随后直接重跑 gate 的 `checks.env.run`，2 PASS、0 FAIL。其余已通过检查不重复；不把首次 default 的退出码改写为成功。
- 测试入口需要 `src/rust-bridge` include，已在构建重新生成 protocol7.h 后补齐。日志见 `tmp/upstream-20260908/clipboard-tests.log`、`game-client-batch1.log`、`gate-batch1.log`。
- 未启动实际客户端执行 RCON 网络会话；本轮测试证明输入边界和回调行为，构建及全量测试证明编译/回归层。

## 版本状态待核对

开始时 `src/game/version.h` 为 QmClient `3.0.1`、DDNet `19.9`，`docs/info.json` 为 `2.74.26`。需确认 metadata 的含义与版本脚本维护范围，不把两者不一致直接改成一致，也不回退到旧计划里的版本。

## 旧台账首批复核

[旧台账复核表](2026-09-08-DDNet旧台账复核.tsv) 已规范化为 235 个完整 hash，保存历史标签与本轮结论两个独立字段。当前完成源码逐 hunk 复核四项：`6f764e4f11`、`98f0064af0`、`c9a9931f8a`、`33fbdc853c`。均确认当前源码覆盖官方修复，不宣称图形运行时验收。其余记录仍为 unverified。

新增 `cbb82fff9` 也已核对：六个 shader 指针初始化为空、DeleteProgram 受 shader 支持条件保护。QmClient 条件外 delete 空指针安全，与上游避免 OpenGL2 退出崩溃的意图等价，保留当前实现。

## 第二批：JSON 输入验证和转义

上游 `51447775d`、`7b7360521`、`ff55fe211`。

先扩充 `src/test/json_test.cpp`，涵盖转义容量边界、数组/对象内非法 UTF-8，两个解析入口及错误文本，十万层深度的有效/无效 JSON。深层测试由 gtest 子进程隔离旧实现栈溢出。

红灯：`testrunner --gtest_filter=Json.*:JsonDeathTest.*` 四组全部失败：两个深层栈溢出、对象值验证漏检、七字节容量把转义截为 `\u000`。随后应用官方最小修复：迭代检查节点、对象遍历真实 value、格式化容量包含已预留终止符。第三方 json-parser 升级仍单列，未混入此批。

绿灯：四组全部通过。审查指出迭代容器分配异常会绕过原有裸指针释放，已在两个解析入口用带 json_value_free deleter 的 unique_ptr 保持树所有权，成功时 release；保持项目 STL 分配异常传播约定，不增加全局 OOM 恢复策略。只读审查增量复核确认所有权问题已解决，无新增确定缺陷；撤回必须捕获OOM的额外要求。

最终验证：

- `cmd /c .\qmclient_scripts\cmake-windows.cmd --build cmake-build-release --target run_cxx_tests -j 14`：2874/2874 通过（包括两个深层子进程用例）。
- `cmd /c .\qmclient_scripts\cmake-windows.cmd --build cmake-build-release --target game-client -j 14`：最终代码构建通过。
- `python qmclient_scripts/gate/check_gate.py --mode quick --base-ref 6d37ad1c112a3dd9b178be5966cb3b25fc786bb9`：10 PASS、0 FAIL、0 WARN、1 不适用。
- clang-format 检查通过；Rust 源码未改，复用第一批全量证据。
- 日志在 `tmp/upstream-20260908/json-*`、`cxx-batch2.log`、`game-client-batch2-final.log`、`gate-batch2-final.log`。

旧台账缺项 `bde42affe` 与 wrapper `ba7a652d4` 的注释本身已核对，但不能只因本地仍用 ui.cpp 就判定结构同步不适用。用户追问后进一步定位前置 `f1dfd5187b93e264821300e4c230dc1e219c4808`（2026-07-11，已在官方20.0内）：633行popup实现逐字搬到 ui_popups.cpp，增加include与CMake源登记，函数体无差异。

两条注释记录改为 `deferred-structure-prerequisite`。独立审查前置拆分与本地popup定制、CMake、源码路径测试的交叉影响；不要为了短期保持路径而永久跳过适用的结构优化，也不为同步注释直接覆盖本地实现。旧台账当前四条已确认源码覆盖、两条已查清但依赖结构候选，其余229条仍未复核。

这个前置提交位于20.0之前，说明只审核20.0之后两个范围无法证明整个20.0基线完整；后续遇到依赖要追溯范围外的前置，并单列记录。

## 用户追加的结构优化范围

用户已授权对比本项目与官方20.0及之后的代码结构并实施适用优化；结构扫描与中间方案已归档，见 [结构对比与优化归档](archive/2026-09-08-DDNet代码结构对比与优化.md)。该工作后置，不阻塞当前官方修复批次与台账复核。

## CMake确认期间的独立复核

逐处检查新增0613b6704和b4eab43a1，确认投票回收链first指针与地图声音两处错误索引初始化已在当前源码覆盖，无需重复修改。新增bdd2253cb尚未覆盖：UpdateDevice缺少零输出设备保护；已有14ef8c4c7音频适配不能代表此后新增的无设备重开保护已完成。该项登记confirmed-gap，运行时和补丁验证待继续。

## 第三批：设备、JNI、文件名与测试长度修复

已适配 `bdd2253cb` 的无音频输出设备保护，零设备时保留设备变更标志；`097eb98f5` 在释放 JNI 字符串前复制工作目录，并释放循环内局部引用；`e32c05342` 清理玩家名中的非法文件名字符，保留本地 GetMapName 路径逻辑；`17e3c2e83` 将 teehistorian 测试的 JSON 输入长度改为缓冲区实际剩余长度。最后一项仅部分适配，json-parser 升级及 sanitizer suppression 清理仍待审计。

2026-09-08 验证：

- `cmd /c .\qmclient_scripts\cmake-windows.cmd --build cmake-build-release --target game-client game-server -j 14`：通过，日志 `tmp/upstream-20260908/stability-build.log`。
- `cmd /c .\qmclient_scripts\cmake-windows.cmd --build cmake-build-release --target run_cxx_tests -j 14`：2874/2874 通过，日志 `tmp/upstream-20260908/stability-cxx.log`。
- `python qmclient_scripts/gate/check_gate.py --mode quick --base-ref 6d37ad1c112a3dd9b178be5966cb3b25fc786bb9`：10 PASS、0 FAIL、0 WARN、1 不适用，日志 `tmp/upstream-20260908/stability-gate.log`。
- 已自查音频重开条件及 JNI 参数所有权；本批尚未独立审查，未完成音频设备拔插运行验证与 Android 构建/实机验证。Windows 构建不覆盖 Android 条件编译代码。

CMake 恢复后的复查发现另有 Apple 图形后端增量（含 `cmake/QmAppleGraphics.cmake` 登记）。这些不属于本批恢复或 popup 拆分，已保留。因此“恢复后仅有 popup 一行差异”只描述当时检查点，不代表当前全部工作树差异。异常覆盖的实际写入来源仍未确定。

第三批独立只读审查已完成：未发现确定缺陷。审查核对设备变更标志保留、JNI 字符串及 argv 生命周期、GetMapName 返回文件名、玩家名副本清理和 teehistorian UUID 头部长度。仍缺 Android 构建/运行、音频设备拔插恢复、特殊玩家名 demo 实际落盘验证；UBSan parser 抑制尚未移除。该结果更新上一段“尚未独立审查”的检查点状态。

## 旧台账继续复核：首个错误覆盖结论

源码与完整官方 diff 逐项核对新增六项：

- `b68c684a2`：Load 在 step 前 reset SQLite 预备语句，已覆盖。
- `b0e1a030f`：自动 demo 录制入口限制 STATE_ONLINE，已覆盖。
- `c965b34c4`：队伍开始时间采用 bitset，完整 set/reset/none/test 已覆盖，本地容量为 129。
- `dc7549396`：conn_timeout 随测试乘数增长并封顶，传给两端；保留本地 Valgrind 乘数20（官方25），记录适配差异。
- `10a899f57`：Clang 不支持参数警告用于 C/C++ 参数探测，已覆盖，未运行 Clang 平台配置。
- `548fac1d0`：旧 covered-equivalent 结论不成立。当前 CMake 缺少该提交新增的 stack-clash/cf/branch 保护、RELRO、FORTIFY3 和 PIE 探测及目标应用，仅有此前 stack-protector-strong 与 FORTIFY2。改为 confirmed-gap，仍待适配和平台验证。

上述结论均为源码复核，不将全量单测泛化为这些运行场景已验证。同步在旧台账保留历史结论与本轮结论，便于追溯。

`bde42affe` 及其 wrapper `ba7a652d4` 的结构前置已完成；ui.h 注释现以中文指向真实 ui_popups.cpp，更新为 adapted-worktree。注释人工核对，不重复构建。

随后核对五条 merge wrapper 的父链及相对第一父提交的 stable patch-id，分别与上述 constituent 相同：`9b0aaeb33` 继承编译加固缺口，`4d766c2b7`、`596c89b70`、`10e5321d9`、`e3f2c21d8` 继承已核对的代码结论。未按标题直接推定合并等价。

当前235条统计：218 unverified，8 verified-code-equivalent，1 verified-adapted-code，4 verified-wrapper，2 adapted-worktree，2 confirmed-gap（同一加固缺口的普通提交与合并提交）。89条新增表和235条旧表均已校验行数、hash唯一性及每行9列；整体复核仍未完成。
