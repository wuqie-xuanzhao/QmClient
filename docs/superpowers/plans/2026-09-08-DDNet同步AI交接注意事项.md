---
title: DDNet 同步 AI 交接注意事项
date: 2026-09-08
status: active
---

# 接手先读

用户要求现在仅整理交接，由其他 AI 继续。整体同步和结构优化未完成；不要因已有测试通过而宣布完成。旧计划必须对照当前源码校对，不得把历史 covered/merged 标签当成证明。

## 目标与授权

- 继续现有 QmClient 主项目，已放弃干净 DDNet 20.0 重构。不要恢复该重构路线。
- 审计官方新增提交、历史合并/延期项，并对比本地与官方20.0及之后的结构，实施适用优化，保留 QmClient 定制行为。
- 子模块分支合入 master 并推送已完成。主仓库没有获准自动提交、合并或推送。
- 协议、预测、输入时序、地图/玩法及回放语义的改动需提出具体方案后确认。不要把整包上游提交默认为已获授权。
- 原总预算上限2,000,000 tokens，已接近耗尽，不能认为全部工作已完成。接手方按自己的实际预算组织工作，不重复读取整段历史或整库 diff。

## 已核实的基线

交接时再次读取：

- 分支 `dyl_dev`，HEAD `6d37ad1c112a3dd9b178be5966cb3b25fc786bb9`。
- 官方20.0：`a5a61806e434ef22141b622db989087fbba8ed21`。
- 固定审计官方头：`59968143f78ce6bff2123e0e540e8763a8ff6f8e`，此前头 `1d507dad90812cd0873b151aac60ad7bff5127b6`。
- `ddnet-upstream` 是 DDNet 官方，`upstream` 是 wxj881027/QmClient，`origin` 是用户 fork。不要混淆。
- `ddnet-libs` 为 `6914a42567a8002a19199369dff93ff96d2185f9`，已推送子模块 origin/master 并曾回读确认；父仓库 gitlink 未提交。
- `docs/QmClient_docs` 已按原 gitlink 初始化，解决过 default gate 环境缺失。

这些是固定审计边界，不代表接手时官方最新。如刷新远程，单列新增区间，保留现有台账。

## 工作树和异常恢复

工作树有多任务并行改动。先读实际 `git status` 和当前规则；不能一键清理、reset、checkout覆盖或整文件套用旧分支。

曾发现 CMakeLists.txt 被替换为已放弃重构分支的内容。用户明确批准恢复后，恢复成当前 dyl_dev HEAD，再添加 popup 源登记。无法确定异常写入来源，不能断言是合并导致或归责某个任务。

备份在 `tmp/upstream-20260908/CMakeLists.unattributed-5b4e830a6796.txt`，SHA256：
`5b4e830a679629ae4de303dac10f1b2cb68cf5c028484a14ae90781474298e46`。

其后有并行 Apple 图形后端改动，当前 CMake 除 popup 一行外还有平台设置和 `cmake/QmAppleGraphics.cmake` 引入。保留这些变化。规则/skills、README、CI、图形配置、图标生成物等也有其他任务改动；不能按本交接清单之外的文件一概回退。

`src/game/version.h` 当前由并行工作改为3.0.2。本同步任务未执行版本递增，不要盲目再加一次；后续交付需核对版本归属。

## 已完成但尚未提交的补丁

1. RCON多行剪贴板：`lineinput.cpp`，官方 `54fcf119a`。每行清理后从实际 lineinput buffer 回调，避免长行绕过容量限制。真实 ProcessInput 行为测试9场景通过；测试在 `qmclient_scripts/tests/test_lineinput_clipboard.py` 和 fixtures。
2. JSON：`51447775d`、`7b7360521`、`ff55fe211`。修正转义终止符容量、对象value UTF-8遍历，改迭代避免深层栈溢出。解析树由带 json_value_free 的 unique_ptr持有，成功才release。新增边界/十万层深度测试，先失败后通过。第三方 json-parser 升级尚未完成。
3. Popup结构：本地 ui.cpp 末尾885行函数体逐字搬到新 ui_popups.cpp，保留Qm定制，登记CMake并更新11处相关源码读取测试及绘制扫描文件列表。QmUiPerf.h是必要直接依赖。ui.h新增中文注释指向实际实现。官方前置为 `f1dfd5187`，此前漏记的 `bde42affe`及wrapper已改为已适配。
4. 第三批：`bdd2253cb`零音频输出设备保护；`097eb98f5` Android JNI工作目录字符串生命周期和局部引用；`e32c05342`服务端demo玩家文件名清理（保留GetMapName）；`17e3c2e83` teehistorian测试传真实JSON长度。最后一项是部分适配，仍保留 parser UBSan suppression。

以上批次已独立只读审查，最终未发现确定缺陷；不代表运行场景全覆盖。

## 验证证据与限制

日志统一在 `tmp/upstream-20260908/`：

- `stability-build.log`：Windows game-client和game-server构建通过。
- `stability-cxx.log`：C++2874/2874通过；交接时重读末尾确认。
- `stability-gate.log`：quick 10 PASS、0 FAIL、0 WARN、1不适用；交接时重读确认。
- `popup-build-final.log`、`popup-cxx-reviewed.log`、`popup-gate.log`：popup最终构建、全量及quick通过。
- JSON日志 `json-*`、`cxx-batch2.log`、`game-client-batch2-final.log`、`gate-batch2-final.log`。
- 第一批default曾因未初始化文档子模块失败；C++和Rust部分通过。初始化后只复核环境项通过。不能说原default整次exit0。

未做：Android构建/实机、音频设备拔插、特殊玩家名demo实际落盘、popup实际UI操作、完整跨平台验证。不要把Windows编译或源码测试描述成这些场景通过。

后续ui.h只改注释、台账和交接文档进行了人工检查，没有为纯注释重跑构建。其他任务继续修改代码时，历史成功日志不能自动覆盖其新状态。

Windows入口：`cmd /c .\qmclient_scripts\cmake-windows.cmd --build cmake-build-release --target <目标> -j 14`。同一build目录内构建/测试串行。wrapper可能缓冲输出，观察超时不代表进程结束，按session继续轮询。

## 尚未完成的关键工作

旧台账235条：218 unverified，8 verified-code-equivalent，1 verified-adapted-code，4 verified-wrapper，2 adapted-worktree，2 confirmed-gap。两条gap是同一个编译加固提交及其merge，不是两个独立缺陷。

已推翻旧 `548fac1d0`“编译加固已覆盖”：当前缺stack-clash/cf/branch保护、RELRO、FORTIFY3与PIE探测/目标应用。需结合平台条件补齐并验证；不要覆盖并行Apple配置。Clang不支持参数探测 `10a899f57`则确实已有。

新增89条（49非merge，40wrapper）也未全部审计。Git cherry差异不能证明功能缺失；已有若干本地等价实现。台账可能存在早期“构建中”的旧证据文字，完成范围以明确批次证据核对，逐项更新，不能批量标完成。

优先阅读[兼容性批次待确认方案](2026-09-08-DDNet兼容性批次待确认方案.md)：

- 快照 `be3e5e6a3`：本地为Rust delta API，容量不足返回-1，但C++调用方用if(DeltaSize)继续；demo Write未拒绝负长度，可到mem_copy。已确认源码失败路径，尚未用合法大快照复现。须连同容量、错误处理、发送上限及demo基准更新适配，不能照抄官方C++接口。
- 输入 `178da1ead`：本地OnUpdate仍在输入采样后的旧位置；前移涉及Qm预测余量与主副角色/UI时序。
- 地图 `a34ebba73`：缺SHA256比较，影响同名同大小不同内容地图的历史榴弹兼容标志。

另待审计：MySQL无CA TLS `78d8f82c5`、demo无关键帧/marker范围、iOS安全区接口链、shader临时数组改vector、json-parser升级、wavpack父项目集成等。详情以新增表为准。

结构初筛490条路径差异（37官方独有、445本地独有、7官方删除、1其他）仅为路径统计。Settings拆分需处理本地共享静态状态、资源缓存、预热；本地已有controls/key_binder/assets模块。base拆分及settings优化还未完成，不能因popup已拆就称结构对齐。

## 文档入口和接手顺序

1. 读当前根AGENTS.md及最小相关skill，规则曾被另一个任务精简，勿恢复旧规则。
2. 核对工作树及并行任务边界，读取[执行计划](2026-09-08-DDNet分批同步与旧台账复核.md)、[结构计划](2026-09-08-DDNet代码结构对比与优化.md)。
3. 用[旧台账](2026-09-08-DDNet旧台账复核.tsv)和[新增台账](2026-09-08-DDNet新增提交台账.tsv)继续逐项证据核对；保持历史标签和新结论分列。merge需核对父链及补丁，不能只看标题。
4. 优先收敛快照失败路径的复现和待批准方案，再按依赖推进适用修复及结构优化；未经批准不改兼容性行为。
5. 验证后更新对应证据与缺口，最后才汇总版本和提交边界；主仓库推送仍需用户授权。

本交接不宣告目标完成，也不将剩余工作缩减为以上举例。用户原目标、完整台账与明确授权共同决定范围。
