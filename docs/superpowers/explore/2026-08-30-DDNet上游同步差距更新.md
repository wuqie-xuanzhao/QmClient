---
title: DDNet 20.0 上游同步差距更新
date: 2026-09-01
status: active
supersedes: ../reports/2026-08-30-DDNet20上游同步差距报告.md
upstream_base: a5a61806e434ef22141b622db989087fbba8ed21
upstream_head: 1f9b189615495defa3114443563863088f8a5f1e
local_head: 5a6ecfae1a5ed41b89233b218d1785161d813710
---

# DDNet 20.0 上游同步差距更新

> 本文是当前活动校对报告。旧版 `docs/REPORT.md` 和归档报告只保留历史背景，不作为当前合并依据。

## 1. 结论先行

- 官方 `20.0` tag 为 `a5a61806e434`；官方 `master` 当前为 `1f9b18961549`，`20.0..master` 为 **243 个提交**。
- QmClient 当前 `HEAD` 为 `5a6ecfae1`，仍使用 `GAME_RELEASE_VERSION_INTERNAL 19.9`。因此不能称为“官方 20.0 完整快照”，但已经吸收多批 20.0 之后的实际代码。
- 本轮最新的 5 个真实行为提交已经完成本地适配并提交：无设备音频、投票回收、地图声音错误状态、音频断言和 WavPack UBSan 抑制，见 `14ef8c4c7`。
- 128 人支持主体已经在更早的本地历史中大规模完成。本报告不把它重新列为待实现功能；后续只验证混合会话、SeeOthers 和 timeout reconnect。
- “剩余工作”必须分三类：
  1. 已合并代码但缺设备、服务端夹具或人工 smoke 证据；
  2. 需要独立平台/策略决定的延期批次；
  3. 只有在运行时验证暴露真实缺口时才新增的补丁。

## 2. 校对口径

### 2.1 权威来源

当前状态以以下对象和源码为准：

```sh
git fetch --no-tags ddnet-official master
git show -s --format='%H %ci %s' ddnet-official/master
git rev-list --count 20.0..ddnet-official/master
git log --oneline 20.0..dyl_dev
```

本次结果：

```text
upstream: 1f9b189615495defa3114443563863088f8a5f1e
date:     2026-09-01 12:36:13 +0000
delta:    243 commits after 20.0
local:    5a6ecfae1
```

旧台账记录的 `1d507dad9081 / 235` 已过时，不能继续用于统计当前差距。`git cherry` 的 `+` 只说明官方 hash 不是当前分支的 patch 等价祖先，不等于行为未合入；QmClient 允许本地重构和适配，必须检查源码意图、依赖、边界和验证证据。

### 2.2 版本身份

`src/game/version.h` 当前仍为：

```cpp
#define GAME_RELEASE_VERSION_INTERNAL 19.9
```

20.0 tag 已存在，但 QmClient 版本升级属于独立 release 决策。本同步报告不擅自修改版本号。

## 3. 已实际合并的代码

下表只列有本地提交或可核验的现有等价实现；不把 merge wrapper、注释提交或未提交工作树改动统计为合并。

| 上游意图 | 上游提交 | QmClient 本地证据 | 状态 |
|---|---|---|---|
| 128 人主体、旧客户端 mapping、timeout/FakeSnap、server-info 和 scoreboard | `dae667a56` 及 `01bbf415e`、`2185493e2`、`54ec87f53`、`38e005621` 等 | 当前服务端/客户端映射、`MAX_CLIENTS` row-plan、旧协议分支 | 已有，禁止重复实现 |
| 地图加载和 IMap 边界 | `860f6aa2b`、`6258cceaa` | `f6d96033c`、`6426052f1` 等本地路径 | 等价覆盖 |
| 地图转换外置 PNG 生命周期 | `bc2c2f930` | `889486023` | 已合并 |
| 快照 ID 池耗尽保护 | `91eb6b94a` | `6c659543b` | 已合并 |
| tuning 溢出/NaN 转换 | `a38ba7bd4` | `58e74ecde` | 已合并 |
| RCON victim 参数校验 | `821d5ae4b` | `ece1c1b4d` | 已合并 |
| 服务端重发、预连接和无连接资源预算 | `6c0126f99`、`4318df0fc`、`8b5d3b7a8`、`eb96fe096`、`c4ba80c69` | `070f529e2`，另有 `6e98b45cb` 回归测试 | 已合并 |
| server-browser SQLite statement 复位 | `b68c684a2` | `53b8dade6` | 已合并 |
| editor brush picker 崩溃 | `e58155fb8` | `a86b5e2cd` | 已合并 |
| envelope 缩放热键模态输入保护 | `c2cea3f30` | `eea5c224e` | 已合并 |
| 本地地址识别和 localhost fallback | `9931d2dc9` | `561740714` | 已合并 |
| HTTP/job 关闭顺序和析构取消竞态 | `ceb86ada9` | `e8c873823` | 已合并 |
| Econ 封禁断开、皮肤度量重置 | `045cce756`、`03407db9e` | `fb5e04c01` | 已合并 |
| 激光门预测长度和碰撞重建 | `ac566d06f`、`9d8a20bd0` | `3f6d2b1e4` | 已合并 |
| GameInfoEx 队伍规模 schema | `a9c6d2ef`、`2612f425f` | `c181dfbd1`、`c931bf276`、`a560ea5a4` | 已合并 |
| 5:4 viewport、OpenGL/Vulkan 坐标和触控映射 | `2805226e3`、`391b70c39` | `9d28e8afa` | 已合并 |
| iOS SDL/OpenGL ES app bundle、依赖和平台入口 | `936f2b7c1` | `1eafa9ed3`、`e5325e77b` | 已合并，运行时待验收 |
| iOS 提示窗口触摸关闭 | `c5ad69216` | `311ab3555` | 已合并，运行时待验收 |
| graphics color setter 重构 | `b3bc9f18c` | `d699c86a1` | 已合并 |
| 地图加载进度显示 | `e8c2df1d5` | `14986766e` | 已合并 |
| 无设备音频继续运行、设备恢复、voice 推进 | `1a16a876b` | `14ef8c4c7` | 已合并 |
| 投票 recycle 首节点修复 | `0613b6704` | `14ef8c4c7` | 已合并 |
| 地图声音 sample index 错误状态 | `b4eab43a1` | `14ef8c4c7` | 已合并 |
| 音频参数/样本 ID 断言 | `ab5109826` | `14ef8c4c7` | 已合并 |
| WavPack UBSan shift 抑制 | `b871d3432` | `14ef8c4c7` | 已合并 |

`14ef8c4c7` 没有机械覆盖上游 `sound.cpp`：它保留了 QmClient 已有的 `CalculateSampleDataSize`、音频解码输入校验、`RateConvert` 返回值检查和 Opus/WavPack 边界处理，再加入设备重连和无设备期间的播放状态推进。这是安全的本地适配，而不是逐字 cherry-pick。

## 4. 对官方 20.0 发布日志的核对

官方发布日志已按官方 commit 与本地源码逐项核对。下表将同一功能链合并展示，`merged-local` 表示已有本地行为实现，`runtime-gap` 只表示缺运行时证据，不表示缺代码。

| 官方发布项 | 官方 commit | QmClient 本地证据 | 状态 / 验证 |
|---|---|---|---|
| 128 player support、mapping、timeout/FakeSnap | `1045298dc` + `2185493e2`/`54ec87f53`/`38e005621` | `dae667a56` 及后续兼容修复 | `merged-local`；仅混合 0.6/0.7/128 运行时验收 |
| Web version、Fetch、IndexedDB | `92360e9ac` + `40e2a7acd` | 仅 `minimal.html`/curl，无 Fetch backend | `deferred`，独立 Web 批次 |
| Editor tabs、per-map state、envelope/editor actions | `21fd5b23f`、`6a627fc3b` 等 | `c46feb6e9`、`e29d29ce8`、`8de34fd77`、`efd164c18`、`7803d6aaf`、`c519155c9` | `merged-local`；editor UI smoke |
| Android aim-relative joystick/direct input | `525479b89` | `7fd6c5eec`，touch controls | `merged-local`；Android 手势 smoke |
| Off-screen culling、dynamic antiping、invisible assets | `37f05bbbc`、`5da2ed06f`、`b78d63e2d` | `eb42293f2`、`45db5bab3`、`f04728e73` | `merged-local`；游戏画面/联网 smoke |
| Scoreboard/text cache、skin tooltip、country filter | `18944ee81`、`9f4a61698`、`85a23eeee`、`648d2e6c7` | `7049d5be1`、`ef974573`、`965bbe8e0` | `merged-local`；UI smoke |
| Hook/telehook/projectile clipping | `fabf09a3a`、`7428f421b`、`eedb3151f`、`21bbcb3a0` | `4c0d1c491`、`eb42293f2` 及本地 hook 修复 | `merged-local`；地图视觉 smoke |
| W bind、Show directory、settings cleanup、icon thread safety | `055a53b69`、`43f5450bc`、`f04c7cb4a`、`888d17568` | `c519155c9`、`bd1a55607`、`809cc872e` | `merged-local`；相关 UI smoke |
| Demo validation、server-info const、map validation/datafile/dummy_map | `0aba685e0`、`47b2296b8`、`52e405122`、`517351151`、`1648a7deb` | `bd1a55607`、`6426052f1`、`c519155c9` | `merged-local`；editor/demo smoke |
| Snapshot alignment、oversized packet、tune-zone validation | `32225d80f`、`d7a5d09c2`、`773bd00e9` | `4c0d1c491` | `merged-local`；`snapshot_test`/`network_test` |
| MySQL SSL、rescue/unfreeze/cancelswap | `4237696c2`、`2ffbc26ef`、`28f2eb8fa`、`3d681ff52` | `5e022ee92`、`7da332dd1`、`1ba1f1af3` | `merged-local`；TLS/真实 server 行为仍缺集成夹具 |
| Snapshot purge、revision hash、Chain assert | `9c4745a60`、`a0ecefa60`、`e53ea2df0` | `0b70ca83c`、`b5355780f`、`e917c4e9c` | `merged-local`；自动合同/回归已覆盖 |
| Settings/popup split、macOS packaging/codesign | `f8fabede8`、`86a91f121`、`fe5c209d4`、`ad4d3612a` | `bd1a55607`、`5a6ecfae1` 及现有 packaging | `merged-local`；本轮 macOS Release 已构建 |

结论是：官方发布日志的代码项已完成逐项对照；真正未合并的只有 Web Fetch/IndexedDB 独立批次。其余差异主要是 runtime、设备、真实服务端或视觉证据，不应再当作待开发功能重复合并。

## 5. 当前仍缺的内容

### 5.1 已有代码，缺运行时证据

这些项目不是等待重新实现的功能：

1. **iOS P3C**：iOS arm64 simulator Release 已完成 Metal 默认 configure、C++ 编译与 shader/metallib 构建；本机没有可用 simulator runtime，因此 asset catalog、安装/启动、触控、联网、后台恢复、截图、Files export、退出和真机 smoke 仍无证据。
2. **0.6/0.7/128 混合会话**：需要旧客户端、新旧 server、SeeOthers 和 timeout reconnect 的真实夹具；不能用单元测试或 128 人源码主体替代。
3. **网络资源预算**：需要 Linux Valgrind integration 和高并发压测；当前 C++ 回归只证明边界和合同。
4. **Econ/HTTP 生命周期**：需要可控 fake HTTP/job 或 TCP reconnect 夹具，验证退出并发和重连时序。
5. **地图/编辑器/音频视觉 smoke**：需要真实地图、音频设备和 editor UI 场景；构建不能代替人工行为验证。

### 5.2 明确延期的独立代码/策略批次

| 项目 | 延期原因 |
|---|---|
| Emscripten HTTP 系列 | 当前没有对应 backend；需连同 Web 构建链、线程取消和 HTTPS 策略成组实现。 |
| Vulkan HDR/headless 等能力扩展 | 不是当前 20.0 桌面同步必要条件，需独立能力矩阵和视觉验证。 |
| DSCP EF/TOS 策略 | 当前 QmClient 仍有跨平台 LOWDELAY/qWAVE 语义；这是 QoS 策略变化，不应无确认替换。 |
| `NET_MAX_PAYLOAD` 重命名 | 涉及协议命名和兼容边界，需原子批次处理。 |
| 翻译刷新 | `translations/i18n/*.toml` 是维护源，生成语言文件不能直接作为 cherry-pick 产物。 |
| Node.js/CI action、20.1 版本策略 | 属于 CI/release 决策，不是当前客户端代码缺口。 |

## 6. 验证证据

本地代码批次的当前验证：

```text
cmake --build cmake-build-release --target game-client -j 8
Result: PASS

cmake --build cmake-build-release --target run_cxx_tests -j 8
Result: PASS, 2935/2935

cmake --build cmake-build-release --target run_rust_tests -j 14
Result: PASS
  qm_update: 15 tests
  Rust doctests: all passed

python3 qmclient_scripts/gate/check_gate.py --mode default
Result: PASS, 13 pass / 0 fail
```

构建中出现的 macOS 11.0 与 Homebrew macOS 26.0 deployment warning、Rust doctest jobserver warning 和 iOS 编译器 warning 均为环境/既有源码警告，未阻断构建或测试。

本报告不把上述验证扩展为未执行的运行时结论：iOS simulator/device 的安装启动与视觉行为、混合协议、Linux Valgrind、高并发、Econ TCP 和真实音频设备仍未验证。

## 7. iOS Metal 结论

### 7.1 当前事实

- iOS 平台本身支持 Metal，Apple 的承载方式是 `CAMetalLayer` 或基于它的 Metal view。
- QmClient 现在已将 macOS/iOS 普通 `Release` 的默认 `METAL` 设为开启，GLES 仍保留为显式回退；iOS 脚本不再强制 `-DMETAL=OFF`。
- `src/engine/client/backend/metal/backend_metal.mm` 已纳入 iOS target，并对 `displaySyncEnabled`、`MTLDevice.lowPower`、`didModifyRange` 等 macOS-only API 做了条件保护；shader 通过 `iphoneos`/`iphonesimulator` SDK 和 deployment target 构建。
- iOS Metal 的真实 layer 生命周期、后台恢复、旋转、触控和 device/simulator 运行时仍需设备或 simulator runtime 验收，不能由 macOS 测试替代。

### 7.2 需要独立处理的移植面

1. UIKit/`CAMetalLayer` 的 view 生命周期、旋转和 drawable 尺寸变化。
2. drawable 获取、present、后台/前台恢复、窗口 resize 和设备丢失。
3. shader/metallib 编译、bundle 资源复制和 iOS framework 链接。
4. render target、MSAA、readback、截图和纹理格式能力协商。
5. iOS device 与 simulator 的 feature-set 差异，以及 arm64 真机和模拟器验证。
6. 与现有 SDL/OpenGL ES 后端并存时的 backend identity、设置项、构建开关和回退策略。

### 7.3 建议结论

后续按 `docs/superpowers/plans/2026-09-01-QmClient-iOS-Metal默认Release适配计划.md` 执行：先完成 Apple 默认 Release 的构建接线和 iOS shader 产物，再按 backend 生命周期、资源能力、绘制/readback、simulator/device 验收分阶段推进。不得把 macOS Metal 已经能编译或能运行当作 iOS Metal 已完成；在真机证据齐全前，GLES 回退必须保留。

官方参考：

- Apple `CAMetalLayer`：https://developer.apple.com/documentation/quartzcore/cametallayer
- Apple Metal：https://developer.apple.com/documentation/metal
- SDL Metal backend：https://github.com/libsdl-org/SDL/blob/main/src/gpu/metal/SDL_gpu_metal.m

## 8. 最终合并建议

1. 当前代码合并批次以 `5a6ecfae1` 收口，不再重复实现 128 人主体。
2. iOS 代码主线按 Metal 默认 Release 计划推进；有 simulator runtime 后优先完成 Metal bundle/启动合同，再做混合 0.6/0.7/128 验收；只对实际失败行为新增补丁。
3. 再做 Linux Valgrind、高并发、Econ/HTTP fake lifecycle 和地图/editor/audio smoke。
4. Emscripten、Vulkan 能力扩展、DSCP、翻译、CI 和版本号按独立决策批次处理。
5. 只有完成适用运行时验收并形成 release plan 后，才考虑把 `GAME_RELEASE_VERSION_INTERNAL` 升到 20.0。

## 9. 工作树边界

本轮已将 Apple/Metal 接线、测试合同和对照报告提交为 `c0b6aa462`，当前工作树干净。后续任何构建仍必须先检查后台的 CMake/Ninja/Xcode/Cargo 进程，并在同一 build 目录串行执行。
