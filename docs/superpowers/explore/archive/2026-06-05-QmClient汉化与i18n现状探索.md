---
type: question
date: 2026-06-05
status: superseded
confidence: high
related:
  - docs/dyl/QmClient_docs/01-功能模块/汉化完善功能报告.md
---

> 文档已过时：2026-06-10 后当前实现已收敛为“源码英文 key + DDNet base 语言包 + QmClient overlay”模型，不再采用本文记录的“中文 key + english 反向映射”混合状态。当前状态以 `docs/superpowers/explore/2026-06-10-QmClient-i18n-overlay收敛记录.md` 为准。

## 速答

当前仓库的 QmClient 本地化口径，已经不是旧文档里那套“代码里统一保留英文 key，再由 `qmclient/languages/simplified_chinese.txt` 提供中文翻译”。运行时实际会把 DDNet 主语言文件和 `data/qmclient/languages/<lang>.txt` 叠加加载，所以现在仓库里已经存在一套“中文 key + `english.txt` 反向英译”的第二层语言系统。`menus_qmclient.cpp` 也已经大量按这个方向落地，当前文件里约有 480 处 `Localize("中文")`，而仍保留约 93 处 `Localize("英文")`，说明它本身就是混合态。

同时，`qmclient_scripts/languages_qmclient/` 下的旧脚本链路没有跟上这个运行时现实：`twlang_qmclient.py` 仍然只扫描 `TCLocalize/TCLocalizable`，不会提取当前大量真实使用的 `Localize()`；`generate_all.py` 的注释和策略仍假定“中文语言文件用英文 key”，与当前已提交的 `data/qmclient/languages/english.txt` 角色不一致。这也是为什么直接跑同步脚本会错误改坏整套语言文件。

如果后续要继续做“以中文为准，进行 i18n”，可以直接把它定义成一次仓库级迁移，而不是继续套旧文档口径。更实际的分批方式是：先承认 QmClient 第二层语言系统当前采用“中文 key + english 反向映射”，然后按文件分批消除剩余英文 `Localize` 和直接中文硬编码；等主干文件收敛后，再统一修语言脚本与文档。

## 关键证据

| # | 结论 | 证据 | 位置 |
|---|------|------|------|
| 1 | 运行时确实会加载 QmClient 第二层语言文件 | `InitializeLanguage()` 先 `g_Localization.Load(g_Config.m_ClLanguagefile, ...)`，再拼接 `qmclient/%s` 并以 `Clear=false` 追加加载 | `src/game/client/gameclient.cpp:573`, `src/game/client/gameclient.cpp:579`, `src/game/client/gameclient.cpp:583`, `src/game/client/gameclient.cpp:584` |
| 2 | `Localize()` 命中失败会直接回退原文，因此代码里传入什么 key，最终就以那个 key 为查找基准 | `return pNewStr ? pNewStr : pStr;` | `src/game/localization.cpp:13` |
| 3 | `menus_qmclient.cpp` 当前已经大规模采用中文 key，而不是英文 key | 文件内当前约有 480 处 `Localize("中文...")`，例如 `Localize("视觉")`、`Localize("功能")`、`Localize("粒子数量")` | `src/game/client/components/qmclient/menus_qmclient.cpp:634`, `src/game/client/components/qmclient/menus_qmclient.cpp:635`, `src/game/client/components/qmclient/menus_qmclient.cpp:6490` |
| 4 | 同一个文件仍保留大量英文 `Localize`，说明不是单点遗漏，而是整体混合态 | 文件内仍约有 93 处英文 `Localize(...)`，例如 `Localize("Favorite maps")`、`Localize("Unknown")`、`Localize("Auto team lock")` | `src/game/client/components/qmclient/menus_qmclient.cpp:5235`, `src/game/client/components/qmclient/menus_qmclient.cpp:5280`, `src/game/client/components/qmclient/menus_qmclient.cpp:5526` |
| 5 | 文件内还有不经过 `Localize` 的直接中文硬编码，不能只处理 `Localize("英文")` | 例如赞助者名单标题和裸字符串 `Ui()->DoLabel(&Row, "栖梦(璇梦),夏日,DYL", ...)`；按简单模式统计，这个文件至少还有约 144 行直接中文字符串使用点 | `src/game/client/components/qmclient/menus_qmclient.cpp:2275` |
| 6 | 当前第二层语言文件已经把 `english.txt` 用作“中文 key -> 英文文案”的反向映射 | 例如 `视觉 == Visuals`、`功能 == Functions`、`粒子数量 == Particle count` | `data/qmclient/languages/english.txt:7`, `data/qmclient/languages/english.txt:10`, `data/qmclient/languages/english.txt:826` |
| 7 | 旧语言脚本仍在按过时模型工作，没有识别真实的 `Localize()` 使用 | `twlang_qmclient.py` 的正则只扫描 `(TCLocalize|TCLocalizable)`，不扫描 `Localize(...)` | `qmclient_scripts/languages_qmclient/twlang_qmclient.py:71` |
| 8 | `generate_all.py` 的策略文案与当前实际运行/提交状态冲突 | 顶部注释仍写 `simplified_chinese.txt: English keys -> Chinese translations`，并把 `english.txt` 定义为新增反向层；这和当前主力文件大量中文 key 的现实不一致 | `qmclient_scripts/languages_qmclient/generate_all.py:5`, `qmclient_scripts/languages_qmclient/generate_all.py:7`, `qmclient_scripts/languages_qmclient/generate_all.py:8`, `qmclient_scripts/languages_qmclient/generate_all.py:9` |
| 9 | 仓库里已有旧探索/报告，但其核心设计假设已经过期 | 旧报告明确写“`Localize()` 的参数始终为英文原文”，并把 `qmclient/languages/simplified_chinese.txt` 视为目标形态；这与当前已提交代码和语言文件不一致 | `docs/dyl/QmClient_docs/01-功能模块/汉化完善功能报告.md:23`, `docs/dyl/QmClient_docs/01-功能模块/汉化完善功能报告.md:40`, `docs/dyl/QmClient_docs/01-功能模块/汉化完善功能报告.md:52` |

## 探索范围

- 聚焦目录：`src/game/client/components/qmclient/`、`data/qmclient/languages/`、`qmclient_scripts/languages_qmclient/`
- 涉及文件：
  - `src/game/client/components/qmclient/menus_qmclient.cpp`
  - `src/game/client/gameclient.cpp`
  - `src/game/localization.cpp`
  - `data/qmclient/languages/english.txt`
  - `qmclient_scripts/languages_qmclient/twlang_qmclient.py`
  - `qmclient_scripts/languages_qmclient/generate_all.py`
  - `docs/dyl/QmClient_docs/01-功能模块/汉化完善功能报告.md`
- 跳过：
  - 没有逐文件穷举整个 `src/game/client/components/` 的所有英文/中文 key，只抽查了 `menus_qmclient.cpp` 这个当前核心目标文件，并用 `menus_tclient.cpp`、`menus_browser.cpp`、`hud.cpp` 做了范围侧证
  - 没有验证所有语言文件的完整一致性，只确认了 `english.txt` 当前承担的反向映射角色

## 置信度说明

**confidence: high**

- 已覆盖运行时语言加载入口、`Localize()` 回退逻辑、当前目标文件、当前语言文件和当前脚本入口
- 结论直接来自实际代码和文件内容，不依赖推测
- 未做的是“全仓穷举统计”，但不影响当前结论：现状确实是混合态，且旧脚本/旧文档口径已经落后于代码

## 后续建议

可以基于这份探索，先写一份“QmClient 汉化迁移分批计划”，明确哪些文件继续按“中文 key + english 反向映射”收敛，哪些脚本和文档最后统一修口径。
