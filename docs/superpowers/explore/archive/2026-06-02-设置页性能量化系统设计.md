---
type: spike
date: 2026-06-02
status: superseded
superseded_by: docs/superpowers/specs/2026-06-10-设置页性能优化总纲.md
confidence: high
updated: 2026-06-09
related:
  - docs/superpowers/explore/2026-06-02-设置页UI性能优化第一性原理分析.md
---

> ⚠️ **本文档已过时。** 稳定结论已吸收到 [设置页性能优化总纲](../specs/2026-06-10-设置页性能优化总纲.html)（2026-06-10）。以下内容仅作为历史背景参考。TS 脚本实现和报表设计仍然有效（`qmclient_scripts/perf/`），但 C++ 数据增强计划、FBO 相关结论和阈值建议以总纲为准。

## 速答

当前性能日志系统的三个缺陷：阈值过高（20ms，遗漏 5-19ms 的"灰色尖峰"）、格式不统一（key=value 文本难以机器分析）、无可视化（179k 行日志无法直观判断趋势）。

推荐方案分三层：**C++ 侧**降低阈值 + 增加结构化字段 → **TS 解析脚本**（`tsx` 运行）批量解析 → **ECharts 自包含 HTML** 生成可交互报表。零外部依赖，浏览器即可查看。

> **2026-06-02 更新**：TS 脚本层（Phase 1）已完成并发量远超原始设计。实际实现了 R 语言级别的统计诊断（核密度估计、箱线图、QQ 图、自动对比）和莫兰迪色系的论文式排版。C++ 数据增强（Phase 2）待做。

> **2026-06-09 更新**：性能量化不能只看内部 `duration_ms`。设置页还需要一个端到端感知指标：在同一硬件、同一窗口/renderer/UI scale、开启无上限帧率的固定操作路径下，记录设置界面的 `avg/max/min FPS`。它不负责归因，但非常适合做优化前后和用户反馈场景的直观对比。

---

## 1. 数据采集层改进（C++ 侧）

### 1.1 阈值调整

```cpp
// config_variables_qmclient.h — 现行与建议
MACRO_CONFIG_INT(QmPerfDebugThresholdMs, qm_perf_debug_threshold_ms, 20, 1, 1000, ...)
//                                                         ^^ 现行默认 20ms
//                                                         vv 建议默认 4ms
MACRO_CONFIG_INT(QmPerfDebugThresholdMs, qm_perf_debug_threshold_ms,  4, 1, 1000, ...)
```

4ms 的理由：目标 UI 帧预算 ≤ 4ms，超过此值即应记录。捕获率从"仅 28x 尖峰"→"捕获所有超出预算的帧"。

### 1.2 端到端 FPS 摘要指标

内部 profiler 字段回答"哪里慢"，无上限帧率下的设置页 `avg/max/min FPS` 回答"用户最终感受到的吞吐量有没有变好"。二者必须同时保留，不能互相替代。

推荐采集口径：

| 项 | 要求 |
| --- | --- |
| 场景 | 固定操作路径，例如打开 Settings、切到 Tee、滚动一屏、停留稳态 10-30 秒 |
| 帧率设置 | 开启无上限帧率；若 vsync、驱动限帧或系统限帧无法关闭，报告中标记为 `fps_cap_limited=1` |
| 环境 | 记录平台、renderer、窗口模式、分辨率、UI scale、客户端版本和硬件摘要 |
| 输出 | `fps_avg`、`fps_max`、`fps_min`、采样秒数、样本帧数 |
| 对比 | 只和同一机器、同一路径、同一设置的 baseline 比较 |

解释规则：

1. `avg FPS` 适合看整体吞吐量；优化后 avg 明显上升通常说明 CPU/GPU 总负载下降。
2. `min FPS` 对尖峰极敏感，适合暴露冷启动、切页、资源合并、文本重建这类长帧。
3. `max FPS` 能反映空闲稳态上限，但容易受帧率计数窗口、后台负载和限帧影响，只作为辅助。
4. FPS 只能做端到端验收和回归信号；具体归因仍必须回到 `perf/menu`、`perf/section`、`perf/interaction`、`perf/device` 的分阶段耗时。

建议报表在 Executive Summary 中新增一个小节：

```text
Uncapped Settings FPS:
  avg=842.1  max=1040.3  min=318.6  samples=18000  seconds=20
  fps_cap_limited=0  operation=settings_tee_scroll
```

### 1.3 新增结构化字段

当前 `perf/menu` / `perf/section` 日志应优先补 CUI 热路径真正能指导优化的字段：

| 现有字段 | 新增字段 | 用途 |
| --- | --- | --- |
| `stage=settings_page_content` | `sections=12` | section 总数 |
| — | `sections_visible=3` | 可见 section 数 |
| — | `rows_total=120 rows_visible=14 rows_rendered=16 rows_skipped=104` | 列表是否做到 O(visible) |
| — | `text_new=5` | 本帧新创建的 text container |
| — | `text_reused=28` | 本帧复用的 text container |
| — | `layout_dirty=1 dirty_reason=width_changed` | layout / 高度是否真的需要重算 |
| — | `draw_calls=42` | 本帧 draw call 数量 |
| `page=tee` | `tab=0` | 子 tab 编号 |
| `duration_ms=2.046` | `frame=N` | 帧序号（用于关联同一帧的多条日志） |
| — | `fps_avg=842.1 fps_max=1040.3 fps_min=318.6` | 操作路径级 FPS 摘要，不放在每个热路径事件里 |

QmUI 不应混进 CUI 的 immediate-mode 字段。它已有 `CUiRuntimeV2`、tree、layout engine、animation runtime 和 render bridge，后续应单列 `perf/ui_runtime` 字段：

| 字段 | 用途 |
| --- | --- |
| `nodes=...` | 判断 tree 规模是否随页面 / 列表异常膨胀 |
| `tree_begin_ms` / `tree_end_ms` | 判断 runtime tree frame 管理成本 |
| `layout_ms` / `layout_dirty` | 判断 QmUI 调用方 layout 是否每帧重算 |
| `anim_ms` / `active_anims` | 判断动效是否在不可见或稳定节点上继续推进 |
| `render_bridge_ms` / `draw_calls` / `clip_pushes` | 判断 QmUI bridge 提交和裁剪成本 |

### 1.4 日志格式统一为 JSON Lines

```
// 现行（key=value 文本，难以解析嵌套结构）：
2026-05-26 01:17:23 I perf/menu: stage=settings_page_content duration_ms=2.046 page=tee tab=0

// 建议（JSON Lines，一行一个对象，TS 直接 JSON.parse）：
{"t":"2026-05-26T01:17:23","sys":"perf/menu","stage":"settings_page_content","dur":2.046,"page":"tee","tab":0,"frame":1842,"sections":12,"sections_vis":3,"rows_visible":14,"rows_rendered":16,"text_new":5,"text_reused":28,"draws":42}
```

通过新增配置 `qm_perf_json 1` 切换格式，默认保持兼容旧格式。

### 1.5 新增指标点

当前只记录了 `perf/menu`（菜单级）和 `perf/gameclient`（游戏循环级）。建议在 section 级别增加：

```
// 每个 section 渲染完成时（在 CSectionLoader::Process() 中）
{"t":"...","sys":"perf/section","page":"tee","section":"skin_list","dur":3.2,
 "visible":true,"dirty":"none","rows_visible":8,"rows_total":120,"rows_skipped":112}
```

这让你可以精确知道**哪个 section 慢**，而不是只知道"设置页慢"。

---

## 2. 分析脚本层（TS 侧）

### 2.1 技术选型

| 选项 | 选择 | 理由 |
| --- | --- | --- |
| 运行时 | `tsx` | 直接运行 `.ts` 文件，零配置，速度快 |
| 解析 | 手写 + `JSON.parse` | 日志行已是 key=value 或 JSON，无需第三方解析库 |
| 统计 | 手写 aggregator | `Map<key, number[]>` 做分组统计，几十行代码 |
| 可视化 | **ECharts (CDN)** | 图表类型最全，`dataZoom` 组件天然适合时间序列 |
| 报表 | 手写 HTML 模板 | 模板字面量 + 内嵌 `<script>` |

### 2.2 脚本结构

```
qmclient_scripts/perf/
├── package.json          # 仅依赖 tsx + @types/node
├── tsconfig.json
├── README.md             # 使用说明
├── lib/
│   ├── parse.ts          # 日志解析器（支持旧格式 + JSON Lines）
│   ├── stats.ts          # 统计算子（百分位、分组、尖峰检测）
│   └── report.ts         # HTML 报表生成器（ECharts 内嵌）
├── analyze.ts            # 主入口：parse → stats → report
└── test/
    └── sample.log        # 测试用样本日志
```

### 2.3 数据流

```
qm_perf_*.log  ──→  parse.ts  ──→  ParsedEntry[]  ──→  stats.ts  ──→  ReportData
                                                                           │
                                                    ┌──────────────────────┘
                                                    ▼
 PERF_REPORT=1 ./analyze.ts    ──→   report.ts  ──→  qm_perf_report.html
```

### 2.4 统计维度 — 已实现

| 统计项 | 方法 | 输出图表 |
| --- | --- | --- |
| 无上限 FPS 摘要 | 按 session / operation 汇总 `fps_avg/max/min` | Executive Summary KPI + 同路径前后对比 |
| 帧时间趋势 | 按时间序列分帧 | Timeline 折线图（对数 Y 轴，兼顾正常帧与尖峰）+ 三条阈值线 |
| 帧时间分布 | 分桶计数 + Gaussian KDE | 直方图 + 核密度估计曲线叠加 |
| 百分位概览 | 排序取 p50/p90/p95/p99 | 垂直柱状图 + 阈值标线 |
| 分布诊断 | QQ Plot vs 正态分布 | 散点图 + 参考线（偏离 = 非正态） |
| 页面耗时分解 | 按页面分组的 Box Plot | 箱线图（Q1/median/Q3/IQR/离群值）+ 对数 Y 轴 |
| 尖峰帧列表 | `filter dur > 16.67` | 表格（时间/Stage/Page/耗时/超标倍数/Severity） |
| Session 自动对比 | 自动找上一次日志，逐指标对比 | Delta 卡片矩阵（改善/退化方向 + 变化百分比） |
| 采样偏差检测 | p5 推断阈值，&gt;4.17ms 则警告 | 黄色横幅（含调整建议） |

**依赖 C++ 数据增强后才可实现的统计（Phase 2 阻塞）：**

| 统计项 | 阻塞原因 |
| --- | --- |
| Section 耗时排行 Top-10 | 无 `perf/section` 日志点 |
| QmUI runtime 分解 | 现有 runtime 日志还没有和页面 / 操作路径稳定关联 |
| 文本缓存效率指标 | 无 `text_new` / `text_reused` 字段 |

---

## 3. 可视化报表设计（已实现）

### 3.1 实际布局（论文式竖向滚动）

```
┌──────────────────────────────────────────────────────────┐
│  QmClient 设置页性能分析报告                              │
│  Settings Page UI Performance Quantitative Analysis       │
│  Date: 2026-06-02  |  Source: qm_perf_xxx.log            │
│  Verdict: [FAIL]  |  Total: 512  |  Spikes: 3           │
│──────────────────────────────────────────────────────────│
│  Executive Summary                                       │
│  自动叙事文本 + 合规率 + 尖峰摘要 + 无上限 FPS 摘要         │
│──────────────────────────────────────────────────────────│
│  ⚠ Sampling Bias（采样偏差警告，阈值>4ms 时出现）         │
│──────────────────────────────────────────────────────────│
│  vs  Session 对比分析（有历史日志时自动出现）              │
│  7 项指标 + 3 项合规率，每项显示 delta 方向 + 变化百分比   │
│──────────────────────────────────────────────────────────│
│  §1  关键性能指标                                        │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌──────┐    │
│  │ p50 │ │ p95 │ │ p99 │ │ Max │ │240Hz│ │60Hz  │    │
│  │2.1ms│ │8.2ms│ │15ms │ │471ms│ │88.7%│ │97.9% │    │
│  └─────┘ └─────┘ └─────┘ └─────┘ └─────┘ └──────┘    │
│──────────────────────────────────────────────────────────│
│  §2  描述统计（四列对比表: Min/Q1/Med/Q3/Max/IQR/...）   │
│──────────────────────────────────────────────────────────│
│  §3  帧时间趋势 (Y 轴对数刻度)                            │
│  ┌────────────────────────────────────────────────────┐  │
│  │  Timeline: ──4.17ms(紫)  ──8.33ms(青)  ──16.67ms(玫红)  │
│  │  ◆471ms general    ◆202ms tee                       │
│  │  ═══════════════════dataZoom═══╛                    │  │
│  └────────────────────────────────────────────────────┘  │
│──────────────────────────────────────────────────────────│
│  §4  分布诊断                                            │
│  ┌────────────────────┐ ┌───────────────────┐          │
│  │ 直方图 + KDE 曲线  │ │  QQ Plot vs N(μ,σ²) │          │
│  │ 莫兰迪色分桶       │ │  散点 + y=x 参考线 │          │
│  └────────────────────┘ └───────────────────┘          │
│──────────────────────────────────────────────────────────│
│  §5  页面级耗时分解（按页面分组）                         │
│  ┌────────────────────┐ ┌───────────────────┐          │
│  │ Box Plot（Tukey）  │ │ 百分位柱状图      │          │
│  │ Q1/med/Q3/IQR/离群 │ │ p50/p90/p95/p99   │          │
│  └────────────────────┘ └───────────────────┘          │
│──────────────────────────────────────────────────────────│
│  §6  尖峰帧详情表格 (Timestamp | Stage | Page | ...)     │
│──────────────────────────────────────────────────────────│
│  §7  数据采集方法 + 判定标准                             │
└──────────────────────────────────────────────────────────┘
```

### 3.2 实际交互功能

- **dataZoom**：时间轴可拖拽缩放，聚焦任意时间段
- **对数 Y 轴**：Timeline + Box Plot 均用 log scale，正常帧和尖峰帧同时可见
- **三条帧预算标线**：4.17ms (240Hz, 紫色虚线) / 8.33ms (120Hz, 青色虚线) / 16.67ms (60Hz, 玫红虚线)
- **尖峰标注**：菱形标记在 Timeline 上标注尖峰值
- **采样偏差检测**：p5 推断采样阈值，&gt;4.17ms 时自动警告
- **Session 自动对比**：自动找上一次日志，逐指标对比，delta 着色
- **无上限 FPS 摘要**：同操作路径显示 `avg/max/min FPS`，用于快速判断整体吞吐量和最差体感是否改善

### 3.3 莫兰迪色系（Morandi Palette）

| 用途 | 颜色 | 色值 |
| --- | --- | --- |
| 主线/面积 | Steel Blue | `#8B9DAF` |
| OK/120Hz | Sage Green | `#8FA89A` |
| 警告级 | Sand | `#C4A77D` |
| 危险/60Hz | Dusty Rose | `#B5838D` |
| 240Hz 标线 | Lavender | `#9B8BB4` |

### 3.4 ECharts 图表选型（已实现）

| 需求 | ECharts 组件 | 备注 |
| --- | --- | --- |
| 帧时间趋势 | `line` + `dataZoom` + `markLine`×3 | Y 轴 `type: 'log'` |
| 帧时间分布 + KDE | `bar` + `line`（双 Y 轴） | Silverman 带宽 |
| QQ 图 | `scatter` + `line` | 正态逆CDF 近似 |
| 页面箱线图 | `boxplot` + `scatter`（离群值） | Tukey, 1.5×IQR |
| 百分位 | `bar` + `markLine`×3 | — |

---

## 4. 实现路线

### Phase 1：脚本原型 ✅ 已完成（超预期）

- [x] `lib/parse.ts` — 解析 key=value + JSON Lines 双格式

- [x] `lib/stats.ts` — 百分位/分组/尖峰检测 + KDE/BoxPlot/QQNorm/会话对比/snapshot

- [x] `lib/report.ts` — ECharts 论文式 HTML 报表（莫兰迪色系，7 节，12 张图表）

- [x] `analyze.ts` — CLI 入口，自动发现最新日志 → Perf_Report/ 输出

- [x] `package.json` / `tsconfig.json` / `README.md`

- [x] `docs/superpowers/explore/2026-06-02-设置页性能量化系统设计.html` — 带模拟数据的预览

**超标项**：实际统计函数远超设计（从 4 个扩展到 16+ 个导出函数），图表从 5 种扩展到箱线图/QQ图/KDE 叠加/自动对比。

### Phase 2：C++ 数据增强 ⬜ 待做

**P0 — 阻塞了最有价值的 section 级分析图表：**

- [ ] 阈值默认值 20→4ms

- [ ] 新增 `perf/section` 日志点（在 `CSectionLoader::Process()` 中）

- [ ] `LogPerfStage` 增加字段：`frame` 序号、`sections`/`sections_visible`、`rows_total`/`rows_visible`/`rows_rendered`/`rows_skipped`、`text_new`/`text_reused`、`layout_dirty`、`dirty_reason`、`draw_calls`

- [ ] QmUI 单列 `perf/ui_runtime` 摘要：`nodes`、`layout_ms`、`layout_dirty`、`active_anims`、`draw_calls`、`clip_pushes`，不要混用 CUI 的 `DoLabel` / `DoButtonLogic` 字段

- [ ] 增加操作路径级 FPS 摘要采集：`fps_avg`、`fps_max`、`fps_min`、`fps_sample_frames`、`fps_sample_seconds`、`fps_cap_limited`

- [ ] 可选 JSON Lines（`qm_perf_json 1`）

**C++ 文件定位**：

- 配置：`src/engine/shared/config_variables_qmclient.h:15`
- 设置页 LogPerfStage：`src/game/client/components/menus_settings.cpp:103-114`
- 菜单级 LogPerfStage：`src/game/client/components/menus.cpp:187-`

### Phase 3：报表增强（部分完成）

- [x] Session 自动对比（`analyze.ts` 自动找上一个 log，`stats.ts` `compareSessions()`）

- [x] 自动检测性能回归（delta 方向计算 + 变化百分比 + 判定变化告警）

- [ ] CI 集成（`game-client` 构建后自动跑 UI 操作采集 perf log，脚本检查 p99 是否超标）

- [ ] 手动指定基线（`analyze.ts --baseline path/to/baseline.log`）

- [ ] 将无上限 FPS 摘要纳入 Session 自动对比，但仅在 operation、环境和限帧状态一致时显示 delta

### Phase 4：待规划

- [ ] 测试覆盖（`test/sample.log` + `npm test` 冒烟测试）

- [ ] watch 模式（`analyze.ts --watch`，新日志生成 → 自动出报告）

- [ ] 多 Session 对比（&gt;2 个 log 叠加时序图）

- [ ] Section 耗时 Top-10 水平条形图（依赖 Phase 2 `perf/section`）

- [ ] QmUI runtime 分解图（依赖 `perf/ui_runtime` 与 operation 关联）

- [ ] 文本缓存效率指标（依赖 Phase 2 `text_new`/`text_reused`）

---

## 5. 关键设计决策

### 5.1 为什么不用 Python？

- Python 需要 `pip install` matplotlib/numpy/pandas，依赖重
- TS + `tsx` 只需 `npm install tsx`（或 `npx tsx` 零安装运行）
- DDNet/QmClient 已大量使用 JS/TS 生态（workflows、skill scripts）

### 5.2 为什么用 ECharts 而不是 Chart.js？

- ECharts 的 `dataZoom` 组件对时间序列数据体验极佳
- 内置 `boxplot`/`scatter`/`line`/`bar`，无需第三方扩展
- Canvas 渲染（比 SVG 更适合大数据量）

### 5.3 为什么生成 HTML 而不是 PNG？

- HTML 可交互（缩放、tooltip、联动）
- 无需 `puppeteer`/`playwright` 等 headless 浏览器依赖
- 可作为 CI artifact 直接上传查看

### 5.4 为什么 Timeline 用对数 Y 轴？

- 性能数据有极端离群值（正常帧 1-4ms，冷启动尖峰 471ms）
- 线性 Y 轴下尖峰撑满整个高度，基线压缩成一条线，无法阅读
- 对数刻度下 1ms/10ms/100ms 各有视觉空间，正常帧和尖峰都清晰可见
- Box Plot 同样使用对数 Y 轴，保持一致性

### 5.5 采样偏差检测

- `LogPerfStage` 函数（`menus_settings.cpp:107`）：`if(!Force && DurationMs < PerfDebugThresholdMs()) return;`
- 用户已将运行时阈值设为 1ms（覆盖了代码默认值 20ms）
- 当 p5 &gt; 4.17ms 时，报告自动显示黄色警告条
- 用 p5（而非 min）推断阈值，避免被 `Force=true` 的告警记录干扰

### 5.6 会话对比策略

- `analyze.ts` 运行时自动检查 `Perf_Report/` 下前一份报告对应的日志文件
- 10 项指标逐项对比（7 项百分位类 + 3 项合规率），每项计算 delta 方向和变化百分比
- 判定变化（PASS→WARN 等）在对比 section 顶部显著标注
- 状态栏输出改善/退化项数总结

### 5.7 实际文件结构

```
qmclient_scripts/perf/
├── package.json                    # tsx + @types/node + typescript
├── tsconfig.json                   # ES2022 + ESM
├── README.md                       # 使用说明
├── analyze.ts                      # CLI 入口（自动对比 + 输出到 Perf_Report/）
├── lib/
│   ├── parse.ts                    # 旧格式 + JSON Lines 双格式解析
│   ├── stats.ts                    # 16 个导出：百分位/合规率/KDE/箱线图/QQ图/对比/…
│   └── report.ts                   # 论文式 HTML 模板（莫兰迪色系，7 节 12 图）
└── test/                           # 预留（待加 sample.log）

输出: %APPDATA%/DDNet/dumps/QmClient_Perf/Perf_Report/qm_perf_xxx_report.html
```
