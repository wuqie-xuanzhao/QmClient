---
type: question
date: 2026-06-13
status: active
confidence: high
scope:
  - docs/superpowers/plans/2026-06-11-设置页性能优化重组版实施计划.md
  - docs/superpowers/specs/2026-06-11-设置页性能优化总纲-重组版.html
  - src/game/client/components/menus.h
  - src/game/client/components/menus.cpp
  - src/game/client/components/menus_settings.cpp
  - src/game/client/components/tclient/menus_tclient.cpp
  - src/game/client/components/qmclient/menus_qmclient.cpp
  - src/game/client/gameclient.cpp
  - src/test/qmclient_monitoring_test.cpp
  - src/test/settings_warmup_test.cpp
  - qmclient_scripts/perf/lib/stats.ts
  - qmclient_scripts/perf/lib/quality.ts
  - qmclient_scripts/perf/lib/report.ts
  - qmclient_scripts/perf/test.ts
commit: cecb13e74
related:
  - file: 2026-06-10-设置页文本缓存覆盖现状.md
    relation: references
  - file: ../plans/2026-06-11-设置页性能优化重组版实施计划.md
    relation: references
  - file: ../specs/2026-06-11-设置页性能优化总纲-重组版.html
    relation: references
---

## 速答

这份计划当前**没有完成**。代码已经从旧的 settings 局部文本池推进到 `CMenus` 级 `m_MenuTextPool`，并且有 visible guard、miss/stale telemetry、TClient/Base/QmClient candidate audit、loading 阶段预构建、perf analyzer blocker；但还不能按更新后的 spec 宣称完成，因为 QmClient candidate audit 当前失败，可见渲染路径仍会调用 `PrebuildVisibleSettingsTextPool()` 给非 TClient/QmClient settings 页补文本，spec 新增的文本池利用率口径也没有落地。

按旧 plan 粗略看，Task 1/2/8 基本落地，Task 3/5 未收口，Task 7 只有部分落地，Task 9 没有完成。按更新后的 spec 看，P0/P1 只能算“进行中”：核心机制有了，但 coverage、不可见阶段边界、利用率证据和真实目标场景验收还缺。

## 关键证据

| # | 结论 | 证据 | 位置 |
|---|---|---|---|
| 1 | 通用池机制已存在，旧 `m_SettingsTextPool` 不再是主要边界 | `SMenuTextStyleKey` / `SMenuTextPlanItem`、`m_MenuTextPool`、`MenuTextElement()`、`DoMenuLabelStreamed()`、`MenuTextPoolSizeForTesting()` 都已存在 | `src/game/client/components/menus.h:1821`, `src/game/client/components/menus.h:1977`, `src/game/client/components/menus.cpp:4160`, `src/game/client/components/menus.cpp:4213`, `src/game/client/components/menus.cpp:4288` |
| 2 | visible miss/stale fallback 已实现 | visible guard 激活时 miss/stale 不新建池项，而是记录 `settings_text_miss` / `settings_text_stale` 并返回 fallback element | `src/game/client/components/menus.cpp:4177`, `src/game/client/components/menus.cpp:4192`, `src/game/client/components/menus.cpp:4293`, `src/game/client/components/menus_settings.cpp:4647` |
| 3 | TClient/QmClient/Base plan builder 已存在，但 QmClient plan 覆盖仍明显偏小 | TClient plan 覆盖大量 settings tab 文本；QmClient plan 当前只列出少数 visual/function 文本项 | `src/game/client/components/tclient/menus_tclient.cpp:258`, `src/game/client/components/qmclient/menus_qmclient.cpp:479`, `src/game/client/components/menus.cpp:4336`, `src/game/client/components/menus.cpp:4472` |
| 4 | 不可见 loading 预构建已接线，但没有持久 cursor | loading 会循环调用 `PrewarmSettingsTextPoolForLoading(8)`；`PrebuildSettingsTextPoolForLoading()` 每次重新 `BuildSettingsMenuTextPlan()` 并从头遍历，没有看到 plan cursor 成员 | `src/game/client/gameclient.cpp:641`, `src/game/client/gameclient.cpp:654`, `src/game/client/components/menus.cpp:4540`, `src/game/client/components/menus.h:1866` |
| 5 | 可见阶段边界仍不符合更新后 spec | `CMenus::Render()` 调用 `PrewarmVisibleSettingsResources()`，后者继续调用 `PrebuildVisibleSettingsTextPool(ContentView, 6)`；该函数只跳过 TClient/QmClient，仍会给其它 settings 页可见阶段补文本 | `src/game/client/components/menus.cpp:2734`, `src/game/client/components/menus.cpp:2833`, `src/game/client/components/menus.cpp:4558`, `src/game/client/components/menus.cpp:4694` |
| 6 | candidate audit 存在，但 QmClient 当前失败 | focused testrunner 跑 8 个相关测试，7 个通过，`QmMonitoringHelpers.QmClientStableTextCandidateAuditIsEmptyExceptAllowlist` 失败，未收口候选为 `Send`、`Animation range`/`QmNewFeatureLabel`、`Copied` | `src/test/qmclient_monitoring_test.cpp:1186`, `src/game/client/components/qmclient/menus_qmclient.cpp:4687`, `src/game/client/components/qmclient/menus_qmclient.cpp:5191`, `src/game/client/components/qmclient/menus_qmclient.cpp:5777` |
| 7 | perf analyzer blocker 已落地且测试通过 | `StableTextCoverageSummary` 包含 `acceptanceBlocked`、miss/stale/remaining；report 会显示“不足以验收”；`npm test` 通过 | `qmclient_scripts/perf/lib/stats.ts:251`, `qmclient_scripts/perf/lib/stats.ts:390`, `qmclient_scripts/perf/lib/report.ts:76`, `qmclient_scripts/perf/test.ts:221` |
| 8 | spec 新增的利用率口径未落地 | 当前 analyzer 统计 miss/stale/prebuild series 和 `text_new/text_reused` 原有字段，但没有候选总数、命中数、hit rate / reuse rate 等 stable text coverage 利用率字段 | `qmclient_scripts/perf/lib/stats.ts:304`, `qmclient_scripts/perf/lib/stats.ts:455`, `qmclient_scripts/perf/lib/stats.ts:744`, `docs/superpowers/specs/2026-06-11-设置页性能优化总纲-重组版.html:461` |

## 细节

本次使用 CodeGraph CLI 同步索引后检查结构：`codegraph status` 显示索引 up to date；`codegraph query/callers/callees` 确认 `PrebuildSettingsMenuTextPool()`、`BuildTClientSettingsMenuTextPlan()`、`BuildQmClientSettingsMenuTextPlan()`、`BuildBaseSettingsMenuTextPlan()`、`stableTextCoverage()` 等符号存在。CodeGraph 调用关系还确认 `PrebuildSettingsMenuTextPool()` 只通过 `PrewarmSettingsTextPoolForLoading()` 进入 loading 预热，而 `PrebuildVisibleSettingsTextPool()` 通过 `PrewarmVisibleSettingsResources()` 从 `CMenus::Render()` 进入可见渲染路径。

执行过的验证：

- `npm test` in `qmclient_scripts/perf`：通过，输出 `qmclient perf tests passed`。
- `cmake-build-release/testrunner.exe --gtest_filter=...` focused 8 项：7 通过，1 失败；失败项是 `QmMonitoringHelpers.QmClientStableTextCandidateAuditIsEmptyExceptAllowlist`。

没有执行完整重建、quick gate、真实 perf capture，因此不能用这次探索证明 `settings:tclient`、`settings:qmclient`、`ingame_esc_open` 目标窗口已经满足验收。

## 探索范围

- 重点看了 2026-06-11 重组版 plan、更新后的重组版 spec、`CMenus` 文本池、TClient/QmClient/Base settings 迁移、source-contract tests、perf analyzer。
- 没有做代码修改，也没有跑完整 C++ build、quick gate、真实 UI perf 采集。
- 没有逐项人工审核所有 migrated helper 的语义，只依赖 CodeGraph、源码片段和 focused source-contract 测试定位完成状态。

## 置信度说明

**confidence: high**

- CodeGraph 索引已同步，结构查询覆盖了相关符号和调用关系。
- 结论依赖真实源码行号和 focused 测试输出。
- 仍然没有完整构建和真实 perf capture，因此“性能是否达标”的置信度不是 high；本报告的 high 只针对“计划尚未完成、当前缺口在哪里”。

## 未决问题

- QmClient audit 的 4 个候选应迁移进池，还是按 `stateful-new-label` / `status-message` / 动态状态补 allowlist，需要按语义确认。
- `PrebuildVisibleSettingsTextPool()` 是否应完全删除或改成只统计不构建，需要按更新后的 spec 决定。
- `ingame_esc_open` 的 stable-text blocker 是否在真实日志中总能拿到 `scope=target_settings`，还需要实际 capture 证明。
- 文本池利用率指标的事件格式尚未定稿。

## 相关文档

- `docs/superpowers/plans/2026-06-11-设置页性能优化重组版实施计划.md` — 旧实施计划，本次用于任务拆解对照。
- `docs/superpowers/specs/2026-06-11-设置页性能优化总纲-重组版.html` — 更新后的权威 spec，本次以它优先。
- `docs/superpowers/explore/2026-06-10-设置页文本缓存覆盖现状.md` — 旧文本缓存覆盖探索，说明从 settings 局部池向通用池演进的背景。

## 下一步

下一步应先收口 QmClient candidate audit 和可见阶段 prebuild 边界，再补 spec 要求的文本池利用率指标；这三项完成后再做完整 build/gate 和真实 `settings:tclient`、`settings:qmclient`、`ingame_esc_open` perf capture。
