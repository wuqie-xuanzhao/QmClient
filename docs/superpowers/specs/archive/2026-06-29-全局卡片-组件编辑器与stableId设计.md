# 全局卡片：跨页面组件编辑器 · stableId 与稳定持久化设计

> **已归档，禁止作为实现依据。** 当前权威规格：`docs/superpowers/specs/2026-07-10-QmClient-设置页UI统一与滚动体系规格.md`。

- 日期：2026-06-29
- status: archived
- 来源：spec v2（`2026-06-29-全局卡片架构-统一拖拽与持久化.html`）+ 用户确认（全局任意排布 + 跨页面组件编辑器 + 默认值 + 稳定持久化）+ 卡片 id 全量调研
- 配套：本文件是主 spec 第 2 章「全局卡片 Model」的深化；卡片全量映射见文末「附录 · stableId 注册表」

> 取代主 spec 第 1 节「待验证假设」——用户已确认方向：**全局卡片任意排布 + 跨页面组件编辑器**。全局单例 Model 是正确架构，不再标记为待验证。

## 0 · 2026-07-07 实现状态

本文仍是**全局卡片组件编辑器 / stableId / 持久化模型**的目标设计。当前代码已经推进到“核心模型能力存在、部分页面接入、公共 UI primitive 收口中”的状态，但还不能视为全目标完成。

> 完成口径：`docs/superpowers/plans/2026-07-07-全局卡片与公共控件收口计划.md` 当前记录的是收口计划、执行证据和未完成台账，不是完成证明。只要人工视觉检查、真实 dropdown clip/hit-test、输入框编辑行为、跨页面生产接入审计或代码审查任一项未收口，本 spec 仍保持“部分实现 / 待验收”。

已具备的结构能力：

- `qm_card_order::CModel` 已包含 `stableId + tab + column + order` 维度，提供 `ColumnIndices(tab, column)`、`MoveToTab()`、`StableIdOrder()`、`StateIndexForStableId()`、`IsDirty()`。
- `qm_card_registry::Defaults()` 已成为全局卡片默认 Placement 的代码单一事实源，覆盖 `qm:`、`tclient:`、`deck:` 三类卡片；当前代码表为 86 张卡（Qm 38 + TClient 19 + deck 29），数量以后以注册表为准。
- `qm_module::QmModuleLayoutAdapter` 已提供 Qm 旧布局、全局布局、`qm_global_card_order` 合并序列化和 `qm_card_order_migrated` 迁移适配能力。
- 栖梦卡片拖拽已采用松手提交 + QmUi FLIP 释放过渡，不再依赖旧的拖拽中即时视觉让位作为唯一方案。
- 公共 UI 收口计划已引入设置卡片 shell/layout、滚动条 preset、输入框 shell、dropdown wheel policy 等目标；执行计划见 `docs/superpowers/plans/2026-07-07-全局卡片与公共控件收口计划.md`。

仍需验收或补齐：

- 生产渲染是否所有页面都只读同一全局 Model，仍要逐页审计，不能仅凭模型存在判定完成。
- 组件编辑器的跨页面移动 UI、跨页可视化管理、reset/defaults 入口仍需按本 spec 验收。
- TClient、常规 Settings deck、Search 结果页是否完全消除旧 helper/旧拖拽/旧样式路径，需要结合结构测试和人工视觉检查确认。
- `qm_global_card_order` 与旧配置迁移必须确认不会覆盖非 Qm 命名空间条目，且只在 `IsDirty()` 或外部配置变更时序列化。
- 视觉手感、dropdown 裁剪、输入框鼠标编辑行为和拖拽让位/释放动画必须以客户端人工检查作为完成证据；普通 editbox 双击全选已有结构路径，但仍需真实客户端操作验收。

## 1 · 愿景（用户确认）

卡片是 **first-class 全局组件**，用 stableId 唯一标识，**不绑定页面**。每张卡片有一个「当前位置」`(tab, column, order)`，用户可任意排布、迁移到任何 tab / 列 / 位置。**tab 既是默认分类，也是可改的位置维度**（不是卡片的固有属性）。默认布局 = 出厂布局，可一键 reset。

一句话：**卡片是组件库里的组件，tab/页面是画布上的视图，用户在画布上任意摆放组件**。

## 2 · 数据模型

```text
Card        = 全局组件 { stableId, ...卡片内容 }        // stableId 唯一，不含位置
Placement   = 位置     { tab, column, order }          // 每卡当前唯一位置
GlobalLayout = 所有卡片的 Placement 集合                // 全局唯一权威
```

- **v1：单实例可迁移**（iPhone 主屏模型）——每卡一个 Placement，可从 tab A 整体搬到 tab B。简单、兼容现状、符合 spec 一直沿用的 iPhone 类比。
- **v2（留口，不实现）：多 tab 共享**——同一卡可出现在多个 tab（Placement 列表）。v1 的 stableId 设计不阻碍未来升级（仅需把 Placement 标量改为列表）。

> ⚠️ 这是**待你拍板的核心决策**（见 §7-1）：若你其实想要「一张卡同时出现在多个 tab」，数据模型要改。下文默认按 v1 单实例展开。

## 3 · stableId 命名规范

两级：`<ns>:<name>`

| ns（命名空间） | 含义 | 来源 |
|---|---|---|
| `qm` | 栖梦侧栏模块（38 个） | 现 `QmSidebarCardOrder` |
| `tclient` | Tclient section 卡（15 个） | 现 `qm_settings_card_order`（已用 `tclient:` 前缀） |
| `deck` | 设置 deck 卡（graphics/sound/ddnet/bindwheel/statusbar，~16 个） | 现内存 map（无持久化） |

**规则**：
- `<name>` 保留各卡现有的本地标识，做最小规范化（不重命名，降低迁移风险）。
- **stableId 不编码 tab**——tab 是可变位置，写进 id 会让「迁移到别的 tab」变成「换 id」，破坏稳定性。
- Tclient 现有 `tclient:<name>` **天然兼容**新规范，零改动。

**迁移映射规则**（旧 → 新，机械前缀化为主）：

| 旧体系 | 旧样例 | 新 stableId | 规则 |
|---|---|---|---|
| 栖梦（裸 key） | `chat_bubble` | `qm:chat_bubble` | 加 `qm:` 前缀 |
| 栖梦（**QiaFen 陷阱**） | `qiafen` | `qm:qiafen` | 加 `qm:` 前缀；**以持久化 key `qiafen` 为权威**，绝不用 UI 名 `keyword_reply` |
| Tclient section | `tclient:visual-nameplates` | `tclient:visual-nameplates` | 不变（已兼容） |
| 设置 deck | `graphics-display` | `deck:graphics-display` | 加 `deck:` 前缀 |

> ⚠️ **QiaFen 三名分裂**（枚举 `QiaFen` / UI 名 `keyword_reply` / 持久化 key `qiafen`，见 `menus_qmclient.cpp:1416 vs :1480`）是迁移最大陷阱。映射表与解析**一律以 `m_pKey`（持久化字符串）为权威**，否则会把用户已有的 `qiafen:` 条目判为未知 key 丢弃。

## 4 · 默认值系统（出厂布局）

### 4.1 组件注册表（单一事实源）
每张卡片在代码内声明默认 Placement：

```cpp
struct SCardDefault {
    const char *m_pStableId;   // "qm:chat_bubble"
    const char *m_pDefaultTab; // "visual"
    EColumn m_DefaultColumn;   // Left / Right / Full
    int m_DefaultOrder;        // 列内序
};
// 全局一张静态表，卡片增删在此维护
static constexpr SCardDefault s_aCardDefaults[] = { ... };
```

这份表 = 全局卡片的**组件注册表**，是默认值、迁移兜底、新卡补位的唯一依据。

### 4.2 SmartDefaults（贪心左右高度平衡）
栖梦现有 `ApplySmartQmModuleLayoutDefaults`（`menus_qmclient.cpp:1636-1692`）的贪心算法（按分组累加估算高度、哪列矮放哪列）**下沉为全局共享能力**，所有 page 复用。各 page 只需提供一个「高度估算」回调（高度是 page/卡特定的）。

- 触发时机：config 为空 / 用户 reset / 新增卡片补位。
- **reset = 清空 config → SmartDefaults 重算出厂布局**（语义清晰，无需单独的 reset 存储）。
- Tclient / 设置 deck 当前没有智能平衡 → 全局化后**自动获得**这个能力（纯增益）。

### 4.3 数据债清理（已纳入注册表）
早期调研发现两张「有默认布局但未归入任何 tab 分组数组」的卡（visual/function/hud 三组都不含），智能默认走兜底槽，渲染可见性存疑：
- **`nameplate_text`**（Right/18）
- **`laser`**（Right/3）——早期调研误判为「死分支」，实为活卡。

当前 `qm_card_registry::Defaults()` 已为它们补齐默认 tab：`qm:nameplate_text` → `hud`，`qm:laser` → `visual`。后续迁移和 reset/defaults 逻辑必须以注册表为权威，不再从旧分组数组反推。

清理遵循既有先例（`qm_lyrics_hud.md` 记录的 Lyrics 36→35 流程）。

## 5 · 稳定持久化模型

### 5.1 存储
单一 config `qm_global_card_order`（替代 `QmSidebarCardOrder` + `qm_settings_card_order` + 设置 deck 内存 map 三套）。

### 5.2 格式
```text
<stableId>|<tab>|<column>|<order>;<stableId>|<tab>|<column>|<order>;...
```
样例：
```text
qm:chat_bubble|visual|left|0;tclient:visual-nameplates|tclient|left|0;deck:graphics-display|graphics|left|0;
```

**字段分隔用 `|` 而非 `:`**——因为 stableId 本身含 `:`（`qm:chat_bubble`），用 `:` 分隔字段会产生歧义。`|` 在 config 字符串里安全且可读。

- `column`：字符串 `full`/`left`/`right`（保留栖梦的 **Full 列语义**，`info` 卡依赖）。
- 兼容旧格式：解析时检测分隔符——含 `|` 走新格式，否则按旧 `key:col:order;`（栖梦）或 `id:col:order;`（Tclient 数字列）回退。

> 格式字符串本身是 B1 实现时定稿的细节；设计层只要求：**无歧义、容错、兼容旧三套格式**。

### 5.3 模型：全量用户布局（沿用栖梦，已验证）
持久化 = 用户当前布局的**全量快照**（所有卡的当前位置），非 override diff：
- 加载 = 组件注册表（default）作基准 + Parse 用户值覆盖。
- 新增卡片（注册表有、config 没）→ 按默认 Placement 补。
- 删除卡片（config 有、注册表没）→ 残留条目容错跳过。
- 这套「全量 + 默认补全 + 容错」正是栖梦 `SyncQmModuleLayout`（`:1959-1983`）已验证稳定的模式，泛化到全局。

**为什么不用 override diff**：override 更紧凑，但要额外处理「显式隐藏 / 显式重排 vs 未改动」的状态机，复杂度高。全量模型简单、已验证，v1 用它；override 留作未来优化。

### 5.4 容错（复用栖梦骨架，泛化到全局）
| 情况 | 处理 |
|---|---|
| 未知 id | 跳过该条 |
| 重复 id | 取首次，忽略后续 |
| 非法 tab/column/order | 跳过该条，回退默认 Placement |
| Full 列保护 | Full 卡（如 `info`）不可拖出 Full；非 Full 卡不可拖成 Full |
| order 空洞 | `NormalizeColumns()` 连续化 |
| 新增卡片 | 按注册表默认补 |
| 删除卡片残留 | 跳过 |

### 5.5 迁移（一次性，幂等）
- 新增 `qm_card_order_migrated` 标志，已迁移则跳过。
- 三套旧源 → 新 config：
  - `QmSidebarCardOrder`（栖梦）：按 §3 规则加 `qm:` 前缀，QiaFen 特殊处理。
  - `qm_settings_card_order`（Tclient）：id 已兼容，仅转换列格式（数字→字符串）。
  - 设置 deck 内存 map：**无旧持久化**，迁移时直接用注册表默认（对用户纯增益）。
- 旧 config 迁移后**不立即删除**，保留一版本作回滚锚点。
- buffer 容量：`qm_global_card_order` 需容纳注册表全集。2026-07-07 代码表约 86 卡，按 40-60 字符/卡估算需要 3.5-5KB 量级；容量应以 `sizeof(g_Config.m_QmGlobalCardOrder)` 的序列化截断测试为准，不能沿用栖梦旧 2048 buffer 经验值。

## 6 · 设计决策（我定的 + 理由）

| 决策 | 选择 | 理由 |
|---|---|---|
| 卡片实例模型 | v1 单实例可迁移 | iPhone 类比、兼容现状、简单；多 tab 共享留 v2 口子 |
| 持久化模型 | 全量用户布局 | 栖梦已验证稳定；override diff 复杂度高，v1 不值 |
| stableId 分隔 | `<ns>:<name>` 冒号 | Tclient 现有 id 零改动；业界常见（URI scheme 风） |
| 字段分隔符 | `\|` | 避免与 id 内 `:` 歧义 |
| 列语义 | 保留 Full（字符串） | 栖梦 `info` 卡依赖；Tclient/deck 获得 Full 能力 |
| SmartDefaults | 下沉全局共享 | 栖梦独有能力泛化，Tclient/deck 纯增益 |
| 迁移权威 | 以旧持久化 key 为准 | 规避 QiaFen 三名分裂丢数据 |

## 7 · 待你拍板

1. **单实例 vs 多 tab 共享**（最关键）：v1 我按「单实例可迁移」设计。若你要「一张卡同时出现在多个 tab」，数据模型改为 Placement 列表，设计要返工——请确认。
2. **SmartDefaults 下沉 vs 各 page 自定义**：我推荐下沉共享（Tclient/deck 白赚平衡能力）。若你担心全局算法不适合某些 page，可改为「下沉骨架 + page 可覆盖」。
3. **字段分隔符 `|`**：纯实现细节，若你有偏好（如坚持沿用 `:` 并接受 id 不含冒号）可调整。
4. **buffer 扩容到 4096**：是否接受，或你有 config 长度规范。

## 8 · 性能考量

基于代码核实（`menus_qmclient.cpp` 热路径），全局化（Qm 38 → 注册表全集）的性能要点。

### 8.1 当前热路径（栖梦 38 卡，已核实）
| 路径 | 每帧成本 | 评价 |
|---|---|---|
| 让位 lerp（`:7876-7906`） | O(N)，`GetQmModuleStateIndexById` **O(1)** 枚举下标（`:1594`） | ✅ 健康 |
| 卡片背景合批（`:894-967`） | 1 drawcall，QuadContainer + cull | ✅ 健康（优化成果） |
| `SyncQmModuleLayout`（`:1959-1983`） | **每帧** Serialize 拼 2KB + str_comp×2 + str_copy | 🟠 冗余 |

### 8.2 🔴 全局化关键：state index 必须保持 O(1)
栖梦 `GetQmModuleStateIndexById` 用 `EQmModuleId` 枚举值直接当 state 数组下标，O(1)——这**依赖栖梦模块是连续枚举 0..37**。全局化后 TClient/deck 卡用字符串 stableId，**没有连续枚举**，若退化成线性查找会让位变 O(N²)。

**要求**：启动或 `SetEntries()/Parse()` 后构建 `stableId → 连续 index` 注册表（数组下标或 hashmap），让位 lerp 保持 O(1) 查找。这是全局化的**性能地基**，当前 `CModel::BuildStateIndex()` / `StateIndexForStableId()` 已提供该结构能力，生产拖拽路径是否全量使用仍需审计。

### 8.3 🟠 每帧 Serialize 冗余 → IsDirty 触发
旧 `SyncQmModuleLayout` 每帧序列化布局字符串 + `str_comp` + `str_copy`，**即使 config 未变**。全局化到注册表全集后成本继续放大。

`CModel` 已定义 `IsDirty()`，Qm layout adapter 也提供 `IsQmLayoutModelDirty()` / `ClearQmLayoutModelDirty()`。验收标准是：生产同步路径只在 `Move` / `MoveToTab` 或外部配置变更后序列化，不能在普通渲染帧无条件重建全局字符串。

### 8.4 🟡 其他放大点与对策
- **切页时全集卡同时 lerp**：组件编辑器跨 tab 迁移会让切页时整页 `TargetRect` 突变，所有卡同时 lerp → 可能帧卡顿。对策：**非拖拽态切页时 `DisplayRect` 直接 snap 到 `TargetRect`**（不走 lerp），仅拖拽态走让位 lerp。
- **让位预览无缓存**：`UpdateDropPreview` 每帧重算。对策：DropIndex/被拖列不变时缓存复用预览布局（spec 3.4 已提）。
- **迁移大 Parse**：首次迁移注册表全集时，线性查找会变成 O(条目×N)。一次性但可能卡启动。对策：复用 stableId→index map 让 Parse 降 O(条目)，并记录迁移耗时。

### 8.5 性能验证口径
- 拖拽注册表全集时 frame time 监控，复用现有 `LogQmPerfStage`。
- 目标：让位 lerp + 合批 < 1ms/帧（亚毫秒），切页 snap 无卡顿。
- 迁移耗时单独 log，纳入 B2 验收。

## 附录 · stableId 注册表

> 当前代码的单一事实源是 `src/game/client/QmUi/QmCardRegistry.cpp` 的 `qm_card_registry::Defaults()`。2026-07-07 代码表为 Qm 38 + TClient 19 + deck 29 = **86 张**；下方表格是 2026-06-29 调研快照，只保留命名规范和迁移注意事项，新增/删除卡片以代码注册表为准。

### A. 栖梦模块（38）· `qm:<key>`
显式默认值齐全（`s_aQmModuleDefaults` + tab 分组数组）。

| 新 stableId | 默认 tab | 默认列 | order | 旧 key | 备注 |
|---|---|---|---|---|---|
| `qm:info` | — | full | 0 | `info` | Full 横跨卡 |
| `qm:chat_bubble` | visual | left | 0 | `chat_bubble` | |
| `qm:camera_view` | visual | right | 0 | `camera_view` | |
| `qm:skin_transition` | visual | left | 1 | `skin_transition` | |
| `qm:focus_mode` | visual | left | 2 | `focus_mode` | |
| `qm:weapon_animation` | visual | right | 1 | `weapon_animation` | |
| `qm:entity_overlay` | visual | right | 2 | `entity_overlay` | |
| `qm:collision_hitbox` | visual | right | 5 | `collision_hitbox` | |
| `qm:streamer` | visual | left | 10 | `streamer` | |
| `qm:translate_ui` | visual | left | 15 | `translate_ui` | |
| `qm:card_appearance` | visual | left | 17 | `card_appearance` | |
| `qm:gores_actor` | function | left | 3 | `gores_actor` | |
| `qm:gores` | function | left | 4 | `gores` | |
| `qm:key_binds` | function | left | 5 | `key_binds` | |
| `qm:mini_features` | function | left | 6 | `mini_features` | |
| `qm:jump_hint` | function | left | 7 | `jump_hint` | |
| `qm:weapon_trajectory` | function | left | 8 | `weapon_trajectory` | |
| `qm:coords` | function→hud | left | 9 | `coords` | 调研列 hud，以代码分组为准 |
| `qm:friend_notify` | function | left | 11 | `friend_notify` | |
| `qm:block_words` | function | left | 12 | `block_words` | |
| `qm:qiafen` | function | left | 13 | `qiafen` | ⚠️ UI 名 `keyword_reply`，以 `qiafen` 为权威 |
| `qm:translate` | function | left | 14 | `translate` | |
| `qm:pie_menu` | function | left | 16 | `pie_menu` | |
| `qm:favorite_maps` | function | right | 6 | `favorite_maps` | |
| `qm:hj_assist` | function | right | 7 | `hj_assist` | |
| `qm:player_stats` | hud | right | 4 | `player_stats` | |
| `qm:speedrun_timer` | hud | right | 8 | `speedrun_timer` | |
| `qm:debug_graph` | hud | right | 9 | `debug_graph` | |
| `qm:input_overlay` | hud | right | 10 | `input_overlay` | |
| `qm:hud_notifications` | hud | right | 11 | `hud_notifications` | |
| `qm:voice` | hud | right | 12 | `voice` | |
| `qm:dummy_miniview` | hud | right | 13 | `dummy_miniview` | |
| `qm:dynamic_island` | hud | right | 14 | `dynamic_island` | |
| `qm:system_media_controls` | hud | right | 15 | `system_media_controls` | |
| `qm:lyrics` | hud | right | 16 | `lyrics` | |
| `qm:background_3d` | hud | right | 17 | `background_3d` | |
| `qm:nameplate_text` | hud | right | 18 | `nameplate_text` | 原数据债，已在注册表补 tab |
| `qm:laser` | visual | right | 3 | `laser` | 原数据债，已在注册表补 tab |

### B. TClient section · `tclient:<name>`（id 不变）
当前显式默认 Placement 以 `QmCardRegistry.cpp` 为准；2026-06-29 快照只列了 15 张，2026-07-07 代码表已有 19 张。

| 新 stableId | 归属 tab | 默认列/order |
|---|---|---|
| `tclient:visual-nameplates` | tclient | 代码序（B1 显式化） |
| `tclient:visual-effects` | tclient | 代码序 |
| `tclient:input` | tclient | 代码序 |
| `tclient:anti-latency-tools` | tclient | 代码序 |
| `tclient:improved-anti-ping` | tclient | 代码序 |
| `tclient:execute-on-join` | tclient | 代码序 |
| `tclient:voting` | tclient | 代码序 |
| `tclient:player-indicator` | tclient | 代码序 |
| `tclient:tee-status-bar` | tclient | 代码序 |
| `tclient:tile-outlines` | tclient | 代码序 |
| `tclient:ghost-tools` | tclient | 代码序 |
| `tclient:rainbow` | tclient | 代码序 |
| `tclient:tee-trails` | tclient | 代码序 |
| `tclient:background-draw` | tclient | 代码序 |
| `tclient:finish-name` | tclient | 代码序 |

### C. 设置 deck · `deck:<page>-<card>`
当前显式默认 Placement 以 `QmCardRegistry.cpp` 为准；2026-06-29 快照只列了 16 张，2026-07-07 代码表已有 29 张，包含 Appearance 子页卡片。

| 新 stableId | 归属 page | 备注 |
|---|---|---|
| `deck:graphics-display` | graphics | |
| `deck:graphics-visual` | graphics | |
| `deck:graphics-backend` | graphics | |
| `deck:graphics-modes` | graphics | |
| `deck:sound-toggle` | sound | |
| `deck:sound-volume` | sound | |
| `deck:sound-audio-pack` | sound | |
| `deck:ddnet-demo` | ddnet | |
| `deck:ddnet-gameplay` | ddnet | |
| `deck:ddnet-background` | ddnet | |
| `deck:ddnet-miscellaneous` | ddnet | |
| `deck:tclient-bind-wheel-editor` | tclient-bind-wheel | |
| `deck:tclient-bind-wheel-preview` | tclient-bind-wheel | |
| `deck:tclient-status-bar-settings` | tclient-status-bar | |
| `deck:tclient-status-bar-items` | tclient-status-bar | |
| `deck:tclient-status-bar-preview` | tclient-status-bar | |

### Gap：注册表完整性验收
默认 Placement 的结构已经存在，但完成标准不是“表存在”，而是：

- 所有生产可见卡片都有 `stableId`、默认 tab、默认列、默认 order、标题和搜索关键词。
- Search 页面能覆盖注册表中的全部可见卡片。
- reset/defaults、迁移和新增卡补位都从 `QmCardRegistry::Defaults()` 派生，不再从页面局部数组派生。
- 新增卡片时必须同步注册表测试，避免代码可见但全局搜索/迁移不可见。
