> **已归档，禁止作为实现依据。** 本文保留为历史记录；当前实现请以活动 spec/plan/skill 为准。

# QmClient i18n 人工迁移批次清单

## 批次 A（低风险纯 UI）

- 模块：`console.cpp` / `hud.cpp` / `menus_ingame.cpp`
- 原则：只改纯 UI 文案，不碰聊天预览、解析兼容路径、服务端旁路
- key 命名：优先沿用 DDNet 既有英文 key；若语义不一致，再考虑 `qm.` 前缀
- 当前进度：
  - 已完成：`menus_ingame.cpp` 主按钮与分身连接 tooltip 改用英文 key
  - 已完成：`console.cpp` 聊天记录导出界面与提示文案改用英文 key
  - 已完成：`hud.cpp` 分身小窗与分身状态行改用英文 key
- 本批次验证：
  - `QmNewUiMenuBranches.IngameMenuPrimaryActionLabelsUseEnglishKeys` 已补成英文 key 结构测试并通过
  - `QmNewUiMenuBranches.ConsoleChatExportLabelsUseEnglishKeys` 已通过
  - `QmNewUiMenuBranches.HudDummyStatusLabelsUseEnglishKeys` 已通过
- 状态：已完成

## 批次 B（静态 Localizable）

- 模块：`menus_settings_controls.cpp` / `touch_controls.cpp`
- 原则：只改 `Localizable("中文")` 静态项
- 风险：中低
- 当前进度：
  - 已完成：`menus_settings_controls.cpp` 的分身/旁观绑定标签改用英文 key
  - 已完成：`touch_controls.cpp` 中对应默认标签改用英文 key
  - 已补：`data/languages/simplified_chinese.txt` 中缺失的最小简中映射
- 本批次验证：
  - `QmNewUiMenuBranches.DummyAndSpectateBindLabelsUseEnglishKeys` 已通过
- 状态：已完成

## 批次 C（待判定混合文本）

- 模块：`chat.cpp` / `menus_browser.cpp` / `menus_demo.cpp`
- 原则：先逐项决策是否迁移
- 风险：中高
- 当前进度：
  - 已完成：`chat.cpp` 翻译设置弹窗标题、开关标签、语言/后端标签改用英文 key
  - 已完成：`menus_demo.cpp` 截图预览失败提示、截图目录 tooltip 改用英文 key
  - 已完成：`menus_browser.cpp` 收藏列表表头与空备注占位改用英文 key
  - 保留待裁决：`chat.cpp` 命令预览/帮助类字符串、`menus_browser.cpp` 分类与难度语义映射、其它混合语义文本
- 本批次验证：
  - `QmNewUiMenuBranches.TranslationAndDemoUiLabelsUseEnglishKeys` 已通过
- 状态：进行中

## 历史译法审计

- 工具：`qmclient_scripts/languages_qmclient/audit_translation_drift.py`
- 作用：对比当前 `translations/i18n/*.toml` 与 Git 历史里的 `data/languages/simplified_chinese.txt`
- 用途：只读检查历史译法是否被新维护源改偏，不参与运行时生成链
- 状态：已接入文档与脚本说明

## 批次 D（运行时 / 资源 / overlay）

- 模块：`gameclient.cpp` / `src/test/qm_new_ui_menu_branch_test.cpp` / `data/qmclient/languages/` / `data/languages/`
- 原则：只在前几批基本收口后进入
- 风险：高
- 状态：未开始
