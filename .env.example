# Copy this file to .env and fill local secrets.
# .env is ignored and must not be committed.

DEEPSEEK_API_KEY=

# Optional fallback for other OpenAI-compatible local HTTP translation providers.
QMCLIENT_LOCAL_HTTP_API_KEY=
